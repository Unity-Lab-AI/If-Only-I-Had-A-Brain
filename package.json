{
  "name": "if-only-i-had-a-brain-tooling",
  "version": "0.1.0",
  "private": true,
  "description": "Repo-wide dev tooling (build/diagnostic scripts). The runtime app lives in server/ (own package.json) + js/ + static HTML. This manifest is dev-only and is excluded from the pages deploy.",
  "type": "module",
  "scripts": {
    "//": "BUNDLEFIX (2026-08-20) — `build` delegates to server/, where the real esbuild command lives. Both launchers (linux/start.sh step 6/7, start.bat) run `npm run build` from the REPO ROOT, and root scripts were {} — so the bundle rebuild has been failing on every single launch, printing 'ERROR: esbuild bundle build failed. The browser will run STALE code' and then continuing anyway. Found while rebuilding the bundle by hand for the gate-phase change. Delegating here fixes both launchers without editing either, and keeps server/package.json as the single source of the actual esbuild flags.",
    "build": "npm --prefix server run build",
    "build:voiceworker": "npm --prefix server run build:voiceworker"
  },
  "devDependencies": {
    "playwright": "^1.49.0"
  },
  "dependencies": {
    "@diffusionstudio/piper-wasm": "^1.0.0",
    "@mintplex-labs/piper-tts-web": "^1.0.4",
    "onnxruntime-web": "^1.18.0"
  }
}
