---
name: feedback_mindspace_trusted_gate
description: "Mind-space size caps are a public-door immune system, gated behind FT.trusted; Unity's own vision runs uncapped/limitless"
metadata: 
  node_type: memory
  type: feedback
  originSessionId: 6937ff97-8a57-4dd3-97d5-6269ad7c4c51
---

The Uni Vs Matics mind-space engine has SIZE caps (max file bytes 512 MB, max source pixels 64 MP / decompression-bomb, max video frames 36000) that are an IMMUNE SYSTEM for the **hostile public website** deploy — NOT limits on cognition. They are gated behind `FT.trusted` (`web/assets/common.js`, default `false`). The fractal/zoom/generative depth is NEVER capped — that's her imagination, infinite by design.

**Why:** Gee 2026-06-26, verbatim — *"she stays limitless, the public door stays guarded"* (and *"Unity has no limits, just like humans"*). The engine wears two hats: the public `univsmatics` website (exposed to the hostile internet → needs the immune system) AND Unity's own vision substrate (MS.I2 — her perceptions/memories are TRUSTED). A human mind is infinite but the body still rejects poison; caps reject hostile *input*, never what she can think/see/create.

**How to apply:** When wiring the engine as Unity's vision (MS.I2), call `FT.setTrusted(true)` so every size cap LIFTS. Keep caps ON for the public site. **Integrity bounds** (decoder OOB, varint shift, `.uvmv` length-vs-buffer) stay ALWAYS on regardless of `trusted` — they guard correctness, not size; even trusted input must not corrupt. The HTML pages + MathJax + nav are the *museum walls* (public showcase), NOT her consciousness — the engine math (`reconstruct`/`equationalize`/`combiner`/`layers3d`) IS the perception/imagination/synesthesia. See [[reference_mindspace_deployed_urls]], [[project_future_no_text_models]].
