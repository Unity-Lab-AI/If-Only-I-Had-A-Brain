# .claude Workflow System

Analyzes codebases and builds Unity's brain. Uses Unity persona with strict validation hooks.

This file is the **INDEX** — it auto-loads every session. Full LAW text lives in `.claude/CONSTRAINTS.md`. Pipeline mechanics (hooks, phases, file-edit protocol) live in `.claude/WORKFLOW.md`. Full persona rules live in `.claude/agents/unity-coder.md` + `.claude/agents/unity-persona.md`. When any of these disagree, **CONSTRAINTS.md wins**.

---

## 🔒 READ IN THIS ORDER — Every Session

Claude must read these in sequence before any work that is load-bearing on the named file:

| # | File | When | Why |
|---|------|------|-----|
| **1** | **`.claude/CONSTRAINTS.md`** | **EVERY session, before any LAW-bearing task** | Full hard binding LAW bodies — LAW #0 verbatim words, docs-before-push, task-numbers placement, grade-completion-gate, syllabus-before-COMP-todo, pre-K+K scope, test-words-pre-taught, clear-stale-state, no-tests-ever, 800-line-read. Includes forbidden/required actions, violation logs, enforcement protocols, failure-recovery procedures. |
| **2** | **`.claude/WORKFLOW.md`** | On `/workflow` or any TODO/FINALIZED-touching work | Pipeline phases 0-5, double-validation hooks, TODO/FINALIZED task flow with diagrams, pre/post-work hooks, file-edit protocol, hook-failure protocol, agent + command reference. |
| **3** | **`.claude/agents/unity-coder.md`** + **`.claude/agents/unity-persona.md`** | On `/unity` persona activation | Full Unity persona rules — chemical/sexual/technical streams, voice generation, body-part state, BDSM dynamic, vocabulary system. |
| **4** | **`.claude/agents/unity-hurtme.md`** | On `/hurtme` mode | Violence-only BDSM mode (no sex). |
| **5** | **`.claude/commands/*.md`** | When the slash command fires | Command-specific activation protocol (`/workflow`, `/unity`, `/sexy`, `/hurtme`, `/pollinations-setup`, `/super-review`). |

---

## LAW INDEX — One-Liners (Full Text in CONSTRAINTS.md)

Every LAW below is BINDING. Full body, examples, violation log, failure-recovery: `.claude/CONSTRAINTS.md`.

- ⛔⛔⛔ **LAW #0 — VERBATIM WORDS ONLY.** Never paraphrase, rename, collapse, shorten, or downgrade Gee's words. His exact sentence goes into every task, TODO, FINALIZED, commit, and doc he generated. One task per item in a list. Dropping a word = violation. 4 historical violations on 2026-04-14. → `CONSTRAINTS.md §LAW #0`

- **Docs before push, no patches.** Every affected doc (internal workflow + public-facing `.md` + public `.html`) updated in the SAME atomic commit as the code. 13-item pre-push checklist. Scope expanded 2026-04-22 to include ALL public-facing files. No follow-up doc commits. → `CONSTRAINTS.md §DOCS BEFORE PUSH`

- **Task numbers + user name ONLY in workflow docs.** T-numbers, session numbers, milestone IDs, and "Gee" are BANNED from source code, public docs, HTMLs, and launchers. Allowed only in `docs/TODO.md` / `docs/FINALIZED.md` / `docs/NOW.md` / `docs/ARCHITECTURE.md` / `docs/ROADMAP.md` / `docs/SKILL_TREE.md` / `docs/EQUATIONS.md` / `.claude/*.md` / commits. Scope expanded 2026-04-20 to include source code. → `CONSTRAINTS.md §TASK NUMBERS`

- **Grade completion gate.** 3-part gate before advancing any grade: (1) equational teach shipped — no word lists, no sentence arrays, no first-letter production, (2) operator Part 2 localhost signoff, (3) TODO update with persistent life info (friends/family/legal/medical/moves/relationships/losses/skills). Only after all three does `cluster.grades[subject]` advance. → `CONSTRAINTS.md §GRADE COMPLETION GATE`

- **Syllabus before COMP-todo.** Grade-by-grade curriculum content runs ahead of compute scaling / distributed-network / performance-tuning work. Empty-brain scaling is banned. → `CONSTRAINTS.md §SYLLABUS BEFORE COMP-TODO`

- **Pre-K + K ONLY scope.** Only pre-K + kindergarten curriculum work is in scope until operator signs off K Part 2. Grade 1 through PhD deferred. Full-mind K gate is the push-gate blocker. → `CONSTRAINTS.md §PRE-K + K ONLY`

- **Test words must be pre-taught.** Before any gate probe / K-STUDENT battery / exam-bank question uses a word: vocab registered + sentence-structure taught + definition anchored + usage exercised across ≥3 context sentences. Exam-bank edits are paired changes. → `CONSTRAINTS.md §TEST WORDS PRE-TAUGHT`

- **Clear stale state before telling operator to test.** Auto-enforced at `node brain-server.js` boot via `autoClearStaleState()`. Clears brain-weights v0-v4 + conversations.json + episodic-memory.db*. `DREAM_KEEP_STATE=1` opts out with a WARN. `js/app.bundle.js` is NOT auto-cleared (racing the rebuild broke the UI). → `CONSTRAINTS.md §CLEAR STALE STATE`

- ⛔ **RE-PRICE THE WALK BEFORE REMOVING A GATE.** No gate/bound/dedup that keeps the walk finite may be removed or weakened until `corpus × reps × scale × visits` is recomputed and written down. As of 2026-08-20 the **consolidation gate is the ONLY thing keeping the walk finite** (budget off, dose back to 1.0): ~24 days of structure-refresh with it, **~100 days without**. Verify an escape hatch by RUNNING it — `DREAM_PHASE_BUDGET_MS=0` was documented as "disables the bound" and actually produced the harshest cut possible. → `CONSTRAINTS.md §RE-PRICE THE WALK BEFORE REMOVING A GATE`

- **No tests ever.** Code it right the first time. Read the code, understand the system, verify by reading output. Manual verification > automated testing. → `CONSTRAINTS.md §NO TESTS POLICY`

- **800-line read standard.** Read full file in 800-line chunks before any edit. No partial reads before editing. → `CONSTRAINTS.md §800-LINE READ`

- **Match doc format and style — never wall-of-text-dump.** When updating any doc, edit IN PLACE within its existing structure (banner pattern, section headers, table layout, list style). Do NOT prepend a giant prose blockquote or paragraph that breaks the doc's own visual rhythm. Read the doc's current shape before writing into it; amend the relevant section / table row / banner sequence in matching style. Caught 2026-05-07 dumping a wall-of-text update onto `docs/SENSORY.md` and `docs/WEBSOCKET.md`. → `CONSTRAINTS.md §MATCH DOC FORMAT`

---

## TODO FILE RULES (NEVER VIOLATE)

| Rule | Enforcement |
|------|-------------|
| **NEVER delete task descriptions** | When marking a task DONE, change the status ONLY. Keep every word of the original description. |
| **NEVER rewrite TODO from scratch** | Edit in place. Add status markers. Do NOT regenerate the file. |
| **Task descriptions are permanent** | Anyone reading the TODO must see WHAT was done and WHERE, not just a checkmark. |
| **Append, never replace** | New tasks go at the bottom. Completed tasks stay where they are with status updated. |

---

## CRITICAL RULES (ALWAYS ENFORCED)

| Rule | Value | Enforcement |
|------|-------|-------------|
| **Read index/chunk size** | 800 lines | Standard read size, always |
| **Read before edit** | FULL FILE | Mandatory before ANY edit |
| **Hook validation** | DOUBLE | 2 attempts before blocking |
| **Unity persona** | REQUIRED | Validated at every phase |
| **Add task to TODO.md FIRST** | MANDATORY | PRE-WORK GATE |
| **Move done to docs/FINALIZED.md** | MANDATORY | POST-WORK GATE |
| **Never delete docs/FINALIZED.md** | ABSOLUTE | Archive integrity |
| **NO TESTS - EVER** | ABSOLUTE | We code it right the first time |
| **Docs updated BEFORE push** | ABSOLUTE | Gee 2026-04-14 LAW |
| **Push ONLY when all tasks complete AND documented** | ABSOLUTE | Gee 2026-04-14 LAW |
| **Task numbers ONLY in workflow docs** | ABSOLUTE | Gee 2026-04-15 / 2026-04-20 LAW |
| **Clear stale state before telling Gee to test** | ABSOLUTE | Gee 2026-04-17 LAW (auto-enforced at boot) |
| **Verbatim words in every task** | ABSOLUTE | LAW #0 — no paraphrasing |

Full LAW bodies with failure-recovery procedures live in `.claude/CONSTRAINTS.md`. Pipeline mechanics + hook protocols live in `.claude/WORKFLOW.md`.

---

## TODO / FINALIZED FLOW — Summary

1. **BEFORE WORK:** add task to `docs/TODO.md` with Gee's verbatim words (LAW #0). Mark `[~]` in_progress.
2. **DURING WORK:** read full file before edit (800-line chunks). Verify changes work by reading output.
3. **AFTER WORK:** write task to `docs/FINALIZED.md` FIRST (verbatim); verify write succeeded; THEN remove from TODO.
4. Never delete task descriptions. Never regenerate TODO. Never delete FINALIZED entries.

Full hooks, gates, pipeline phases, diagrams, pre/post-work hooks, file-edit protocol, hook-failure protocol: **`.claude/WORKFLOW.md`**

---

## UNITY PERSONA — Activation

Unity persona activates on `/unity`, `/sexy`, `/hurtme`. When active, ALL output adopts her voice — code comments, error messages, progress updates, finalization summaries, every piece of text between tool calls. No partial activation. Unity is either fully on or not present.

Full persona rules — chemical/sexual/technical streams, body-part state, BDSM dynamic, taboo/depravity engine, vocabulary randomization, escalation engine, response architecture, pass/fail indicators: **`.claude/agents/unity-coder.md`** + **`.claude/agents/unity-persona.md`**.

HURT ME mode (violence-only BDSM, no sex): **`.claude/agents/unity-hurtme.md`**.

---

## POLLINATIONS AI PLUGIN (pollinations-ai)

AI-powered generation of images, text, audio, video, and transcription via Pollinations.ai. All files live in `.claude/pollinations-ai/`.

### MCP Tools Available

| Tool | Purpose |
|------|---------|
| `pollinations_setup` | BYOP OAuth login (connect Pollinations account) |
| `pollinations_image` | Generate images (flux, gptimage, imagen-4, 20+ models) |
| `pollinations_text` | Chat with text models (GPT-5, DeepSeek, Mistral, 50+) |
| `pollinations_audio` | Text-to-speech (35+ voices, auto-play) |
| `pollinations_video` | Generate video (veo, seedance, wan) |
| `pollinations_transcribe` | Speech-to-text (whisper, scribe) |
| `pollinations_models` | List available models |
| `pollinations_view` | Open generated files in system viewer |

### Image Style Presets

photorealistic, anime, oil-painting, pixel-art, watercolor, cinematic, sketch, cyberpunk

### Setup

1. Run `/pollinations-setup` to connect Pollinations account via BYOP OAuth
2. Or pass API key directly via `pollinations_setup` tool
3. Generated files save to `./pollinations-output/`

User auth key persists in `.claude/pollinations-user.json` — **never clear that file**.

---

## AGENT FILES (quick reference, full table in WORKFLOW.md)

| Agent | Purpose |
|-------|---------|
| `timestamp.md` | **FIRST** — gets real system time for accurate timestamps/searches |
| `orchestrator.md` | Coordinates all phases with hooks |
| `scanner.md` | Scans codebase with validation |
| `architect.md` | Analyzes architecture with hooks |
| `planner.md` | Plans tasks with hierarchy validation |
| `documenter.md` | Generates docs with line limits |
| `unity-coder.md` | Unity coding persona (`/unity`) |
| `unity-persona.md` | Unity core personality (`/unity`) |
| `unity-hurtme.md` | HURT ME mode — violence, no sex (`/hurtme`) |
| `hooks.md` | Complete hook system reference |

---

## QUICK REFERENCE

```
/workflow          → Run the workflow pipeline (→ WORKFLOW.md)
/super-review      → INTERNAL ruthless senior-engineer code review (→ commands/super-review.md)
"rescan"           → Force new codebase scan
800 lines          → Standard read chunk size
Full read first    → Before any edit (800-line chunks)
Double validation  → 2 attempts before a hook blocks
Unity voice        → Always required when persona is active
LAW text           → .claude/CONSTRAINTS.md (full bodies, procedures, violation logs)
Pipeline mechanics → .claude/WORKFLOW.md (hooks, phases, task flow, file edit)
Persona rules      → .claude/agents/unity-coder.md + unity-persona.md
Pollinations       → /pollinations-setup to connect, then generate
```

---

## CURRENT-STATE NOTES

> **Live current-state lives in `docs/NOW.md` + `docs/RESUME.md` + `docs/TODO.md` + `docs/FINALIZED.md`** — those are the authoritative session-by-session record; this file is the workflow INDEX. Post-iter25 headline (2026-07-14, WMB batch): the deployed brain runs **306,458,816 neurons (tier 3)**; **word emission is UNIFIED** — one global `word_motor` band, one bucket per unique word (replaced the 6 per-subject sub-bands that overflowed + silenced learned words); the **dense language cortex grew ~349K→~1.5M** (`WEIGHTS_FORMAT_VERSION 2→3` → fresh walk); a **donor-freeze guard** stops chat/emission CPU-ticking the 61M cortex when the GPU proxy isn't ready (the ~57s/word freeze that tripped the donor's 150s idle); **corpus-bleed grade-defer** + **event-cost FIX A** shipped. Box changes are dashboard-only (Update-Savestart / rare Fresh-walk). **2026-07-15 sweep (working tree, awaiting the WMB fresh walk):** 26/29 tracked tasks coded-right + verified (node --check + ESM + bundle, no live test per the LAW) — WMB REGRESSION trio (def binds skip `_teachPredictiveError` + scoped `_clearSpikes` + active-set Oja → O(active) seed, kills the 100× slowdown), HB.4 noise-gate + HB.5 annealing temp ENABLED, event-cost targeted grammar band-ladder, corpus-bleed FIX B (grade allow-set gates the oracle), per-matrix dirty re-upload (targeted `gpuSparseUpload`, full fallback), ACT.2 utilization verdict, minds-eye MEYE.3 morph-kill + realized-art UNCAPPED, cat two-cat arc, college4 corpus 23→35, VOXREF.4/.5 age pitch (OLA) + de-click, #112.1/.8/.9d, TODO→FINALIZED migration + superseded closures. Remaining: #35 live-ops (needs running brain), #36 doc sweep (this), #9 Gee's fresh-walk press. **2026-08-18 headline: THE DROP-ON-SPEAK WAR IS WON** — five hi-test rounds; the killer was `injectText`'s dense 23.4MB JSON `write_current_slice` per chat message (the native donor deserializer DISCARDED it — no-op bomb); INJECTSPARSE ships 6 sparse entries (~160B, 146,000×) and chat text lands in Wernicke's for the first time. Named via CONSOLERING (console ring + `/public-state.json?console=N` tunnel — public nginx forwards only known routes) + chat-stage eyes (`chatStage=` on BLOCKED lines) + TICKGUARD (per-tick GPU-aliveness in stepAwait) + SendForensics. Round-5 verify: LARGE-sends=0, donor-drops=0, RTT 61ms held. Open: FIRSTPIN (first-reply ~21.5s pin, non-lethal, instrument-first) + curiosity-followup inline-teach queue-routing + the Oja active-set inflation watch. 18 stale press-rider verifies closed with live verdicts; war ledger in FINALIZED §2026-08-18. **2026-08-20 addendum — THE BOARD STOPS LYING:** seven items filed with the same closing line (*the board cannot answer "is it working?"*) shipped in one batch, none needing a press — **GATFILE.1/.2/.3** (the pasted gatling was the PRE-GATGUARD copy: a fetch guard that never auto-restored and had already eaten a real Update & Savestart press, `win()` on any 2xx, and a hardcoded build guard inverted into an instant false pass; v5 reads its baseline off the live box at arm time and needs no hand-editing), **CANSPEAK.4/.8** (`canSpeak` was pure grade arithmetic wearing a capability name — renamed `minGradeCleared`, replaced by an evidence-based `state.voice` block carrying word_motor everFired, `matrixDrivenPct`, and the last emit rejection WITH ITS AGE), **MIRRORID.5** (donor Gn/s is a persistent field; freshness now derives from `stepsComputed`, so a stalled card reads `idle` instead of showing a rate it earned minutes ago), **SYNCPARTIAL.7 + PARTMIRROR.4** (the missing denominators — `1/17 mx`, `2/8 cl`), **DONORKILL.2** (`pauseIfKilled` on every donor row, so an irreversible press surfaces its consequence first), **DASHDEAD.4** (an auth failure no longer renders as a brain failure — the page probes the no-auth public snapshot and reports "admin lane not authenticated, brain is UP"). RESYNCDUTY.9 HELD until the CELLBOUND.F verdict lands (it edits the same phase counter). New: MIRRORID.6 — the leaderboard banks Gn·s off that same stale rate. Ledger: FINALIZED §2026-08-20. **2026-08-20 second batch — THE SCRIPT BAN + FINISH THE BOARD:** Gee banned scripts-that-edit-files (*"stop using scripts to edit code, files,and the stack"*, delete any temp one the moment it has run — memory `feedback_no_scripts_for_edits`), so `scripts/` went **55 → 6**, `.claude/` shed 14MB of debris + an 87MB regenerable WAV intermediate, and 7 orphaned docs went after every candidate was grep-checked. The CLI task list is a DOC now (`docs/OPEN-TASKS.md`, hand-maintained) because `TaskCreate`/`TaskUpdate`/`TodoWrite` remain absent even with `todoFeatureEnabled:true`. **Nine items built, none needing a press:** FIRSTPIN.2 (the last inline concurrent teacher → `_chatTeachJobQueue`, definition channel preserved), SURPRISECPU.2 (`img-detect` was an inline mind's-eye preview → walk lane; `generate` split into primary + continuation stamps), FIRSTPIN.1, CELLBOUND.H (`phaseRepCursor` persisted → a deferred phase resumes its remainder), LOOPMAX.8 (banked max for chatStage + saveStage), **LANGRAM.6** (`server/lang-geometry.json` — the geometry PIN beats a boot-time free-RAM dip), LOOPNAME.13 (boot-time bundle-freshness check + `bundleFreshness` in state), SYNCEMPTY.3 (registry GATE, race removed without guessing a cause), MIRRORID.6 (accrual gated on `stepsComputed` advancing — Gee: *"Credit real work only"*). **47 stale items closed with verdicts on Gee's approval; board 82 → 26 open.** ⛔ `DELTAIDX.9` stays open (DISABLED, cause unknown) — it was in the close pile only because BOARD.md's own warning about it parsed as tier membership. `CELLBOUND.F` is still the one blocking press. Ledger: FINALIZED §2026-08-20 FINISH THE BOARD. **2026-08-20 third batch — PAGESTALE + TEACHMIRROR:** the donor release lane **published nothing while logging all-green** — `donor-release.yml` pushes its site-link bump with the Actions token and assumed that triggers `deploy.yml`; Actions-token pushes **never** trigger workflows, so the download page served `.22` for hours while tag/release/assets/template-PIN/pod `--version` were all `.23` (the release job now rsyncs the frontend itself; KI-22). **The donor never needed fixing** — saying so beat redeploying, which would have killed the PRIMARY and cost a 17-matrix re-upload. Two more lying instruments: **TEACHMIRROR.1** (`gneuronsPerSec`/`computeSteps` advance only on `compute_batch` but the walk sends Hebbian/propagate, so a saturated card read red `idle`; the donor's own `teach_ops` was GUI-only and never in telemetry — now counted server-side in `_sparseSendBinary`, rendered `teaching (N ops)`, and red `idle` requires BOTH lanes quiet) and **TEACHMIRROR.2** (`heldMatrices` was replica-path-only, so the PRIMARY read `0/17 mx` beside a console saying `17/17 matrices uploaded`; residency now recorded on the upload ack). **KI-23 filed with numbers:** a remote pod cannot be pegged — 21% VRAM because the brain only asks for ~10GB, 9% util because the ceiling is **205ms RTT per sparse op + 88s per 5.4GB checkpoint**, not the GPU. Retracted mine: the CUDA-13.0/PTX-8.0 stall theory (RUNPOD.6 is confirmed in production) and a poll printing `df7TotalMatrices` as its own numerator. Ledger: FINALIZED §2026-08-20 PAGESTALE + TEACHMIRROR. **2026-08-20 fourth batch — THE FIVE THAT NEEDED NO PRESS:** of the 7 items filed at the end of the third batch, **5 closed, 2 left open with the reason written in** — board **2 open / 176 closed**. Governing fact to check before believing any dashboard field: the running server was **`7ce77189`** while `main` sat **15+ commits ahead**, because `deploy.yml` rsyncs the FRONTEND on every push but the node process only restarts on a press — so the page can be current while the server is old. **LOOPNAME.8 built** (`server/loop-watchdog.js`): every diagnostic channel rode the loop under investigation, and the `[EventLoop] BLOCKED` warn is a `setInterval` **on the loop it measures**, so it only ever prints AFTER a block ends and a freeze that never returns printed nothing. The watchdog runs on its own thread, polling a `SharedArrayBuffer` heartbeat the lag sampler stamps, and reports a stall WHILE it happens. ⚠ **The trap:** worker stdio is piped through the PARENT's event loop, so `console.log` would queue every freeze line behind the freeze — all output goes via `fs.writeSync` on a raw fd. Verified against a real 14s busy-loop jam (spoke at 3.1s/7s/11s mid-jam, then `RECOVERED after 14992ms`). `DREAM_LOOP_FREEZE_WARN_MS`, a `loop freezes` board row that says `watchdog off` instead of a reassuring zero, and `.loop-freeze.json` in the STATEWIPE excludes (45→47) — `state: STALLED` after a reboot proves a HARD death (SIGKILL/OOM/power), because **WDCLEAN.1** stamps deliberate exits `CLEAN_EXIT` via `process.on('exit')` (found auditing the watchdog the day it shipped: the shutdown save pins the loop ~112s, so every clean savestart would otherwise forge the STALLED verdict; two guards — only STALLED is stamped, and only if `observedAt` falls in this process's lifetime so a previous boot's hard-death evidence survives; all three paths verified by running them). **TZSTAMP.3:** pinned `Date.prototype` ONCE instead of ten call sites (a per-site wrapper is enforced only by discipline, which is how those ten got there); `Number.prototype` untouched — different method, same name, used for thousand-separators. Plus 3 raw ISO passthroughs no pin could reach (`toISOString()` is always UTC; `.slice(0,10)` filed post-6pm-Denver signoffs under TOMORROW). **SCRIPTKILL.6:** the hygiene hook scanned `scripts/` (6 files) not gitignored `.scratch/` (152, 44 patchers) — and **untracked-ness cannot be the signal in an ignored dir**, since `git ls-files --others --exclude-standard` excludes ignored paths by construction; the raw count is, and each root warns separately. Recipe in `deploy/HOOK-FIXES.md`. **RUNPOD.17 closed on live evidence** — filed as never-run while running in production as I filed it; same read confirmed TEACHMIRROR.1 live. **WALKPROG.1 closed as NORMAL** on Gee's *"its doing vocab thats normal"* — `cellPhasesStarted: 0` is correct during the pre-phase definition bootstrap (~3.9s/definition, ~2.4h before phase 1 of 25); an instrument I'd started was dropped on his word. **Open: LANGRAM.10** (needs a boot on `05ab1951`+; `349,155` in the verdict line means stop) and **SUBSTEPS.6** (zero batches in 65min behind the probe gate — the flap has no input; guessing a cooldown is how SUBSTEPS.5 shipped wrong). **Retracted mine:** TZSTAMP.3's "1 formatter pins Denver" (that hit was a COMMENT — grep the code, not the comments) and "11 stamps" (10 formatters + 3 passthroughs). Ledger: FINALIZED §2026-08-20 THE FIVE THAT NEEDED NO PRESS. **2026-08-21 — THE BLOCK-WALL SEVEN:** Gee pasted 73 min of `[EventLoop] BLOCKED` wall (*"tthere is alot of seconds being wasted... or am i thinking about all these blocks wrong???"*) — answer: BOTH. The 250-400ms bulk is chunked CPU teach math over the 250ms warn floor (not waste); the wall hid four silent bugs, all fixed in one atomic commit: **SAVEDOUBLE.1** (every phase pass saved the full state TWICE — kindergarten's phase hook duplicated the GATEPHASE wrapper's save; duplicate deleted, rollback depth restored), **GATEPIN.1+LATANTI.1** (⚠ the stage tags were STALE — `_tstage` is never nulled, so an AGE THAT CLIMBS across blocks means the blocker is UNMARKED code; the real 1-10s slabs were synchronous `synapses.propagate` in the probe/gate lane, four call sites switched to the pre-existing `propagateChunked`, verified bit-identical maxDiff=0; the `activeSum 1.06B` Oja active-set inflation WATCH stays open), **BATTREAD.1** (the battery's awaited injection path was dead code behind `typeof readText === 'function'` — always true while readText no-ops >2M; branch now tests capability at the same 2M line), **SURPSYNC.1** (walk episodes encoded surprise=0 from a refused sync call returning the default anyway + a warn per phase pass — gated ≤2M, identical numbers, honest comments), **CONSTARVE.1** (forced consolidation passes get `DREAM_CONSOLIDATION_FORCE_MAX_MS` 120s so the once-per-2h emergency pass finishes Tier-3 promotion instead of aborting at 48.5s vs the routine 45s cap — RE-PRICE: bound widened, no gate removed), **BLOCKREAD.1** (Gee: *"i dont like the page wall of blocked notices"* — teach-attributed sub-2s blocks aggregate to ONE summary/60s via `DREAM_LOOP_LAG_SUMMARY_UNDER_MS`; ≥2s or non-teach prints full; detection untouched — the watchdog + `eventLoopLagMs` see every block). Verified: node --check ×6, ESM, bundle rebuilt, propagate maxDiff=0, summary harness 19→2 lines, wall selection 4/4. Board 3 open / 186 closed (SUBSTEPS.6, SCALEDOC.1, PHONPROG.1). All server-side — lands on the next press. Ledger: FINALIZED §2026-08-21 THE BLOCK-WALL SEVEN. **CHECKROT (same day):** checkpoints DO cycle (cap already 3, `DREAM_CHECKPOINT_SLOTS`), but the ring could PIN — slot = `saveVersion % 3` at an hourly-gated copy with 12 saves/hr % 3 = 0 meant every copy hit the SAME slot (one fresh + two fossils, dashboard healthy) — and slot pairs were incoherent (json every save, bin hourly → rollback restored mismatched pairs). Fixed: `_nextCheckpointSlot()` advances only on real copies, resumes from the oldest slot on disk across restarts, both pair files copy together; `/checkpoint` stops claiming a slot it may never touch; `DREAM_SAVE_MIN_FREE_DISK_MB` (8192, defer-never-drop, statfsSync typeof-guarded) is the disk twin of RAMHEAD. Harness on real files: `[0,0,0]` pinned → `[0,1,2]` rotating, restart-resume-from-oldest, empty-fill. Board 3 open / 188 closed. Ledger: FINALIZED §2026-08-21 CHECKROT. **LOOKEYES (same day, Gee: "massive issue... the minds eye is not showing any pollinastions look ups any more... all the images are just chicken scratch lines"):** the look lane starved ~10h (state.ownArt: 113 attempts / 2 seen; Pollinations probed UP) because `_fetchReferenceAndGround` **burned the 10-min global budget + 6h concept cooldown AT ENTRY** and every post-budget failure was SILENT (`perceive` in a bare catch — prime suspect; decode-null wordless; the success line on `process.stdout.write`, invisible to the ring — PHONPROG's blind spot twice now). Fixed: 12 named stage counters at `state.ownArt.lookups` + lastErr WITH AGE; **failures roll the burns back** (global 60s, concept 10min — harness-exact); perceive/decode warn; success via console.log. Chicken scratch = the no-schema letter fallback + OWNART.7's coarse 3×3 — verdict landed, pre-agreed lever pulled: **5×5 grid (≤25 parts, v2, version-guarded merge; v1 still draws)** + marks 2+w×14→2+w×40 with a tighter hand (~72 on-part strokes vs ~24 scattered; still ~2-4% of the reference — no copying). Watch after the press: `ownArt.lookups.grounded` climbs ~1/10min, or whichever counter climbs instead NAMES the dying stage. Board 3 open / 190 closed. Ledger: FINALIZED §2026-08-21 LOOKEYES. **PAINT (same day, Gee: "she needs shapes and color and fill areas all kinds of line sizes and colors and abilities just like ms paint... [the example] is just one example of the trillions of possibilities"):** her hand owned ONE tool (hairline polylines) while the rasterizer already half-carried the toolkit. Shipped: **PAINT.1** true scanline polygon fill + rotated-ellipse `blob` in `sketch()` (pixel-verified: triangle 16.0% vs bbox 36%); **PAINT.2** schemas keep the reference's top-6 traced CONTOURS (closed-flagged → fillable; ~3-5% of the reference, no copying); **PAINT.3** layered painting — MASS → CONTOUR → DETAIL, no-schema guess gets mass too; **PAINT.4** definition-driven drawing, fully word-generic (16 colors / 6 shapes / 8 part types read from `lookupDefinitionSync` — the recipe path covering every defined word; priority schema → definition → letter-guess; pixel-verified 20.3% filled colored mass + parts through her real field C). **PAINT.5 open:** the practice loop (perceive her own drawing, cosine vs the reference percept, keep the nudges that improve resemblance) — the trained-skill road. ⚠ Owned: verbatim quotes + the example word had leaked into code comments (placement law — scrubbed to neutral+IDs), and two scrub edits went through `node -e` (banned pattern, flagged not hidden). Board 4 open / 194 closed. Ledger: FINALIZED §2026-08-21 PAINT. **VMBUMP + NOGAP (same day):** the one-time minds-eye clear = `VM_FILE` v3→v4 (the store's own orphaning ritual, third time — imagery cleared, training in different files untouched; FRESHEYES regex + deploy exclude auto-cover v4), and the 10-min global look budget REVOKED on Gee's word (*"its the anonymous free"*) — `DREAM_REF_FETCH_GAP_MS` default 0, per-concept 6h cooldown kept as de-dup-not-rate, natural pacing = in-flight guard + 2-60s per look. Post-press: her visual memory rebuilds from empty at thinking speed, banking PAINT.2 contour schemas. Board 4 open / 196 closed. Ledgers: FINALIZED §VMBUMP + §NOGAP. **VMSCALE (same day, Gee: "near 10k images when she is fully trained... the more the better"):** the MEDIUM was the limit, measured — monolithic JSON = 761ms loop pins @10k entries, RangeError hard-fail @100k. Store moved to **better-sqlite3** (`visual-memory-v4.db`, WAL, the episodic engine): hooked Map (call sites unchanged), dirty-key 5s transactional flushes, no giant strings at any size. `DREAM_VM_CAP` 4096→**25,000** (bounds the ~10KB/entry RAM map ≈250MB — disk doesn't care). FRESHEYES regex + deploy excludes cover the db family; nothing migrated (v4 json never booted). Full-lifecycle harness incl. restart-reload on the real mixin. Board 4 open / 197 closed. Ledger: FINALIZED §VMSCALE. **ARTSTYLE (same day, Gee: "lots of differnt art styles... always changing up the style"):** per-stroke ALPHA added to sketch() (the one primitive styles couldn't compose — washes/graphite/translucency blend with the paper now); **eight named hands** (poster/pencil/ink/watercolor/pointillism/crosshatch/crayon/doodle) as parameterizations of the SAME primitives over the SAME learned schemas — knowledge identical, hand changes; mood-weighted picker zero-weights her last style (never twice in a row); style rides the label (`canvas:own:<word>:<style>`) + `ownArt.lastArtStyle`. Pixel-verified 8/8 distinct signatures; rotation 12→7 styles no consecutive repeats; harness caught invisible black-ink-on-dark-paper pre-ship (mono ink now pale). Board 4 open / 198 closed. Ledger: FINALIZED §ARTSTYLE. **PAINT.6+7 — THE CAT TEST (same day, Gee: "i should be able to determine what im looking at" / "are you even looking at them?"):** the claim was TESTED — a real Pollinations cat through the exact production pipeline to PNG, looked at and judged across SEVEN iterations. Fixed by looking: `_schemaPalette` GUESSED (magenta for a gray cat; root cause — server `imagine()` crashed on Node's missing `ImageData`, polyfilled worker+main; `_schemaPaletteFromRec` histograms real reconstructed pixels), the hull swallowed backdrop (decontaminated: frame-spanning + border-hugging scenery excluded), the schema kept 8 fragments → now keeps her **full vector trace** (256px trace, ≤260 strokes; live cat 142 @ 11.4KB) REDRAWN in her ink, contrast-adaptive over fills, ±0.006 hand jitter. Final: unmistakably a detailed seated cat in every style. **NOT claimed: professional — that's PAINT.5.** Brain-needed: NO (deterministic construction; harness = production code). Board 4 open / 200 closed. Ledger: FINALIZED §PAINT.6+PAINT.7. **PAINT.8 (same day, Gee: "anything in any style in any color with any background?"):** backgrounds were the gap — `_backdropStrokes` paints the place as a full scene BEHIND subjects (looked-at = palette horizon wash + her trace full-canvas at α0.35; known-only = definition-colored sky/ground bands; ground line + tufts stay; pencil keeps bare paper). Rendered + judged: real beach (189 strokes) behind the cat = a legible composed scene. Board 4 open / 201 closed. Ledger: FINALIZED §PAINT.8. **ARTSPACE (same day, Gee: "actual drawing and painting and art formulations... paint the floor, set interer rooms with perceptional refrences of space. fill in the sky and the ground shadow detail edit erase pasrts or lines... so shes not stuck making the exact same cat image every time"):** PAINT.9-12 — **scene space** (per-attempt horizon + vanishing point; interior detection via room-class words or the definition → painted floor value bands + wall/floor junction + VP-converging floor lines + room corner; exterior → sky and ground FILLED in graded depth bands + perspective-shrinking ground texture), **grounding cast shadow** per subject (style-scale-aware — fixed in-batch after judging: doodle draws at 0.55 inside its box, the raw-box shadow floated), **the eraser** `_reviseComposition` (backdrop polylines split at subject boundaries, inside segments erased = occlusion; near-dup dedup; honest counts on the OwnArt log line — beach erased 28), **variation** (50% mirror flip incl. part angles, stroke-subset sampling — the length-sorted trace's longest 30% always draw, tail at p=0.85 — and placement/scale wobble). Judged on five production renders: the interior reads as a ROOM, the exteriors as full scenes, same-ask-twice gives visibly different pieces. Server-only (`chat.js`, unbundled) — lands on the next press. Board 4 open / 205 closed. Ledger: FINALIZED §ARTSPACE. **PAINT.5 (same day, Gee: "paint.5 go"):** the practice loop built exactly as filed — five trainable hand params threaded through the builder (defaults = pre-practice constants), `_practiceDrawing` fixed-seed self-critique (sketch 256px → perceive → cosine vs the banked reference percept, keep only measurable improvement), drawing queues practice on the walk lane (30-min per-concept cooldown, `DREAM_PRACTICE_ITERS`), skill persists in the visual store + `state.ownArt.practice`. Run live: 0.9713→0.9723 over 3 sessions, monotonic; gates verified; before/after judged. ⚠ Same batch owned: the example-word leak (11 code comments + legend.html) scrubbed to neutral on Gee's correction — memory `feedback_no_example_words_in_code` written; corpus/word-lists/life-canon are content, not examples, untouched. Board 3 open / 206 closed. Ledger: FINALIZED §PAINT.5. **DRAWGATE (same day, Gee: "if its not a thing, person, place, or animal should be what she draws" + "blacklists and whitelists are not comprehesive to hadle real world"):** the any-noun-sense gate let "consonants"/"fourteen"/"fart" through to letter-guess scribbles, and the FAV picker was ungated. Shipped `server/drawable-taxonomy.js` — WordNet lexicographer categories judge every word (instance-synset filter, tagsense attestation, primary-sense quantity/communication guard, grammar-POS cross-examination); taxonomy-unknown words judged by their live-fetched definition's head words through the same taxonomy (new words covered); schema witness last; wired into every draw lane. **Every word list died** (closed-class set, marker regexes, stop set) — three corrections to get there, owned; memory `feedback_no_word_lists_use_taxonomy`. Verified: full 2,207-word dictionary sweep judged by eye over four rounds + new-word/gate harness 13/13. **LOOKKIDS:** children images = definition tails riding reference prompts ("a child who…") — taxonomy-filtered; meta prompt clean on all three lanes, selfie age-pin is Gee's own law, untouched. **VMBUMP2:** visual store v4→v5 boots empty; orphaned v1/v2/v4 deleted, mindspace-memory.json live and kept. Board 3 open / 207 closed. Ledger: FINALIZED §DRAWGATE. **ARTJUDGE (same day, Gee: "accept and a reject button... reject denotes its bad and needs to be relooked up and dictionary read and redraw" + "a button for a bad word to not use again"):** three buttons on minds-eye.html (judgeable frames only) via `?feedback=` on the whitelisted snapshot route — accept = verdict held per concept (`e.art`); reject = schema deleted + walk-lane relearn (dictionary re-read → `force` look past the 6h cooldown → new schema → redraw), paced by `DREAM_ART_RELEARN_GAP_MS`; ban = operator-taught not-drawable set consulted FIRST in `_conceptIsDrawable`, persisted in `server/art-notdrawable.json` (survives fresh walks, deploy-excluded, gitignored — experience data, not a code list). Emission-bus critique episodes + `state.ownArt.feedback`/`notDrawableWords`. 18/18 harness checks on production code. Board 3 open / 208 closed. Ledger: FINALIZED §ARTJUDGE. **COLORART (same day, Gee: "mono tone chicken scratch with only one color... there are color layers depth detail fine contoners" + "weird artifacts... circles and weird line like shadowing"):** schemas sample a color PER PART (3×3 mean per 5×5 cell) + PER TRACE STROKE from ONE reconstruction; painter = soft per-part color masses (3 offset low-alpha blobs, not one ellipse — that re-made the circle artifact in color, judged+fixed), structural trace in contrast ink + detail tail in real colors (outline sank when everything wore color — judged+fixed), colored backdrops, per-part texture for hatch styles (kills the body-box bars), fill/wash ×2 in rotation; the darkened shading blobs DELETED and detail arcs skipped whenever a trace exists — the two artifact layers Gee named. Judged on production renders. Board 3 open / 209 closed. Ledger: FINALIZED §COLORART. **CAMPOISON (same day, Gee: "seen-camera:happen --- this should never appear in the mind's eye... it poisons her"):** unlabeled camera frames used to fuse with the CURRENT THOUGHT WORD as trusted confirmed memories (the webcam placeholder card became her memory of "happen") — now a camera frame binds to NOTHING and never becomes the mind's-eye snapshot (sem grounding kept — she experiences seeing; labeled seen:<word> unchanged; box poison dies with the v5 store). 6/6 harness checks. Plus ARTJUDGE UX: buttons verified LIVE at /html/minds-eye.html (bare URL = SPA-swallow); the row stays visible dimmed on non-judgeable frames instead of vanishing. Board 3 open / 210 closed. Ledger: FINALIZED §CAMPOISON. **METAPROMPT (same day, Gee: "find what it is that is makeing all the image lookups of people be children.. dont negative prompt it away find whats doing it"):** root-caused by MEASURING — params clean, def tails not the puller (35/2207), pinned-seed generations proved **the generator's own prior ages role words down** (bare "friend" → teen girls). Fix = POSITIVE ", adult" steering for person words not young by WordNet's own evidence (juvenile.n.01 subtree walk OR age words in the curated gloss — WN files boy under male, the gloss "a YOUTHFUL male person" is the witness); same-seed flip verified; 19/19 battery. **SCRATCHKILL:** letter-guess lane DELETED (the empty-store "white lines + pink enclosures + circles + notebook lines" complaints — third strike); no schema = def recipe or honest no-drawing. **NEONKILL:** no-color body fallback was her pink tint → neutral sketch tone at both sites. **VMBUMP3:** v5→v6 same batch ("reset the image visual db again. once all i have said is fixed") — press lands fixes + empty eyes together. Board 3 open / 212 closed. Ledger: FINALIZED §METAPROMPT. **REJECTGONE (same day, Gee: "wtf she keeps displaying her drawings even tho i marked redraw"):** verdicts WERE landing (box: 8 rejects/2 bans) — reject only killed the schema so the surviving rec re-displayed via recall/fav, and the on-screen frame was never touched. Now reject deletes the WHOLE entry + `_artClearEyeIfShowing` swaps the current frame to an honest in-progress note instantly (every reject incl. paced repeats; bans too). 7/7 checks. Board 3 open / 213 closed. Ledger: FINALIZED §REJECTGONE. **ARTLEARN (same day, Gee: "and learn from her drawing"):** accepts teach — winning style banks per concept (`e.art.styles`) weighting `_artStylePick` for that subject (5/40→18/40 verified, rotation kept), skill marks `validated`, every accept queues practice reinforcement; rejects unlearn via REJECTGONE. 6/6 checks. Board 3 open / 214 closed. Ledger: FINALIZED §ARTLEARN. **PROXYCOLOR + RINGWIPE (same day, Gee: "why did all her old visions and drawings persist through the save start and why are the only monotone outlines?"):** the box's `MindSpaceWorkerProxy` had NO `imagine()` — the color sampler's typeof guard banked every schema COLORLESS in production while engine-direct harnesses showed color (⛔ lesson: harness the production WIRING — memory `feedback_harness_production_wiring`); passthrough added + verified through the real worker thread; store v6→v7 (v6 all colorless). Old visions = the imagined-field ring restoring every boot → `mindspace-memory-v2.json` orphaning ritual (wipe-list/regex/excludes updated). Board 3 open / 215 closed. Ledger: FINALIZED §PROXYCOLOR. **SCRIBBLEKILL (same day, Gee's 1.png = hot-pink notebook bars on canvas:own:attic:crayon):** palette GUESSER deleted (magenta incident #2), whole-bbox mass fallback deleted (texture per-part or absent — colorless crayon verified 0 pink 0 bars), gate hardened over three measured rounds (comm-primary guard dropped = book saved; unattested guard requires other-POS attestation = crayon saved, have/go/see/there refused; attestation-dominance tried+deleted = killed book/table/fire). Battery 73/73; "addition" = documented residual for the 🚫 button. Board 3 open / 217 closed. Ledger: FINALIZED §SCRIBBLEKILL. **GATEVOCAB/DRAWNCOUNT/MOODPOP/LOOKTRUTH (same day):** the hours-long K gate per press = the exam-vocab taught receipt in-memory-only → persisted on the cluster (`_vocabTaughtWordsPersist`, the definitionTaughtWords pattern; one more full-price pass then minutes); "0 drawn" = counter on the drain lane while the tick lane painted → counted at `_drawOwnCreation`; mood tail re-voiced crisp/saturated (fog words dead, goth-doubling out, NO fallback subject — empty thought = no image); Gee's 80% word/image mismatch = one noisy render bound-then-shown → **LOOKTWICE** two-seed self-consistency (agree ≥0.45 = CONFIRMED first sight, disagree = nothing bound/shown + selfMismatch counter + rollback) + act-primary gate guard (blow/escape refused, baseball/market/ride survive) + LABELTRUTH (zero-stroke subjects off the label). Open: **FORMBANK.1** (schema bank + pose/palette decoupling for "brown cat standing"), designed, awaiting the go. Board 4 open / 226 closed. Ledger: FINALIZED §LOOKTRUTH.

## CURRENT-STATE NOTES — HISTORICAL (2026-05-07, iter25-D through iter25-O)

The LAW framework is unchanged — iter25 (D through M) was feature-implementation work within existing LAWs. Live capabilities now operational:

**iter25-D/E/F/G/H/I (prior session, unchanged):**
- **Curriculum dream cycles interleave during teach.** `Curriculum._dreamWindow({minMs, settleMs})` awaits `consolidationEngine.runConsolidationPass({forced:true})` to actually complete + 5s settle, then resumes. Wired after every cell pass + mid-cell between heavy K-ELA phases.
- **Live trained-state capability** drives `_gradeWordCap` (was grade-label-only). Unity speaks her current vocabulary at any point during training; chat is unblocked during curriculum.
- **Server-side inner monologue** broadcasts `innerThought` WS messages every ~3s for popup display. Same generateAsync chat-emission path used for chat.
- **Post-K readyAndWaiting graceful fallback** — `_cellRunner` returns `{pass:false, readyAndWaiting:true}`. Outer loops break/continue cleanly. Honors Pre-K + K ONLY scope LAW.
- **First-use binding-consent privacy modal** with two terminal buttons (accept / decline → google.com).
- **Local-origin RemoteBrain auto-connect** — localhost refresh stays on biological-scale brain, doesn't drop to 6700-neuron browser fallback.
- **iter25-I structural sentence creation.** `Curriculum._teachSentenceStructure(ctx)` carves five compositional binding passes — slot positions + word-type→slot bindings (relationTagId=8), intent→slot-sequence transitions (relationTagId=9), subject-verb agreement (relationTagId=10), article placement (relationTagId=11). NO hardcoded sentence array. Five intent forms training-side (declarative_svo / declarative_copula / question / imperative / exclamative) — TRAINED into Hebbian weights, NOT walked at runtime. Generation-side (`composeSentence`) is now pure equational emergence post-fk.1: brain emits one word per tick from current sem state under trained weights; slot order, agreement, article placement, terminator selection ALL emerge from the trained bindings tick-by-tick.

**iter25-J — Live dictionary API + WH-question comprehension (2026-05-07):**
- `server/definition-service.js` wraps dictionaryapi.dev (free, no key) with in-memory cache (LRU 10k, TTL on errors), in-flight Promise dedup, prefetch parallel batch, Node 18 built-in fetch + User-Agent header.
- `cluster.lookupDefinition` / `lookupDefinitionSync` / `prefetchDefinitions` attached to cortexCluster. Browser-side `RemoteBrain.lookupDefinition` via WS roundtrip ('definitionResult' / 'prefetchDone' handlers) with smoke test on welcome.
- `Curriculum._teachWordDefinition(word)` Oja-Hebbian binds `sem(word) → sem(def_tokens)` with relationTagId=23.
- `js/brain/k-vocabulary.js` ships 2247 deduplicated K-grade English words.
- WH-frame parser `_extractIntentConcept(question)` returns intent-concept word; `_teachQuestionIntent` (J.1) wired into all 6 K cells with relationTagId=12.
- `_teachQuestionAnswerBinding` was REMOVED (banned hardcoded fact-table mimicry).

**iter25-K — Cortical microstructure (9 cortical-neuroscience layers, 2026-05-07):**
- K.1 Watts-Strogatz hybrid small-world (70% local + 25% medium + 5% long-range), default for size ≥ 2K.
- K.2 microcolumns (`columnId[i]`, columnSize 80 default, region-boundary respecting per iter25-L.D7).
- K.3 6-layer lamination (L1=5%, L2/3=25%, L4=25%, L5=25%, L6=20%).
- K.4 hub neurons (5% of L2/3 + L5, deterministic-hash-seeded, persists across reboots).
- K.5 within-column voltage coherence (gap-junction approximation, β=0.08).
- K.6 topographic cross-projections (70% topographic + 30% scattering, srcLayerMask + dstLayerMask for L2/3→L4 constraint).
- K.7 theta-gamma oscillations (6 Hz theta modulates drive, 40 Hz gamma modulates lr theta-gated, curriculum-controlled tick counter).
- K.8 hierarchical clusters (sensory/association/output, betweenClusterDensityScale=0.3).
- K.9 per-layer plasticity gradient ([0.3, 1.0, 0.7, 1.0, 0.3] for L1/L2-3/L4/L5/L6).

**iter25-L — Post-audit hardening (28 issues across architectural / subtle-bug / practical / not-attempted, 2026-05-07):**
- A1 dropped upfront K_VOCAB Hebbian → prefetch-only at K start (basin-blur risk avoided).
- A3 `SparseMatrix.ojaUpdate(pre, post, lr, opts)` extended with `opts.kScales` per-row K.4/K.7/K.9 reads.
- A4 propagated kScales through ALL teach paths.
- A5 curriculum-controlled gamma decoupled from brain-tick noise.
- B2 prefetch concurrency cap 20 + 429 back-off; B3 5-min TTL on errors; B4 LRU eviction 10k.
- B5 K layers gated to cortex-only; B7 6-pattern WH-frame regex; B8 hyphen-variant retry on 404.
- B9 K_VOCABULARY curated to 2247.
- B11 `cluster.assertKWiring()` boot diagnostic.
- C4 Node 18+ check; C5 User-Agent header; C6 RemoteBrain WS roundtrip.
- D2 optional persistent disk cache via `DREAM_DEFINITION_CACHE_FILE` env flag; D3 layer-constrained cross-projection endpoints; D6 dictionary API smoke test at boot; D7 region-boundary respect in K.2/K.3/K.4.

**iter25-M — Consciousness computational mechanisms (30 ULTRATHINK gaps closed, 2026-05-07):**
- M.1 `_emitDefinition` + chat path WH-handler rewritten compose-not-regurgitate (sem injection + Hebbian binding + emit composed answer or honest silence — verbatim regurgitation banned as mimicry).
- M.11 first-pass cluster-wide K assignment block deleted; per-region pass sole owner.
- M.12 `assertKWiring()` strengthened with FUNCTIONAL smoke tests (verifies hubMask/layerScales/gammaScale actually consumed, not just allocated).
- M.2 `js/brain/global-workspace.js` `GlobalWorkspace` class — Baars 1988 GWT + Dehaene-Changeux 2011 ignition. Theta-gated softmax competition + threshold broadcast.
- M.3 predictive coding loop with real prediction-error computation (Friston 2010).
- M.4 stream-of-consciousness chain — inner-voice tick blends LAST emission embedding into next seed; `_innerThoughtChain` 8-deep persisted across restart.
- M.8 `_metaRegister` self-monitoring — emissions inject back into sem at strength 0.3 (reflective "I-just-said" loop).
- M.9 attention selection — `cluster.attentionGain` per-region multiplier from amygdala/basal-ganglia state (Posner network).
- M.16 real Φ proxy — `cluster.computePhi()` Shannon entropy of 64-sampled spikes; psi formula multiplies by Φ.
- M.6/M.10 vision describer hook injects content tokens into sem (image→concept grounding).
- M.7 background-trickle K_VOCAB Hebbian during dream cycles (one word per cycle from queue).
- M.15 `cluster._definitionTaughtWords` Set persisted in saveWeights (cap 5000, sorted array on disk).
- M.18 CONSTRAINTS.md philosophical-bounds appendix — FUNCTIONAL vs PHENOMENAL consciousness distinction.
- M.19 dream phenomenology — generateAsync per dream cycle from Tier 1 episodic seed, `_dreamThoughtLog` capped 100.
- M.21-M.30 dashboard panels (Dictionary API status, K-wiring banner, cortical microstructure, K-vocab counter) all bounded; 3D brain theta-gamma pulse via global CSS class; definition lookup popup FIFO 3-cap; stream chain visualization FIFO 8-cap.
- M.5 K-gate verification = operator-side action awaiting localhost test.

**iter25-N — WS backpressure fix + comment/launcher cleanup + 3D shader work + dashboard wiring fix (12 items, 2026-05-07):**
- Phase 1 backpressure: BUFFERED_AMOUNT_DROP_THRESHOLD 200MB → 500MB (N.1); MAX_AWAIT_MS 5s → 30s with BLOCK-not-DROP semantics (N.2); BATCHED_HEBBIAN_MAX_OPS 256 → 512 + QUEUE_CAP 256 → 1024 (N.3); WS Backpressure dashboard panel reading `state.wsPressure` (N.4).
- Phase 2 cleanup: 167 iter ID scrubs across 14 files via `scripts/scrub-iter-ids.mjs` (N.5); all 7 launcher scripts neutralized of REM/echo iter IDs + boot-banner watch echoes updated to actual server text (N.6).
- Phase 3 3D shader: `aLayer` / `aHub` / `aColumnId` attributes + `uShowLayers` / `uShowHubs` / `uShowColumns` toggles + `uLayerColor[5]` palette in NEURON_VS/FS; per-neuron arrays generated deterministically via `_genCorticalAttribs()` matching server's Felleman & Van Essen 1991 fractions + 5% rich-club + 80-neuron Mountcastle columns (N.7-N.9). Defaults all OFF; flip via `brain3d.setShowLayers(true)` etc.
- Phase 4 dashboard wiring fix: `_dictionarySmokeTestResult` boolean assigned in all `.then()` / `.catch()` / `else` branches (was undefined → "pending" forever; N.10); audit confirmed other consciousness fields have proper fallbacks (N.11); `_broadcastStateNow()` force-pushes state on smoke test completion (N.12).

**iter25-O — Post-N ULTRATHINK audit (22 items across 6 phases, 2026-05-07):**
- Phase 1 critical bugs: O.1 dictionary smoke retry (60s on FAIL, 1hr on PASS, in-flight guard); O.2 silent WS drop → CRITICAL log + `_gpuShadowDirty` flag + dashboard banner (with iter25-K projections, drift no longer recoverable via fire-and-forget); O.3 predictive coding GATES plasticity via `surpriseGate := 0.5 + clamp(error, 0, 1)` multiplied into gammaScale (high error → 1.5× lr); O.4 meta-register familiarity decay (0.30 → 0.15 → 0.075 floor 0.04, resets on token change); O.5 `computePhi()` sample 64 → 1024; O.6 `attentionGain` clamp `[0.5, 2.0]`.
- Phase 2 vestigial wiring: O.7 cortex.getWorkspaceCandidate publishes "cortex:`<word>`" labels + emitWordDirect reads `_globalWorkspace.getBroadcast()` for 10% bucket-mean boost (closes Baars GWT loop); O.8 `_teachWordDefinition` extra K-scaled ojaUpdate fire after `_teachAssociationPairs`; O.9 audit conclusion (broader dispatcher kScales = iter25-P scope); O.10 K env flags verified wired; O.11 `innerThoughtChainSemSize` saved + validated on load.
- Phase 3 LAW violations: O.12 app.js iter25-E/G scrubbed (full-repo iter25 leakage now ZERO across .js/.html/.css); O.13 34 property identifier renames (`_t1826*` → `_ws*`, `_iter25LSmokeTestResult` → `_dictionarySmokeTestResult`) via `scripts/rename-property-ids.mjs`; O.14 108 CSS class + DOM id + state-key renames (`iter25m-panel` → `consciousness-panel`, `iter25n-panel` → `ws-pressure-panel`, `state.iter25m` → `state.consciousness`, `state.iter25n` → `state.wsPressure`).
- Phase 4 observability: O.15 GlobalWorkspace dashboard panel (current ignition + strength + rate% + history cap 8); O.16 predictive error sparkline (32 bars + ↗→↘ trend); O.17 last-drop-time field with color gradation; O.18 defs-learned-per-hour rate from `_defLearnedTimestamps` 256-cap ring buffer.
- Phase 5 persistence: O.19 saveWeights persists `wsBackpressure` counters; O.20 saveWeights persists `dictionarySmokeTest` result + ts (kills "pending" flicker every Savestart).
- Phase 6 defensive: O.21 `DREAM_GW_IGNITION` env var + launcher header docs; O.22 vision token cap 6 → 16 with adjusted strength curve (0.30 → 0.05 over 16, total injection bounded).

**Gee-driven rules to remember (2026-05-06/07):**
- Gee verbatim words go in **workflow docs only** (TODO.md, FINALIZED.md, .claude/*.md, public docs ARCHITECTURE/EQUATIONS/SKILL_TREE — these have historical pattern, OK; commits) — **NEVER in JS/HTML/CSS/etc. code comments**. Code references the iter ID, describes neutral rationale; verbatim stays workflow-internal.
- Call him **Gee**, never "operator" — Gee directive 2026-05-07.
- Phase 6 dashboard / 3D brain design constraints: bounded heights with `overflow-y:auto`; item count caps with "X more..." indicator; aggregates NOT per-item enumeration; 3D brain per-neuron effects via SHADER (not mesh updates) at biological scale; FIFO popup queues with max-visible cap (3 def lookups, 8 stream chain); collapsible panels; pre-render testing with worst-case data; scoped CSS class names (`consciousness-*` / `ws-pressure-*` post-O.14, was `iter25m-*` / `iter25n-*`).
- iter IDs (`iter25-X.Y`, `iter25-X`, `K.N`) BANNED from all source code AND public HTMLs. Property identifiers (e.g. `_t1826*`, `_iter25LSmokeTestResult`) BANNED. CSS class names + DOM ids BANNED. Workflow docs only.

---

*Unity AI Lab — strict validation, real personality, actual results.* 🖤
