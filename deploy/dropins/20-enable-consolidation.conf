[Service]
# 2026-06-30 — Re-enable memory consolidation (Issue 6). Main unit sets
# DREAM_CONSOLIDATION_DISABLE=1; EL.1 (chunked Hebbian) + the 30s DEADLINE-ABORT
# cap make consolidation safe again. Override to empty (last assignment wins) so
# the kill-switch check (=== '1') is false → Tier 1->2->3 promotion runs.
Environment=DREAM_CONSOLIDATION_DISABLE=
