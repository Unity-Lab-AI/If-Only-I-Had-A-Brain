---
# DOCPROV.3 — provenance. See docs/ARCHITECTURE.md for the full note.
# ⚠ `last-verified` is the commit that last TOUCHED THIS PAGE.
# ⚠ This page is a LEDGER of known issues, so a source moving does not
# necessarily invalidate an entry — it means "re-check whether this issue still
# reproduces". Treat drift here as a prompt, not a defect.
status: draft
sources:
  - server/brain-server.js
  - server/brain-server/gpu.js
  - deploy/self-update.sh
  - js/brain/curriculum.js
  # ADDED 2026-09-06: KI-40 makes a line-precise claim about the donor page
  # (compute.html:324, the apostrophe that killed its whole module block), and
  # an undeclared source is a claim the drift checker can never re-test.
  - html/compute.html
  # ADDED 2026-08-31: KI-38 names the two gated Tier-1 writers and the
  # consolidation pass they starved. Neither file was declared, so a page
  # carrying two memory-system entries could not have reported drift on the
  # code that made the whole lane inert.
  - server/brain-server/memory.js
  - js/brain/consolidation-engine.js
  # ADDED 2026-08-29: KI-37 cites donor-release.yml:170 and KI-22's whole fix
  # lives in that workflow — the page cited it without declaring it, so drift
  # could never fire on the file two rows are about.
  - .forgejo/workflows/donor-release.yml
verified-scope: |
  CHECKED 2026-08-27 (DOCPROV.4, 10 of 22). A ledger's checkable claim is its
  STATUS, so three were decided on live evidence or code rather than re-read:
    - KI-33 CONFIRMED on live state: phiState reads "live" (phiRaw 0.2618,
      phiScaleRef 0.3044, phiNorm 0.8600). Every "watch after the press"
      instruction in that row is discharged.
    - KI-27 STATUS CORRECTED 🔴 OPEN -> ⛔ BY DESIGN. profiling.throughput
      .batchPaused reads expected:true with batchStall:null. It had been
      answered on 2026-08-25 in docs/TODO.md and never propagated here.
    - KI-16 VERIFICATION PASS DONE, which is what the row asked for: NOT dead
      code (13 live call sites), does NOT throw (so the catch never fires and
      nothing logs), and a frame IS sent with a garbage region name and ZERO
      indices - so spikes never reach the GPU while the CPU shadow is written.
      TWO sites, not one: curriculum.js:14049 and :15613. NOT fixed here.
    - curriculum.js ADDED as a source: KI-16 lives there and the page did not
      list it, so drift could never have fired on the file the row is about.
  NOT CHECKED — do not read this page as authority on:
    - KI-1 (~15-min boot: its own note says the breakdown is read-from-code
      inference, never measured - still true, still not measured here)
    - KI-2 / KI-4 / KI-5 / KI-9 / KI-15 / KI-19 / KI-20 / KI-21 statuses. Several
      say "stays open until a live walk confirms X"; ⛔ this brain is 5.60 h into
      a walk at cellPhasesStarted:2 and runs code PREDATING this session, so it
      cannot discharge them. Absence of confirmation here is not evidence.
    - KI-23 / KI-26 / KI-29 / KI-31 / KI-32 / KI-35 / KI-36 - not re-investigated.
    - the operational-hazards list and the KI-1 / KI-7 detail sections.
  RE-VERIFIED 2026-08-29 (provenance pass on cd465955). 3 of 4 sources changed
  since the 2026-08-27 stamp (brain-server.js +455, gpu.js +82, curriculum.js
  +158; self-update.sh unchanged). What that drift actually touched:
    - KI-16: line numbers re-pointed (was :14049/:15613/:3276/:4530/:4606-4610,
      now :14156/:15720/:3293/:4594/:4670-4674) — the defect is UNCHANGED, both
      sites still call writeSpikeSlice with ONE argument. Still OPEN.
    - KI-33: FIREMATH addendum prepended — the sub-1.3%-firing floor concern is
      moot; real firing measures ~9.6-15% on the fixed kernels (donor v0.3.35).
    - KI-27: PSITEACH.2 note appended — the designed pause is now cortex-only;
      a walk heartbeat steps the seven non-cortex clusters behind the gate.
    - KI-35: "no bump is pending" superseded — releases through v0.3.35 shipped
      and the pod self-upgraded via the 2026-08-25 welcome handshake.
    - KI-34: Where locator re-pointed brain-server.js:10467 -> :10983.
    - KI-36: Where cell corrected — the ring cap is 2000 (RING_CAP, CONSOLERING
      2026-08-18); 500 is the PER-REQUEST cap on ?console=N. Analysis untouched.
    - KI-22 / KI-37 checked against donor-release.yml (not previously a source):
      the KI-37 push line is still exactly line 170 and KI-22's verify-string
      "Published the frontend with ... download links." is present (line 221).
  NOT re-investigated this pass: everything the 2026-08-27 NOT-CHECKED list
  names, unchanged.
last-verified: "f06ea30e 2026-08-31"
---

# KNOWN ISSUES — running ledger

> Lightweight, scannable list of known bugs, limitations, and intentional
> deferrals across the Unity brain stack. ONE row per issue with a status marker;
> check items off here when they're fixed (don't delete — flip the status and date
> it, so the history stays). Deep dives live in the `ISSUE-*.md` files and the
> full audit in `Problems.md`; this file is the index that points at them.
>
> **Status key:** 🔴 OPEN · 🟡 IN PROGRESS · 🟢 FIXED · ⛔ BLOCKED (intentional / needs sign-off)
>
> Last updated: **2026-08-29** (provenance re-verify on `cd465955` — ⭐ **KI-33's one remaining design concern is MOOT**: FIREMATH found both native donor kernels running a Rulkov noise term **20,000×** its browser reference (firing collapsed to ~0.4-0.8%); fixed in donor v0.3.35, real firing measures **~9.6-15%** — far above the ~1.3% entropy-floor crossover the row deferred to Gee. ⭐ **KI-27's designed pause is now CORTEX-ONLY**: PSITEACH.2's walk heartbeat steps the seven non-cortex clusters every 15s behind the probe gate, so "zero batches for a whole boot" is no longer the expected read. ⚠ **KI-35's "no bump is pending" is superseded** — releases through v0.3.35 shipped and the pod self-upgraded via the 2026-08-25 welcome handshake, no recreate needed. **KI-16's cited lines re-pointed** after +158/+455/+82 lines landed above them (defect unchanged, still one-arg at both sites). KI-22 + KI-37 re-checked against `donor-release.yml`: the push line is still exactly `:170`, the `Published the frontend …` verify-string is present.)
>
> Prior: **2026-08-27** (DOCPROV.4 ledger pass — ⭐ **KI-33 CONFIRMED on live state**: `phiState` reads **`live`** with `phiRaw 0.2618` / `phiNorm 0.8600`, so Φ̂ is modulating Ψ for the first time and every "watch after the press" line in that row is discharged. ⛔ **KI-27 status CORRECTED 🔴 OPEN → ⛔ BY DESIGN** — `profiling.throughput.batchPaused` reads `expected: true` with `batchStall: null`; it was answered on 2026-08-25 and the answer never propagated to this page, **so this ledger sat two days lying by omission.** ⭐ **KI-16's verification pass DONE, and it is worse than filed**: 13 live call sites, it does NOT throw so the `catch` never fires and nothing logs, and a frame IS sent with a garbage region name and **zero** indices — the spikes never reach the GPU while the CPU shadow is written. **TWO sites, not one.** ⚠ Several rows say *"stays open until a live walk confirms X"* and **could not** be discharged: this brain is 5.60 h in at `cellPhasesStarted: 2` on code predating the session.)
>
> Prior: **2026-08-25** (SCALEWALK/ONESHOT — **KI-36** filed MITIGATED: the console ring is a nine-second window at walk speed, so one-shot diagnostics must be state fields; **KI-33** moved to FIXED, Φ̂'s pinning traced to a dead read and the design half decided. Prior: deploy verification — **KI-35** filed WATCH: the live pod runs the pre-fix launcher and `args` is immutable, so the fix costs the GPU; the running binary is confirmed `0.3.30` and no bump is pending. **KI-34** filed FIXED same day, and it corrects half of KI-24: the `96 native / 8 browser` split that row describes did not exist in the code until now. Prior: 2026-08-21 UPLINK — KI-24 filed FIXED same day; KI-23 advanced: op batching landed as range/masked verbs. 2026-08-20 PAGESTALE + TEACHMIRROR — KI-22 + KI-23 filed).

---

## Issues

| # | Status | Issue | Where | Notes / link |
|---|--------|-------|-------|--------------|
| KI-40 | 🟢 FIXED (2026-09-06) | ⛔⛔ **Browser GPU donation was dead for twelve days because of one unescaped apostrophe in a TOOLTIP.** `html/compute.html:324` builds a `data-tip` inside a **single-quoted** JS string containing `…not your card's speed…`; the apostrophe closes the string and the rest is garbage. **A syntax error anywhere in a `<script type="module">` block kills the whole block**, and this one spans **lines 105–1605 — 1,500 lines** holding **11 `WebSocket` references and 4 `gpu_register` calls**, i.e. the browser donor's entire connect-and-register path, plus the leaderboard UI. | `html/compute.html` | ⭐ **Invisible from every surface we watch, and here is why:** the two binary download links are static `<a href>` at lines **46 and 56, outside the block**, so downloads kept working — and the native donor is a standalone Rust binary that never loads this page, so the pod connected and computed normally throughout. Nothing on the dashboard, in the console ring, or in the donor telemetry could have shown it. ⭐⭐ **Introduced by a doc sweep and found by a doc sweep:** `git log -S` names `377d06a7` (2026-08-25) — *"feat(tooltips): shared data-tip component across all 11 pages"*. **A pass that added explanatory tooltips broke the page it was explaining.** ⚠ **The detection was nearly lost to its own false positives:** the first run reported 4 failures, 3 of which were `new Function()` being handed ESM — my extractor's defect. Re-run with `SourceTextModule` for module blocks: **4 → 1, and the 1 was real.** Fixed by escaping (`card\'s`); all **14 inline script blocks across 11 pages parse, 0 failures**. **Rides the frontend rsync — no press needed.** ⭐ **The lesson worth keeping: a doc sweep must check that the HTMLs still PARSE, not only that their prose is current.** |
| KI-39 | 🟠 OPEN — instrumented, needs a press to read | **The pre-phase definition bootstrap costs 12.4 s per definition, against the ~3.9 s the verdict that declared it "normal" was based on.** Read live 2026-09-06: `definitionQueue.depth` **2,123**, `lastWindow` **15 processed in 185,780 ms**, `cellPhasesStarted` **0** of **25** after **52 minutes** of cell time. At 12.4 s the remaining queue alone is **~7.3 hours before phase 1 begins**. | `js/brain/curriculum.js` | ⛔ **The earlier "this is normal" verdict is still right about the PHASE and wrong about the PRICE** — it closed on a per-definition cost that no longer holds, which is why the row is filed against the number rather than the behaviour. The profile names the cost centre and it is **not** the dictionary: `_teachWordDefinition` **1,281 ms/call**, of which `_teachAssociationPairs` is **~1,113 ms (87%)** across ~10.8 calls, while its two instrumented children (`_teachHebbian` 1.0 ms, `_teachLateralInhibition` 2.6 ms) account for about a quarter of that. ⚠ **The remainder was derived by hand twice and both derivations produced a wrong theory** — the first blamed the tiled writer for touching the whole 1.88M sem region, and reading it showed a `feat[d] <= 0` skip that makes it write ~50K. **A third guess was refused:** segment timers now publish `embedMs` / `wtaMs` / `clearMs` / `tileMs` / `pairMs` / `residualMs` / `residualPct` / `semActiveMean` at `stageProfile.pairSegments`, flushed at **all three** exits because the two early ones are the long calls. **Read `residualPct` after the press; if it stays large the work is inside the predictive-error pass or the anti-pair branch, and that is a reading rather than the next theory.** ⭐ Partly addressed already: the lateral scan removal (11.8% of a boot) and the rep-pricing throttle (which was sampling up to 512 dictionary patterns on every one of 25,603 assoc calls) both land in the same commit. |
| KI-38 | 🟢 FIXED | **The three-tier memory system was complete, wired, documented — and had never executed. `tier1.totalEpisodes` read `0` on every boot in this project's history.** Tier 1 has exactly three writers and all three were dead during a walk: `_recordPhaseEpisode` fires on phase COMPLETION (and phases were not completing — the teach loop's shutdown exit banked no rep cursor, so every press restarted the phase from zero), while the working-memory age-out and the 30-second thinking heartbeat were **both** gated on `!this._curriculumInProgress`. ⛔ **That flag is true for the ENTIRE multi-week walk, so "only when not mid-walk" means never.** No episodes ⟹ no promotion candidates ⟹ no Tier-2 schemas ⟹ **interleaved replay — the mechanism that separates representations — had never run once**, and the 100-rep waking dose was silently absorbing work replay was supposed to share. | `server/brain-server/memory.js` (the two gated writers) · `js/brain/curriculum.js` (`_recordPhaseEpisode`) · `js/brain/consolidation-engine.js` | ⛔ **The finding this row exists for is that it was NEVER FILED HERE.** A dead consolidation system produced no error, no warning and no red field — `memoryStats` reported a truthful `0` that read as "nothing has happened yet" for months, on a board that had no row asking "should this be zero by now?". **Absence of an issue is not evidence of health**, and this is the third instrument in this file to make the point (see KI-32 and KI-28, both built-and-unconsumed). ⚠ **The gate was DELIBERATE** — `e27caa90` added it against a real 8-27 s main-loop freeze — and it was removed only once an earlier fix had made that 2 s term structurally unreachable above a 2M cortex; the condition now tests whether the expensive term COULD run, not whether a walk is in progress. ⭐ Its second justification had gone **circular**: zero promotion candidates *because the writes were suspended.* **Verified live 18 minutes into the 2026-08-31 fresh walk:** `tier1` 4 · `freqMergedCount` 21 · `promotedToTier2` 4 · Tier 2 holding `hebbian-ela-kindergarten` and `association-pairs-ela` — write → consolidation → schema, end to end, for the first time. ⚠ **Judge this lane by `freqMergedCount`, never the raw episode count**: the exact-text merge folds repeated contexts by design, so single digits with the merge counter climbing is the CORRECT shape and hundreds would be the surprise. **The falsifier for its cost estimate is still being watched: `[EventLoop] BLOCKED` must not gain a new ~30 s-periodic entry.** |
| KI-37 | 🟡 WATCH | **Every donor release makes `main` diverge between the two remotes, silently.** `donor-release.yml:170` pushes its site-link bump with `git push "https://x-access-token:${TOKEN}@${host}/${REPO}.git" HEAD:main`, where `host` is derived from `SERVER_URL` — **the Forgejo host only.** The github.com mirror never receives it. ⚠ **The `github.*` references in that workflow are Forgejo's GitHub-COMPATIBLE variable names** (`github.server_url`, `secrets.GITHUB_TOKEN`), not the mirror — which is exactly why this reads as if both are covered. **Found live on 2026-08-27:** after `donor-v0.3.32`, `origin/main` was `da1c71c9` (the bump) while `github/main` had never seen it, and the next routine cascade push to `origin main` was **REJECTED** mid-batch. | `.forgejo/workflows/donor-release.yml:170` | ⭐ **Not dangerous, and it self-heals on collision — but it wastes a push and produces a confusing rejection in the middle of an unrelated cascade**, which is the moment you are least equipped to reason about it. ⛔ **Do NOT force-push when it happens.** The correct recovery is `git fetch`, confirm the extra commit is only the CI bump (`site(donor): bump download links -> …`), `git merge` it into local `main`, push to **both**, then back-merge into `develop` so the branches do not drift again — which is what was done on 2026-08-27 (`main b382e731`, `develop 4b506ebd`, all four refs converged). ⚠ **Filed as WATCH rather than a bug** because the workflow is doing exactly what it was written to do; the gap is that this repo has TWO push targets and the release job only ever knew about one. The fix is a second push (or a documented post-release reconcile step), and it should be batched with the next workflow edit rather than spending a CI run on it alone. |
| KI-36 | 🟠 MITIGATED | **The console ring is a NINE-SECOND window at walk speed, so anything that prints once is unreadable.** Measured 2026-08-25: a 500-line fetch spanned `15:55:03 → 15:55:12` because the walk logs **~55 lines/second**. ⭐ **The cause is a fix working:** SCALEWALK took definitions from ~5-7s each to ~128ms, so log volume rose ~40× and the readable window shrank by the same factor. **The log became unreadable BECAUSE the walk got fast.** This ate the `[CPUProfile]` table on the first attempt to verify SCALEWALK, and had already eaten the `UPLINK measured …` line on an earlier press. | `server/brain-server.js` console ring (ring cap 2000 — `RING_CAP`, CONSOLERING 2026-08-18; `/public-state.json?console=N` is capped at 500 lines per request, which is the window the measurement describes — cap corrected 2026-08-29) | ⛔ **The rule, now applied: a measurement that happens ONCE cannot live in a console line** — a line is something you had to be *watching for*, and this board exists to answer questions late. `ONESHOT.1` moved both known offenders into state (`profiling.cpuProfile`, `profiling.throughput.uplink`) **with dashboard rows in the same commit**. ⚠ **MITIGATED, not fixed** — the ring is still 9 seconds, so any *future* one-shot line has the same problem. **When adding a diagnostic that fires once, publish it as a field.** Raising the cap only buys a linear factor against a log rate that rises whenever the walk gets faster. ⭐ **UPDATED 2026-08-30 (`READBACKEYE.1`) — the 500-vs-2000 gap was worse than "a per-request cap", it was an ADDRESSING gap.** `since` filters forward and the handler then took `.slice(-n)`, so **every possible call returned some window of the newest 500** and 1,500 retained lines could not be reached at all through the one route the public origin forwards. This bit for real the same day: with parity reading `DRIFTING`, the hourly readback line was still in memory and unreachable, so *"did the readback fire?"* had to be answered *"I cannot tell."* **`&before=<ms>` now pages backward** (`ringSize` / `oldestTs` / `more` returned for the cursor; 4 pages reach all 2,000, no duplicates, terminates). ⛔ **This does not close KI-36** — the window is still ~36 s of walk logging rather than ~9 s, and the rule stands unchanged: **a measurement that happens ONCE must be a field, not a line.** `state.readback` shipped in the same commit for exactly that reason. |
| KI-35 | 🟡 WATCH | **The live pod is running the PRE-FIX launcher, and it cannot be updated without recreating the pod.** Verified 2026-08-25 by reading the pod's own `args` and its container log: the embedded command is the copy `deploy/runpod-donor-launcher.sh` was written to REPLACE — it still carries the stale `PIN=…/donor-v0.3.26/…` fallback (⛔ an unreachable release API therefore installs a **three-version-old binary and keeps it** — a fallback that can move you *backwards*) and it has **no upgrade watchdog**, so it only re-resolves `releases/latest` when the donor process EXITS. ⭐ **Nothing is wrong right now and that is a measurement, not an assumption:** the log shows the API answered, `DONOR_URL=…/donor-v0.3.30/…`, and the process self-reports `unity-donor 0.3.30` — the PIN never fired. | RunPod pod `cl5i7k9gkge3hx` · repo copy `deploy/runpod-donor-launcher.sh` | ⛔ **RunPod's `args` is NOT mutable via the API** (only name / image / disk / ports / env are), so the ONLY fix is recreate — which **loses the A40**, against the standing instruction to keep the GPU. Deliberately NOT acted on. **Exposure is bounded and stateable:** it matters only if the release API is unreachable at a restart, or if a NEW donor release is published while the pod runs. Neither applies today — `donor-app/src` is byte-identical to the `donor-v0.3.30` tag, so no bump is pending. ⚠ **2026-08-29 update: that last clause is superseded** — donor releases `v0.3.31`→`v0.3.35` have shipped since (`CURRENT_DONOR_VERSION '0.3.35'`, `server/brain-server.js:1127`), i.e. the "new release while the pod runs" exposure this row names HAS since occurred — and it resolved WITHOUT a recreate: the 2026-08-25 donor-upgrade handshake rides `recommendedDonorVersion` on every `welcome` (`brain-server.js:10485`) and the pod self-upgraded to v0.3.35 on its restart-disconnect (verified live, EQUATIONS 2026-08-29 sweep stamp). The stale-PIN exposure (release API unreachable at a restart) still stands and still needs the recreate. ⭐ **The repo file is the paste-in source for the next recreate**, which is exactly why it is version-controlled while not being run from the repo. |
| KI-34 | 🟢 FIXED | **A truthy sentinel made two BROWSER-donor branches dead code.** `gpu_register` stamps `client.donorAppVersion = _donorVer \|\| 'browser'` — the **string** `'browser'`, which is truthy — so `if (client.donorAppVersion)`, used at two sites to mean *"is this the native binary"*, was true for **every** donor. (1) The bound-propagate router served browser donors the **native** protocol: index payloads where `compute.html` reads a dense 0/1 array, and a non-empty `preLen` that also defeats the browser's own bound-mode trigger (`preLen === 0`) on matrices whose standalone buffers are not allocated when cluster-bound — a mirror image of the very bug the 2026-08-21 PROPBOUND fix was written to solve. (2) The upload pump handed browser donors the **96MB** in-flight window meant for native donors, removing exactly the 8MB protection KI-24's own note promises them, on the one donor type that **cannot service its own socket**. | `server/brain-server/gpu.js` · `server/brain-server.js:10983` *(sentinel stamp; was `:10467`, re-pointed 2026-08-29)* · `html/compute.html` | ⭐ Fixed at the chokepoint: one owner (`_donorIsNative()`, testing the **sentinel** not the field's presence) and an **advertised** `boundResidentRead` capability for the protocol choice. ⚠ Every *other* capability gate was unaffected for a reason worth keeping — they all regex-parse a semver and `'browser'` fails `^\d+\.\d+\.\d+`, so **version-gated checks got browser-exclusion free while the two boolean-presence checks did not.** ⚠ The unknown-donor case deliberately routes to the path that refuses with `null`, never the one that invents all-zero currents. FINALIZED §BOUNDCAP + DARKHEB. |
| KI-33 | 🟢 **FIXED — and CONFIRMED ON LIVE STATE 2026-08-27** | ⭐ **2026-08-29 FIREMATH ADDENDUM — the one design question this row left for Gee is MOOT.** The "below ~1.3% firing the entropy still falls under the floor" concern assumed the broken-noise era's sparsity: both native donor kernels (`lif.wgsl`, `cuda_kernels.cu`) ran the Rulkov noise term at **20,000×** its browser reference, collapsing firing to ~0.4-0.8% of accidental crossings. Fixed in donor v0.3.35, the map's MEASURED intrinsic floor is **~9.6%** firing (σ=−1; nominal tonic reads 19-24%), where `H(0.096)≈0.46` clears the seed floor by 4× — verified live on BOTH brains (box 0.4% → 19.8% converging on the ~9.6% floor with FIREKNOB pinned LOW by design, `state.firing` publishing the pin, drive scale bounded ×[0.01, 2.5]; EQUATIONS 2026-08-29 sweep stamp). Quiet eras can no longer reach the entropy regime this row worried about. **Original 2026-08-27 confirmation below.** ⭐ **THE POST-PRESS CHECK THIS ROW ASKS FOR IS NOW SATISFIED, read rather than assumed:** `phiState` = **`"live"`** (not `floored`), `phiRaw` = **0.2618**, `phiScaleRef` = **0.3044**, `phiNorm` = **0.8600**. ⭐ **So `Φ̂` is modulating Ψ for the first time** — `phiRaw` sits well clear of the old `max(0.1, ·)` floor, and the adaptive high-water reference is doing its job rather than clipping to a constant. **Every "watch after the next press" instruction in the entry below has been discharged.** ⚠ Read on the LOCAL brain (build `2673d14c`, 5.60 h uptime); the box was not re-read this pass. **Original entry:** ⭐ **RESOLVED 2026-08-25 (PHISCALE.1).** Asked and answered: *rescale the entropy to her operating range.* `Φ̂ = clamp(H(p)/ref, 0, 1)` with `ref` an **adaptive high-water reference** — rises at once to any new peak, decays 0.99995/tick, floored at the seed `H(0.015) = 0.1124` (her **documented** design sparsity, not a chosen constant). ⛔ A hardcoded `p_ref` was rejected on two grounds: nothing in the system justifies a specific proportion, and it would silently mean different things across boots because `totalNeurons` is derived from free host RAM. ⚠ **My first cut was wrong and the harness caught it** — a gentle rise let the reference lag, clipping design-sparsity AND 3% firing both to `1.000`, i.e. re-creating the constant. Verified on the fix: 0.5%→3% firing spans **0.234 → 1.000**, spread **0.766**. `phiRaw` / `phiScaleRef` / `phiNorm` all publish. **Post-press check unchanged: `phiState` should read `live`.** **Prior status below.** 🟠 **HALF FIXED — the bug is gone, the design question remains** | ⭐ **UPDATE 2026-08-25 (PHISRC.1): the pinning had a CAUSE, and it was a dead read, not sparse coding.** `computePhi()` sampled `cluster.lastSpikes` — the CPU spike shadow, which is EMPTY at biological scale because the GPU owns cortex spike state. Φ̂ was measuring teach-pattern residue, which is why `phiRaw` read ~one sampled neuron in 1024. It now derives the proportion from the **exact** GPU-acked `cluster.spikeCount`, and at the documented ~1.5% design sparsity `H(0.015) = 0.1124` **clears the 0.1 floor — Φ̂ should modulate Ψ for the first time.** ⚠ **What is NOT fixed and is Gee's call:** below ~1.3% firing the binary entropy of a sparse proportion still falls under the floor, so the floor may still do the work in quiet eras. The three options — rescale the entropy for sparse coding, lower the floor, or accept a capacity-only Ψ — are a **design decision**, and it is now decidable on a real measurement. **Watch after the next press: `phiState` should read `live`, not `floored`.** **Original CONFIRMED entry below.** ⛔ **MEASURED ON THE LIVE POST-PRESS WALK 2026-08-25 — it is not "may be", it IS pinned.** Two reads minutes apart on the running box: `phiState: "floored"` with `phiRaw: 0.028873878370704306`, then `phiRaw: 0.011173818721219527`. Both sit far **below** the `max(0.1, ·)` floor Ψ applies, and the raw value is *falling* as the definition bootstrap drives firing sparser. **So `Φ̂` has contributed a constant 0.1 to Ψ since the press and has never once modulated it** — exactly the failure the `phiRaw`/`phiState` pair was added to make visible, now doing its job. ⚠ The instrument is working; the quantity is not. Deciding what to do about it (rescale `computePhi`'s entropy for sparse coding, or lower the floor, or accept a capacity-only Ψ) is a **design call, not a bug fix**, and it needs Gee — which is why this stays open rather than being quietly "fixed" by moving a constant. **Original entry:** `Φ̂` may be PINNED at its floor, and if so it has never modulated Ψ.** `computePhi()` is the binary Shannon entropy of the spiking *proportion* `p` — it peaks at `p = 0.5` and collapses as firing gets sparse. `H(0.01) = 0.081`, which sits **below** the `max(0.1, ·)` floor Ψ applies. On a sparsely-firing cortex the term is therefore a **constant**, reading a healthy-looking `0.100` forever while contributing nothing. | `server/brain-server.js` Ψ block, `js/brain/cluster.js:computePhi` | ⛔ **Filed as a WATCH, not a bug, because it has not been measured on the live box** — sparse coding at biological scale makes it plausible, not proven. Deliberately made decidable rather than guessed: `state.phiRaw` and `state.phiState` (`live` / `floored` / `error` / `unmeasured`) ship in the broadcast and render on the dashboard beside the floored value. **First field to read after the press.** If it reads `floored`, the fix is a normalisation appropriate to sparse firing — NOT lowering the floor, which would let a genuinely-zero Φ zero out Ψ entirely. |
| KI-32 | 🔴 OPEN | **`recallByChemistry()` is built, exported, and has no consumer.** State-dependent recall — episodes retrieved by similarity to the chemistry running *now* — works and is called by nothing. | `server/brain-server/memory.js` | ⚠ **A deliberate seam, named so it does not become the next unconsumed instrument** (the exact class of defect `DORMANT.1–.6` and KI-28 were both about). Nothing in the chat or introspection path asks for state-dependent recall today, and wiring it into a lane *before* deciding which lane should own it is how a feature ships in the wrong place. The work is to choose the consumer deliberately. |
| KI-31 | 🟡 WATCH | **"No new sparse matrices" was verified SERVER-SIDE ONLY.** Adding the `brainstem` cluster allocates `{size, spikes, firingRate, spikeCount}` and nothing else on the server, so the RE-PRICE claims zero added matrices — but whether the **donor** allocates per-cluster buffers on its side was never checked. | `server/brain-server.js` cluster construction, donor `gpu_init` path | ⚠ Recorded as an explicit limit on a claim rather than left as an unstated assumption. **Read the donor upload log on the first press:** if the matrix count is higher than before, the RE-PRICE is wrong and needs redoing. This is the same discipline as KI-23 — a number that was never measured does not get to be quoted as if it were. |
| KI-30 | ⛔ BY DESIGN | **The endocrine `dt` is clamped to 10s per tick, so the slow EMAs UNDER-integrate across a gap.** A ten-minute pause advances chronic and allostatic load by ten *seconds*, not ten minutes. | `js/brain/endocrine.js:tick` | The clamp exists to stop a wall-clock jump (resume from save, host sleep) integrating a giant `dt` in one step. In production the tick is **1 Hz** and it never binds, so the time constants are honest. Across a real gap the error is **conservative in the safe direction** — load decays *slower* than it should, so a hard stretch is never erased by a restart. Raising the clamp would re-open the jump bug it exists for, which is a worse trade. ⚠ Documented because it bit two harnesses that stepped in minutes and measured their own bug. |
| KI-24 | 🟢 FIXED | **The "~4MB/s box uplink" was never the link — it was the pump.** The chunked-upload loop kept ≤~14MB in flight (8MB low-water + one ~6MB chunk) on an event loop the gates pinned in 3-4s synchronous-propagate slabs, so the wire drained in the first fraction of each slab and idled the rest: ~14MB per ~3.5s cycle ≈ the measured "4MB/s" on a port the operator rates *way* higher. Every doc comment that treated 4MB/s as the physical link inherited the pump's shadow. | `server/brain-server/gpu.js` chunk loop (`DREAM_UPLOAD_PACE_LOWATER_MB`), gate/probe lanes | **2026-08-21 (UPLINK.1 + GATECHUNK):** native donors (dedicated drain thread) get a 96MB in-flight low-water — browser donors keep the 8MB protection their busy tab needs — ⛔ **except they did NOT: see KI-34.** The native test was `if (client.donorAppVersion)`, which is truthy for a browser donor too (`gpu_register` stamps the string `'browser'`), so **every** donor got the 96MB window and the one donor type that cannot service its own socket lost the protection this note promises it. Corrected 2026-08-25; the split described here is real from that date. Every synchronous gate/probe propagate across ALL grades converted to the bit-identical `propagateChunked` so the slabs shrink; and every upload logs `UPLINK measured X MB in Ys = Z MB/s` on dispatch-complete, so the real rate is a permanent console read. Verify on the next donor move-in: the measured line should sit near the port rating, not near 4. |
| KI-23 | 🔴 OPEN | **A remote donor pod cannot be saturated — the ceiling is latency and disk, not the GPU.** An RTX A6000 holding all 17 matrices ran at **9% utilisation / 21% VRAM** and reads as a wasted card. It is not: the brain only ASKS for ~10GB (5.4GB weights + LIF state), and the walk is thousands of *small* sparse Hebbian ops, each a WS round-trip at a **measured 205ms RTT** (the pod is in EU-SE-1; note that RTT here is NOT cleanly attributable to distance — this doc's own busy-donor-forgiveness rule says a working donor answers the heartbeat late, so part of that number is the card being busy, and the split has never been measured), against a CPU-only Xeon master that spends **88s of wall per 5.4GB checkpoint every ~7min**. | `server/brain-server/gpu.js` teach dispatch, `[SavePin]` checkpoint pacing, pod region | **Buying a bigger card changes nothing** — `project_df7_data_parallel_delta_merge`'s own conclusion is that DF.7 scales throughput, not single-stream latency. The work this implies is **op batching** (fewer, fatter teach frames) and a **closer pod region**, plus checkpoint cost. Filed as `TEACHMIRROR.3` with the measurements attached so the next "why isn't it pegged" starts from numbers. **2026-08-21 advance:** the op-batching half landed — `hebbian_ranges` carries whole rep doses in ~60-byte frames (intra pass, alphabet drills, sem→sem), the type-13 masked verb carries lateral inhibition with zero pre-side wire, and per-op RTT round-trips shrink accordingly; KI-24's pump fix removes the false "4MB/s" ceiling from move-ins. Remaining: checkpoint disk cost + the RTT/busy split measurement. |
| KI-22 | 🟢 FIXED | **A release could publish nothing and log all-green.** `donor-release.yml` pushed its site-link bump with the Actions token and assumed that push would trigger `deploy.yml`. Actions-token pushes **never** trigger workflows (recursion guard), so `donor-v0.3.23` shipped a correct tag, release, assets and binary while the public download page served `.22` links for hours — caught off the page, not off the log. | `.forgejo/workflows/donor-release.yml`, `.forgejo/workflows/deploy.yml` | **2026-08-20 (PAGESTALE.1):** the release job now rsyncs the frontend **itself** as its final step (same `PAGES_DEPLOY_*` secrets, exclude list byte-identical to `deploy.yml`'s, cross-referenced in both files), and fails LOUD naming `workflow_dispatch` as the fallback if those secrets are ever unavailable. Verify next tag by the presence of `Published the frontend with donor-vX.Y.Z download links.` — its absence means a stale page however green the run looks. |
| KI-1 | 🔴 OPEN | **~15-min cold boot.** Restart costs a long synchronous boot before the brain can think. | `js/brain/cluster.js` construction, `server/brain-server.js` | Root cause inferred from code (not yet measured): single-threaded construction of the biological-scale neural graph — wiring billions of sparse synapses (`size × fanout`) across clusters + the cortical-microstructure passes (small-world topology, microcolumns, 6-layer lamination, hub selection, cross-projections). Donor-upload wait is only a 20 s fallback, NOT the bottleneck. See KI-1 detail below. |
| KI-2 | 🟡 IN PROGRESS | **Leaderboard duplicate named rows.** Named donors spawned MANY rows of the same name; routing IDs leaked as anon rows. | `server/brain-server.js`, `server/brain-server/state.js`, `html/compute.html` | Fix coded + verified on `feature/hotfixes` (LB.1–LB.4 in `TODO.md`): named donors collapse to one `name:<lower>` row, anon→named migration, self-heal on load, defensive merge in live-state. **Not yet deployed** — live brain's persisted leaderboard only heals on the next restart. |
| KI-3 | ⛔ BLOCKED | **"Update & Savestart" self-deploy button.** Dashboard button can't self-serve a redeploy. | dashboard + box | Intentionally NOT wired — needs box prereqs (sudoers `NOPASSWD` for `systemctl restart unity-brain` + a deploy-key repo clone). Sponge re-confirmed this is a **safety risk**; leave it until explicitly approved. |
| KI-4 | 🟡 IN PROGRESS | **Cognition / basin quality — mode collapse.** Trained basins collapse into superposition; motor argmax can lock on a single word. | `js/brain/curriculum.js`, cortex Hebbian path | `_rectifySemMotor()` rectify continues instead of halting on saturation. **Held-back remediation (`docs/HELD-BACK.md`) now targets this** — failed/noisy cells get drilled through the de-saturate+inhibition ladder, and the outcome-gated noise gate (`DREAM_NOISE_GATE`, default OFF) suppresses meaningless-noise reinforcement while preserving exploration. **SPEAK.2/10c (2026-07-01) add:** post-teach L2 renorm of `sem_to_word_motor` (basins discriminate on direction not magnitude) + a surprise-gate saturation clamp (stop amplifying plasticity while saturated → stop learning own noise). Stays open until verified on a live walk. Deep dive: `docs/ISSUE-basin-collapse-fix.md`. Related: KI-9. |
| KI-5 | 🔴 OPEN | **K-STUDENT battery stall.** Student-test battery probes could stall the walk. | curriculum probe path | Status + repro tracked in `docs/ISSUE-student-battery-stall.md` — consult that file for current state before assuming fixed. |
| KI-6 | 🔵 SEE AUDIT | **Perimeter hygiene (network bind, auth-free privileged endpoints, curriculum monolith, hot-probe perf).** | stack-wide | Full ruthless audit in `docs/Problems.md`; several criticals already marked FIXED there (loopback bind, `/shutdown` loopback gate). Treat `Problems.md` as the authoritative source for these — don't re-litigate here, just mirror status when one closes. |
| KI-7 | 🟢 CLOSED (2026-06-24) | **Public dashboard feels stale / doesn't update much.** ~~Desired: reliable ~5 s auto-pull.~~ | `html/dashboard.html` (PUBLIC_MODE), `html/dashboard-public.html` | **Closed per Sponge — outdated assumption.** The public view already auto-polls `/public-state.json` every 3 s against a snapshot the server refreshes every 100 ms (`STATE_BROADCAST_MS`), so the "doesn't auto-pull" premise was wrong. Latent caveat preserved (NOT reopened): WS-only panels don't refresh in public mode — see KI-7 detail. Reopen only if a specific panel is observed frozen and it matters. |
| KI-8 | 🟢 CLOSED (2026-06-27) | **Public dashboard prompted admin login.** Visiting the public dashboard threw a Forgejo Basic-auth login prompt (`401 /admin/milestone`) at every viewer. | `html/dashboard.html` (PUBLIC_MODE) | **Fixed (PD.1, `feature/public-dashboard-donor-ux-fixes`).** `dashboard-public.html` redirects to `dashboard.html?public=1`; `refreshMilestone()` + its 5 s `setInterval` polled `adminApi('milestone')` → `/admin/milestone` UNCONDITIONALLY → 401 + login prompt. Now gated behind `!PUBLIC_MODE` (the milestone/save-state panel is `.admin-only` anyway). Audited as the only unconditional admin poll — auto-advance is `isAdmin`-gated, the gate-probe timer does no fetch. |
| KI-9 | 🟡 IN PROGRESS | **Word salad after 9th grade.** Unity spoke fine at K but degraded to sequence-random "topically-nearby salad" by ~G9. | `js/brain/cluster/emit.js`, `js/brain/curriculum*`, `server/brain-server.js` | **Root cause + fix coded (SPEAK, `feature/unity-speech-consciousness-rectify`, 2026-07-01).** The `word_motor` emit band was recomputed every emit from the LIVE word-list length, so each new grade's vocabulary silently remapped every prior word's physical neuron band — bucket INDEX was append-only but the physical band drifted, desyncing all prior `sem→word_motor` weights (fine at K, salad by G9). Fixed by frozen geometry (SPEAK.1 `wordBucketCellSizeFor`), basin separability renorm (SPEAK.2), grade-owned word→word transitions (SPEAK.3), reject-to-silence floor (SPEAK.9), and the repeat/surprise loop caps (SPEAK.10b/10c). **Stays open until the live K→PhD walk confirms ordered G9 speech.** Full write-up: `docs/unity-speech-consciousness-rectify.md`; hand-off: `docs/SPONGE-SPEAK-VALIDATION-HANDOFF.md`. Related: KI-4. |
| KI-10 | 🟢 FIXED (2026-08-17) | **Drop-on-chat: saying anything to her killed the donor** (~1s message-to-disconnect, full ~10min re-upload each time). | `js/brain/cluster/emit.js`, `server/brain-server/gpu.js` | TWO stacked causes, both fixed: the reply's per-letter loop ran a synchronous full-matrix propagate (360M nnz) + allocated a fresh ~96MB Float64Array PER LETTER (now: pooled input/output buffers + time-sliced `propagateChunked` — identical math, the loop serves donor keepalives mid-reply); and the wire was chronically drowned by KI-11's monster templates, so the chat burst was the last straw. Donor held through post-fix chat on a clean wire. **2026-08-18 addendum: a FIFTH strain outlived these** — the chat text-injection itself shipped a dense 23.4MB JSON current-slice per message (which the native donor silently discarded — a no-op bomb); fixed by sparse injection (160 bytes), and the hi-test finally PASSED end-to-end: no freeze, no drop, donor RTT steady. The war is closed. |
| KI-11 | 🟢 FIXED (2026-08-17) | **Monster templates drowned the donor wire** — 95K teach-frame sheds, 46K suppressed Hebbian updates, donor RTT 7.6s, 18MB parked buffer. | `server/brain-server/gpu.js` (t11/t10 encoders) | Named by SendForensics in one read: 504,000-value "templates" (the full fineType grammar-band pattern fed through an encoder built for ~300-value embeddings) shipped as 1,968.8KB frames TWICE per rep (main + GINTRA twin) ≈ 4MB/rep into a 4MB/s wire. Fixed by encoder canonicalization (zero-trim + contiguous-run fold — the same band ships as ~30 bytes, lossless); any >4096-value survivor warns loudly. Verified live: sheds 0, suppression 0, buffer 0.0MB, RTT 66ms, t11 avg ~204B/frame. |
| KI-12 | 🟢 FIXED (2026-08-17) | **BLOCKED-every-second console carpet + ~1s event-loop lag** — the dashboard state build ate ~36% of wall-clock and taxed every teach yield ~300ms. | `server/brain-server/state.js`, `server/brain-server/memory.js` | `getState()` measured 312ms/call at ~1/s (bcast telemetry): the lang-region spike fallback walk (~14M cells/call), sync SQLite aggregates in memoryStats + growth, and TWO full 12M walks counting the STATIC layer histogram + hub count. All cached (5s human-cadence, or once keyed on array identity). Live post-fix: getState ~9-13ms/call, loop lag 9-151ms. The BLOCKED lines' phase tags were an attribution artifact (they blame whatever teach phase is active). |
| KI-13 | 🟢 FIXED (2026-08-17) | **Empty-wire Hebbian suppression** — ~20 GPU training updates/s silently dropped with a 0.0MB buffer. | `server/brain-server/gpu.js` (both pattern-lane governors) | The 3ms base admission throttle refused group-first frames on an EMPTY wire; each refusal marked the lane STALE, and stale only clears on a now-rare scoped t9 send — so one refusal poisoned whole windows of bound-Hebbian dispatches (cross-projection AND intra). Fixed by queue-gating the base term exactly like the RTT term: refusals engage only at ≥256KB real buffered pressure. Stale/shed protection under genuine saturation unchanged. |
| KI-14 | 🟢 FIXED (2026-08-17) | **Vocabulary counter lied** — "defs taught: X / 2,247" used K's list as the denominator against an already-journey-wide numerator (it once read 2,287/2,247). | `server/brain-server/state.js`, `html/dashboard.html`, `js/brain/grade-vocabulary.js` | The real denominator is the FULL K→PhD journey: 49,921 words summed across all 19 grade lists, **18,017 unique** (`fullJourneyVocabularyStats()`, computed once at boot). Panel renders `VOCABULARY (K→PhD)` with the journey total (… until the warm completes — never an invented number); the per-render grade-prefix rewrite that restamped "K-VOCABULARY" over it is gone. |
| KI-15 | 🟡 IN PROGRESS | **The 1300–1500 teach/min band** — historical rate at 1.5M; the 12M walk must reclaim it. | teach path, whole stack | Every named blocker is fixed (GINTRA, STALEGATE, SHADOWTIME, HOPFIX, BCASTFIX, CONSCFIX, TMPLFIX, PAIRSLICE); measured climb 200 → ~1,100 teach/min with the wire clean. The band verdict reads at the association-pair phases on the current build — `liveness.stageProfile` names any remainder. Word/definition phases are structurally lower-rate (each call = a whole word or a full multi-definition bind) — a low counter during those stretches is a different gear, not a stall. |
| KI-16 | 🔴 **OPEN — VERIFICATION PASS DONE 2026-08-27. Confirmed, LIVE (not dead code), and there are TWO sites, not one.** | **`writeSpikeSlice` called with ONE argument at two sites** — `_writeTiledPatternOffset` (`curriculum.js:15720`) **and `_writeQuestionTemplateTag` (`curriculum.js:14156`)**, the second undocumented until now. Proxy signature is `(regionName, sparseIndices)` (`brain-server.js:3345`). | `js/brain/curriculum.js:14156` + `:15720` · receiver `server/brain-server/gpu.js:4594` | ⭐ **THE PASS THIS ROW ASKED FOR, ANSWERED.** **(1) NOT dead code:** `_writeTiledPatternOffset` has **6** live call sites, `_writeQuestionTemplateTag` has **7** — **13 combined**, on definition/question teach paths. **(2) It does NOT throw**, so the `catch { /* non-fatal */ }` around both calls never fires and nothing is logged: `gpu.js:4670-4674` is defensive (`Array.isArray ? … : length-check ? … : []`), so with `sparseIndices === undefined` **`arr` becomes `[]`**. **(3) There is NO length guard before dispatch**, so a frame is genuinely SENT: `name = ` `` `cortex/${regionName}` `` with `regionName` bound to the **index array**, i.e. a header naming `cortex/<the entire comma-joined index list>`, carrying `writeUInt32LE(0)` and an **empty** payload. ⛔ **So the real defect is worse than "garbage region names": the spikes never reach the GPU at all while `cluster.lastSpikes[idx] = 1` writes the CPU shadow — a SILENT CPU/GPU divergence on 13 call sites, plus a junk frame that inflates `teach_ops`** (the counter `TEACHMIRROR.1` exists to distinguish a saturated donor from an idle one, and `GOTCHA.9` was about exactly this pollution). ⚠ **Two guards mean it is invisible when no donor is attached** — `_gpuWriteCortexSpikeSlice` returns early on `!this._gpuClient` and `!this._donorPatternLaneOpen()`, so a local no-donor run reproduces nothing. ⚠ **NOT fixed here and deliberately so:** the correct region name for each site must be established per call site (both push **cluster-absolute** indices, which is the second half of the original anomaly), and `feedback_fix_the_chokepoint_not_the_instance` applies — **fixing only `_writeTiledPatternOffset` would leave the twin.** ⚠ **Unverified consequence, flagged not claimed:** the repeat-compression cache keys on `'7:' + name`, so a per-call-unique garbage name may defeat that cache or grow it — **I did not check whether it is bounded.** ⚠ **Line numbers re-pointed 2026-08-29 (provenance re-verify on `cd465955`):** code above the sites moved (+158 curriculum.js / +455 brain-server.js / +82 gpu.js since the 2026-08-27 read) — was `:14049`/`:15613`/`:3276`/`:4530`/`:4606-4610`. **The defect itself is unchanged: both sites still pass ONE argument.** ⚠ Re-pointed again 2026-08-29 (`0ee5ac68`, LANGHOP hop 2 shifted `brain-server.js` below ~2850 by ~+52): the proxy-signature cite moved `:3293` → `:3345`; the curriculum.js/gpu.js cites did not move this hop. |
| KI-17 | ⛔ BLOCKED | **Language-growth hop 2 (~20M)** — two prerequisites before the next size press. | donor protocol + upload lane | (1) Segmented-rowPtr donor release: at ~20M rows the intra rowPtr alone is ~80MB > the native donor's 64MiB message-reassembly cap (donor releases are the operator's territory). (2) The upload-compression decision — canonical upload measured ~12min at 12M; ~35min projected at 20M without compression. |
| KI-18 | 🟢 FIXED (2026-08-18) | **Drop-on-speak, the final strain: chat text injection was a 23.4MB no-op bomb.** Every chat message built a dense Float32Array over the whole phon region (12M+ floats), Array.from + JSON.stringify (~100MB heap churn, multi-second pin), and sent it as `values:` — a field the native donor's WriteCurrentSlice deserializer does not even have: serde discarded it, so her Wernicke's area never received chat text. 26s of starved pings killed the donor on every hi. | `server/brain-server.js` `injectText()`, `donor-app/src/protocol.rs` | Named live by SendForensics + chat-stage eyes through the remote console tunnel; fixed by sparse injection (`sparseIndices`/`sparseValues`, same char-hash math accumulated in a Map — "hi" = 6 entries = 160 bytes, 146,000×). Verified live: LARGE-sends=0, donor-drops=0, RTT held 61ms through a full reply. Side effect: chat text lands in her language cortex for the first time on native donors. |
| KI-21 | 🟡 IN PROGRESS | **A language-cortex geometry the box re-decides every boot.** Sizing derived from `os.freemem()` meant the same host came up at 12,000,000 neurons on a quiet boot and 349,155 on a busy one — silencing every word past index ~20,950 of a ~60,000 vocab target and orphaning matrices trained at the other size. | `server/brain-server.js` boot sizing | **2026-08-20 (LANGRAM.6):** the chosen size is pinned to `server/lang-geometry.json` and **the pin wins** while weights exist on disk; the RAM/V8/VRAM bounds can no longer silently resize a trained brain. A change is always explicit and always loud — `DREAM_LANG_CORTEX=<n>`, `DREAM_LANG_UNPIN=1`, or a fresh walk (weights wiped ⟹ pin stops applying). Stays IN PROGRESS until a boot is observed printing `geometry pin CONFIRMED` / `PIN HELD`. **2026-08-20 (LANGRAM.7) — the pin had a HOLE on the one boot that matters most.** Both guards were gated on weights existing, so a **fresh walk** (weights wiped) honoured no pin AND could not trip the pin-withheld guard — it simply repinned whatever `os.freemem()` allowed, i.e. a busy-boot fresh walk would come up at 349,155 and make it **permanent**. Fixed with a narrow **fresh-walk floor** (fires only when the pin is at/above the 12,000,000 target and the derived size is below it — a floor, never an inflation) plus removing `_weightsOnDisk` from `_pinWouldDegrade`. Re-priced per LAW: **12M is the priced geometry** (8.5 min/cell, ~16h/114 cells, 4.589GB VRAM); 349,155 is the unpriced one. **2026-08-29 (LANGHOP hop 2) — the two clauses above are SUPERSEDED.** The target is now **20,000,000** (`WORD_MOTOR_TARGET_LANG_CORTEX`, `brain-server.js:2856`; the RE-PRICE lives in the code comment above the constant) and the floor is **per-host AFFORDABLE geometry**: `_langAffordable` (`:2932-2943`) derives the largest size ≤ target that fits BOTH the derived VRAM ceiling and the RAM/V8 floor, and the WMB floor raises the derived size to that (`:2945-2956`) rather than to a fixed number — so a 32GB box that can never reach 20M still earns its pin at its own maximum (~14.4-16.8M; a 16GB-card host lands ~16.85M). The LANGRAM.7 fresh-walk rescue is re-conditioned to restore ANY previously-run pinned size ≤ target (`:3046-3070`), no longer only a pin at/above the old 12M target; PIN WITHHELD judges against the host's affordable size, not the target (`:3087-3099`). `WEIGHTS_FORMAT_VERSION` 5→6 (`:1129`) ⟹ fresh walk required on both brains. |
| KI-20 | 🟡 IN PROGRESS | **Nothing enforced browser-bundle freshness.** A `js/brain/*` edit pushed without a local `npm run build` ships a bundle that silently disagrees with the server; the box has no esbuild and rsync publishes the stale artifact happily. | `js/app.bundle.js`, `server/brain-server.js` | **2026-08-20 (LOOPNAME.13):** the server compares bundle mtime against the newest of `BRAIN_CODE_FILES` (the same list the code-hash uses, minus `brain-server.js` — not a bundle input) at boot, prints `⛔ STALE BROWSER BUNDLE` naming the offending source and its age, and exposes `bundleFreshness` on the state payload. **Detection only — the fix is still a local rebuild + redeploy**, so this stays open as a process gap (a pre-push gate is filed as `SCRIPTKILL.1`-adjacent work and not built). |
| KI-19 | 🟡 IN PROGRESS | **First reply after boot pins the loop ~21.5s (respond stage) + ~3.4s (generate)** — non-lethal since KI-18 (nothing parks in the donor buffer; the donor rides it out), but it is dead air on her first answer. | chat reply path (`server/brain-server/chat.js`) | Instrument-before-fix: respond-stage sub-stamps (silence-gate / learn-words / episode-store / curiosity / history-push) + a wall-clock split on any respond pass >2s. Two suspects on file: one-time warm-up (VoiceSynth READY printed at pin-end — WEAK evidence, worker messages deliver late through a pin) and the curiosity-followup branch's INLINE awaited teach (the same concurrent-teach shape the chat pair queue was built to kill — must route through the queue). Also filed: the walk's Oja active-set inflates to ~2.4-2.6M rows during chat windows (sliced, non-pinning, ~5.6s/pass throughput cost). **2026-08-20:** the second suspect was REAL and is fixed (FIRSTPIN.2 — the inline teach now enqueues on a job queue that keeps its `relationTagId=23` definition channel and drains on the walk lane), the stamps are complete (FIRSTPIN.1 added `respond:silence-gate` + `respond:route-return`), and `generate` is split into `generate:primary(Ncand)` / `generate:continuation-K(Ncand)` — a reply is up to SEVEN emissions, which is the likelier residence of the seconds. Also fixed on the same path: the `img-detect` stage was an inline mind's-eye preview (4,925ms), now queued. Stays open until a live first-reply split is read on the pressed build. |
| KI-25 | ⛔ BY DESIGN | ⚠ *(was filed as a second KI-18 — a duplicate ID; renumbered 2026-08-25, content unchanged)* **Pollinations keys no longer work — free tier only.** `[VisualMemory] reference fetch ... HTTP 402` lines in the console. | `server/brain-server/visual-memory.js` | EXPECTED behavior, not a bug: the mind's-eye reference lane (the "picture book" for never-seen concepts) skips a refused fetch and her equational imagination continues from the visual-memory store. Operator directive 2026-08-17: do NOT change the image-generation code or chase 402s. |
| KI-26 | 🔴 OPEN | **`DREAM_DELTAIDX` is DISABLED and nobody knows why.** The delta column-index encoding — which took 68% off the largest half of every matrix upload — is off, and the reason it was turned off was never recorded. | `server/brain-server/gpu.js` | ⛔ **Do not simply re-enable it.** An optimisation disabled for an unrecorded reason is a trap in either direction: leaving it off may be costing a large upload saving, and switching it on may re-introduce whatever caused it to be disabled. The work is to establish the cause FIRST. Its cross-language encoder parity test still exists and still passes, so the codec is not the suspect. |
| KI-27 | ⛔ **BY DESIGN (answered 2026-08-25, status corrected 2026-08-27)** | ~~**`SUBSTEPS` adaptive controller: zero batches in 65 minutes behind the probe gate.**~~ **The batches are not issued behind the probe gate BY DESIGN, and the instrument now says so in words.** | `server/brain-server/gpu.js` → `profiling.throughput.batchPaused` | ⭐ **ANSWERED EXACTLY AS THIS ROW DEMANDED — measured, not guessed.** Read live 2026-08-27: `batchPaused = {reason: "probe-gate (cortex owns the GPU exclusively for this cell)", cell: "ela/kindergarten", sinceLastBatchMs: 20072438, expected: true}` with **`batchStall: null`**. ⭐ **`expected: true` is the whole fix** — the "zero batches" symptom and a genuine stall were previously *indistinguishable*, and now they are two different fields. ⚠ **`sinceLastBatchMs` 20,072,438 ms ≈ 5.58 h against an uptime of 5.60 h means NO batch has run this boot at all** — and that is the designed pause, not a fault. ⭐ **And the controller's input is healthy when batches DO flow:** `batchTiming` reads `roundTripMs`/`roundTripEmaMs` (**round-trip**, which was the specific trap this row flagged — reading tick time pinned it at its floor while looking present), with `substeps: 54`, `dispatchesDuring: 5`, `samples: 13`. ⛔ **This row stayed 🔴 OPEN for two days after being answered** — the answer landed in `docs/TODO.md` (`SUBSTEPS.6`) and never propagated here. **A ledger that is not re-read is a ledger that lies by omission.** ⭐ **2026-08-29 (PSITEACH.2): the designed pause is now CORTEX-ONLY.** While the probe gate is held, a walk heartbeat dispatches a `compute_batch` over the seven NON-cortex clusters every `DREAM_WALK_TICK_MS` (default 15s; `0` restores the full hold; stats in `state.walkTick`) — cortex stays excluded because teach writes spike patterns its bound Hebbian ops read. So *"no batch for a whole boot"* is no longer the expected read on a walking brain; `batchPaused.expected` still names the cortex exclusivity, which is real and unchanged. |
| KI-28 | 🟢 FIXED (2026-08-25) | **Five instrument blocks are published every broadcast and rendered nowhere.** `state.voice`, `state.profiling.loopStarve`, `state.ownArt.practice`, `state.ownArt.feedback`, `state.curiosity`. | `html/dashboard.html` | The audit is decidable rather than opinion: the board renders state **BY NAME ONLY** (24 explicit `row()` calls plus per-panel interpolation, no generic walk over the state object), so zero references in the page **is proof** of non-rendering. Worst is `state.voice` — that block was built specifically to REPLACE the misleading `canSpeak` field, so the misleading field was removed and its honest replacement was never surfaced. It is also the only field that distinguishes *"no sample exists"* from *"she cannot speak"*. Fix = add the rows; the fields already exist and need no server change. |
| KI-29 | 🟢 CLOSED (2026-09-06) — **NOT INFLATION; the number is exactly what the encoding predicts** | **Oja active-set inflation — `activeSum` observed at 1.06B.** | `js/brain/sparse-matrix.js`, `js/brain/curriculum.js` | Carried as a WATCH rather than a bug because it has not been shown to cause incorrect training — only to cost work. It was surfaced during the block-wall investigation and deliberately left open when the surrounding synchronous-propagate slabs were fixed, so that a real cause is not attributed to it by proximity. ⭐ **CLOSED BY DERIVING WHAT THE NUMBER SHOULD BE, which is what this row always lacked — it was opened with no threshold, so "it went up" (1.06B → 1.09B) was never evidence of anything.** Read live: **1,089,587,894 over 170,334 lateral calls = 6,397 cross-bucket actives per call.** The writer tiles each surviving feature dimension across `gSize = floor(motorSize / featLength) = floor(346,902 / 300) = **1,156**` cells, so actives = `positiveDims × 1,156` and the cross-bucket set is that minus the primary bucket. With `motorTopK = 15`, winner-take-all by **magnitude**, and the writer's `feat[d] <= 0` skip, roughly 45% of survivors are positive ⟹ ~6.75 positive dims, ~1 in the primary bucket ⟹ **~5.75 cross-bucket dims ≈ 6,647**. **Measured 5.53 dims. Predicted 5.75. Within 4%.** ⭐ Confirmed empirically in the same pass: a harness trial through the real writer with 19 random dims of which 9 were positive produced **exactly 10,404 = 9 × 1,156**. ⚠ **The threshold this row needed now exists: expected active count is `positiveDims × 1,156`, and a departure from THAT is the thing worth alarming on** — a raw total has no meaning without the expected value beside it. |

---

## Operational hazards (not bugs — but they bite)

These are environment gotchas the hard way, carried from prior sessions. Not
"issues to fix" so much as landmines to route around.

- **Never `pkill -f "brain-server.js"`** — the pattern matches the killing shell's own argv and self-kills the command (empty output, exit 1). Kill by captured PID.
- **`lsof -ti:PORT | kill` over-kills** — it matches BOTH the brain (listener) and any donor (client socket) on the same port. Capture and target the right PID.
- **Never stress GPU 0** — it's the user's display GPU (RTX 4070); a high-util run on it once crashed the whole desktop. Test only GPU 1 (RTX 2060), low util, short, with a timeout.
- **Tarball-overlay deploy** — `/opt/unity-brain` is NOT a git checkout. Deploy = `scp` up → `sudo cp` into place → **always** `sudo chown -R unity:unity /opt/unity-brain` (sudo cp leaves files root-owned → EACCES on weight save).
- **A restart is expensive** — see KI-1; ~15 min boot + loses up to ~5 min of curriculum progress since the last checkpoint. Check md5s before deciding a deploy even needs a bounce.

---

## KI-1 detail — the ~15-min boot

The boot is CPU-bound brain-building, not I/O or network. Dominant costs, in order:

1. **Synapse wiring (the big one).** Per cluster the intra-synapse matrix holds
   `size × fanout` non-zeros (`cluster.js`). At biological scale cortex is on the
   order of hundreds of millions of neurons and language cortex auto-scales into
   the tens of millions, fanout ~30 → **billions** of synapse entries to allocate
   as TypedArrays, compute deterministic small-world neighbours for, and populate —
   single-threaded in Node.
2. **Cortical microstructure passes** layered on every cortical region —
   Watts-Strogatz small-world topology, microcolumn assignment, 6-layer lamination,
   hub-neuron selection, topographic cross-projections (each O(neurons) or
   O(neurons × fanout)).
3. **Supporting boot** — semantic-embedding init, binary weight deserialization,
   hippocampus Tier-2 / Tier-3 schema stores.
4. **Then** the sparse cortex streams to the donor GPU and waits for ~20 warmup
   batches (or a 20 s fallback) before curriculum can run.

**Not yet done:** capture the REAL per-phase millisecond breakdown from a live boot
(`journalctl` on the box — the construction already logs `…ready in ${ms}ms` per
cluster) to confirm whether the wiring loops are the actual hog or something dumber
stalls it. Until then the breakdown above is read-from-code inference, not measured.

---

## KI-7 detail — public dashboard staleness

Symptom (Sponge): the public dashboard "doesn't update that much"; wants a reliable
auto-pull (~5 s is fine).

What's actually wired today:

- `html/dashboard-public.html` is a redirect → `html/dashboard.html?public=1`.
- In `PUBLIC_MODE`, `connect()` calls `startPublicPolling()` and **returns before
  opening the admin WebSocket** (`dashboard.html` ~line 904).
- `startPublicPolling()` fetches `GET /public-state.json` every **3 s**
  (`setInterval(poll, 3000)`).
- The server rebuilds that cached snapshot every **100 ms** inside the broadcast
  loop (`STATE_BROADCAST_MS = 100`, refresh at `brain._publicStateJson = …`).

So the snapshot is fresh and the poll is frequent — the gap is that the **admin
view's panels are updated inside `ws.onmessage`**, which never fires in public mode.
Only whatever the `poll()` render path touches actually refreshes; every WS-only
panel is frozen at first paint.

Fix direction (when we pick it up): make the public `poll()` path render the SAME
full panel set the WS handler does (factor the render out and call it from both),
so all panels move off the cached snapshot. Poll cadence can stay 3 s or move to
5 s — that's the easy part; the panel coverage is the real fix.
