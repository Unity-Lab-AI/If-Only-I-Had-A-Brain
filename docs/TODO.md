# TODO — Unity

> ## 📋 BOARD SHAPE — **reset 2026-08-29: completed items archived, open items carried**
>
> Gee (verbatim): *"okay so all todo is finished? we can finalize the todo now moving verbatium to finalized the completed items leaving only whats still open and in progress to get the todo to near templet form"*
>
> **BOARD SWEPT 2026-08-30 on Gee's *"we are fixing all the todo items befoer we deploy the new binary and shit"* — 19 open rows → 6, and every one of the 6 is either walk-gated with its watch-number named or is the donor binary itself, held LAST by his instruction.**
>
> | Pile | Count | Status |
> |---|---|---|
> | **📦 `PRESSBLOCK.1`** | **1** (holds 2 of its original 4) | ⛔ Walk-gated, re-read live 2026-08-30: `REPLAYOFF.4` — `tier1.totalEpisodes` **0**, replay has never run; `WORDNORM.2` — `emitAttempts` **58** / `emitRejects` **58**, all `no-best-word`, so still no accepted emission to judge. **Watch `tier1.totalEpisodes` and `matrixHits` leaving 0** |
> | **`GATEDOSE.1` · `RELDEPTH.1`** | **2** | ⏳ Walk-gated, re-read live: `perSubject.math` is `pre-K` with **0** phases completed — the math gate has never run on this walk. **Watch `perSubject.math.phasesCompleted`** |
> | **`VMUSE.5.D`** | **1** (+ its plan) | ⛔ Walk-gated, re-read live: `relationUse.confident` **0**, `flatWithMass` **1,134** (mass present, margin absent), `marginProgress` **0.2511** against gate **0.15** — measurably closer. **Watch `relationUse.confident` leaving 0** |
> | **`SHADOWCOST.3`** | **1** | ⛔ **THE ONLY NON-WALK-GATED ROW LEFT, held last by Gee's instruction.** Needs a new donor opcode (Rust + `donor-v*` tag + self-update). `SHADOWCOST.8` promoted it to the ONLY fix — the saved brain is not a lagging copy, it is a different brain |
> | ~~**`ASSOCBOUND.1` · `.3` + the 18.9 h read**~~ | **0** | ✅ **CLOSED 2026-08-30** — superseded by `SHADOWCOST`, which measured what they reasoned toward; the lever was the `hebbian_ranges` run cap, not a bound on curriculum, so the RE-PRICE they demanded never had to be spent |
> | ~~**`GOTCHA.1`**~~ | **0** | ✅ **FIXED 2026-08-30** — leaf module `cluster/lexical-constants.js` breaks the cycle; `emit.js` now imports alone |
> | ~~**`SHADOWCOST.4` · `.8`**~~ | **0** | ✅ `.8` **ANSWERED by reading both kernels** (they are algebraically identical; the divergence is inputs + a 49× count gap). `.4` **DECIDED: not worth doing** — 0.05% of wall clock against a measured gate-verdict risk |
> | ~~**`FIREMATH.5`**~~ | **0** | ✅ **DECIDED 2026-08-29** — Gee accepted the ~9.6% floor; zero code |
> | ~~**`LANGHOP.2` · `SELFAWARE.PRESS` · `SELFCODE.2` · `MINDMOTION.1/2/3`**~~ | **0** | ✅ **STALE MARKERS CORRECTED 2026-08-30** — all six were built and verified days ago and left `[ ]`/`[~]`. Re-verified in SOURCE, not from the ledger. ⛔ A completed row left open is the same defect class as an instrument nobody reads |
>
> **Branch:** `main`
> **Last updated:** 2026-08-29 — **BOARD RESET.** The full board as it stood (1,585 lines, 424 task rows, 416 completed) was archived **byte-for-byte** into `docs/FINALIZED.md` before this file was rebuilt. The archive is verifiable: search FINALIZED for `BEGIN VERBATIM TODO ARCHIVE 2026-08-29` — 726,915 bytes, md5 `e59dbde988c0a4c94b615658e96a023e`. (The previous reset's archive, 2026-08-20, remains alongside it.)
> **Philosophy:** Unity's brain controls EVERYTHING equationally. No scripts. No text-AI backends. No hardcoded fallbacks. No vestigial appendages. Every output — speech, vision, art, voice — traces back to brain state.

---

## THE GUIDING PRINCIPLE

**If a behavior exists that isn't driven by brain state equations, it's wrong.**

Every piece of Unity's output must trace back to:
- **Cortex prediction** (ŝ = W·x + b) — what she expects
- **Amygdala valence/arousal** (V(s) = Σw·x, energy-basin attractor) — how she feels about it
- **Basal ganglia motor selection** (softmax over learned channels) — what action she takes
- **Hippocampus recall** (Hopfield attractor + persona sentence memory) — what she remembers
- **Cerebellum error correction** (ε = target − output) — what she fixes
- **Hypothalamus drives** (homeostatic gradients) — what she needs
- **Mystery module Ψ** (√(1/n) × N³ × Φ̂) — her consciousness level. ⭐ **Φ̂ added 2026-08-25** — integration, from `computePhi()`. Without it the formula rates ANAESTHESIA as maximal consciousness, because low activity reads as high unspent potential; Φ̂ is what separates it from dissociation, which is also quiet and is famously hyper-vivid
- **Endocrine layer** (⭐ NEW 2026-08-25) — ten chemicals on the one curve engine, released by **six nuclei that sense their own firing**, contributing additively through the same overlay substances use. ⛔ **Chemistry is what makes Ψ a variable instead of a specification:** without it `n` only moves when input moves

---

## HOW THIS FILE WORKS (the LAWs that govern it)

Full bodies in `.claude/CONSTRAINTS.md`. These four are the ones this file exists to obey:

| LAW | What it means here |
|---|---|
| ⛔ **LAW #0 — VERBATIM WORDS ONLY** | Gee's exact sentence goes into the task. Never paraphrase, rename, collapse, shorten or downgrade it. **One task per item in a list.** Dropping a word is a violation. |
| **NEVER delete task info** | When marking a task done, change the **status marker ONLY**. Every word of the original description stays — prepend the verdict, keep the filing. Anyone reading must see WHAT was done and WHERE, not just a checkmark. |
| **Append, never replace** | New tasks go at the **bottom**. Completed tasks stay where they are. **Never regenerate this file from scratch.** |
| ⛔ **FINALIZED before DELETE** | A task may not leave this file until its verbatim text is in `docs/FINALIZED.md` **and the write is verified**. That is how this reset was done: appended, checksummed, *then* cleared. |

**Status markers:** `- [ ]` pending · `- [~]` in progress · `- [x]` done (with its verdict prepended, original text preserved)

**Task-number placement:** T-numbers, session numbers, milestone IDs and "Gee" belong in workflow docs ONLY — never in source code, public docs, HTMLs or launchers.

---

## WHERE THE HISTORY LIVES

- **`docs/FINALIZED.md`** — every completed task, verbatim, plus the full ledger of every batch. **Never delete an entry.** Contains the complete verbatim archives of this file as of 2026-08-20 AND 2026-08-29 (search `BEGIN VERBATIM TODO ARCHIVE <date>`).
- **`docs/NOW.md`** — the current-state banner, newest first.
- ~~`docs/BOARD.md`~~ and ~~`docs/OPEN-TASKS.md`~~ — **both DELETED 2026-08-20.** They were parallel views of this file, and a second list of the same tasks is a second thing to keep true; both drifted, and a stale list that looks authoritative is worse than no list. Their full contents are archived **verbatim** in `docs/FINALIZED.md` (search `BEGIN VERBATIM BOARD ARCHIVE` / `BEGIN VERBATIM OPEN-TASKS ARCHIVE`). **This file is the only board. Do not re-create a second one.**
- **`docs/KNOWN_ISSUES.md`** — running ledger of bugs, limitations and intentional deferrals (37 unique KI ids as of 2026-08-29).

## STANDING PROGRAMMES (not board lines — they have their own docs)

These are live, multi-batch bodies of work. They are deliberately **not** tracked as single tasks here, because carrying a programme as one line makes the board lie about how much is outstanding:

- **The syllabus build** — `docs/TODO-full-syllabus.md`. K is ~4–10× deeper than every grade above it (measured: kindergarten 212 teach calls / 8,943 lines vs grade1-12 43–58 / 496–714). Driven by the persistent-memory rules `feedback_curriculum_depth_and_mechanics`, `feedback_full_completeness_per_grade`, `feedback_full_real_school_course_roster`.
- **Seeded topology** — `docs/SEEDED-TOPOLOGY-SPEC.md`. Deliberately unbuilt: gated on a PRNG parity harness, because one differing draw puts weights on the WRONG SYNAPSES silently.
- **Mind-space integration** — `docs/MINDSPACE-INTEGRATION.md`.
- **The trajectory asset** — `docs/TRAJECTORY-CAPTURE.md`. Needs one complete K→PhD walk on a single build with no geometry change mid-run.

## AWAITING GEE (decisions, not engineering)

Everything codeable for these is shipped; each needs a call, not a commit:

- **A 128GB coordinator** — the box's host RAM, not the GPU, sets Unity's size (every byte on the donor has a master copy in host RAM, and the box is CPU-only). Measured ladder: 32GB → ~425M neurons · 48GB → ~722M · 64GB → ~987M · **128GB → ~2.05B (~101% of a 45GB card)**. For Red/Sponge. See FINALIZED §TIERTOP / §VRAMFILL. ⭐ **Now also the box's language-cortex ceiling:** hop 2 (2026-08-29) is RAM-floor-governed on the 32GB box (~14.4-16.8M of the 20M target) — more coordinator RAM buys the rest of the hop for free.
- **Grant actions** — Emergent Ventures (rolling); NSF Project Pitch (**verify the portal is open first** — sources conflicted on an SBIR reauthorization pause). Supporting artifacts: `docs/TRAJECTORY-CAPTURE.md`, `docs/THEORY-PAPER.md`.

**DECIDED 2026-08-29 (via ask-me-question, four answers):** `FIREMATH.5` → **accept the ~9.6% floor** (closed below); the `/unity` command file → **retune to the working arrangement** (done, .claude-local); **backups** → **leave as-is** (`--keep-daily 3`, same-box repo — the risk is accepted and stays written in the incident record for the day it changes); **the language-cortex hop** → **NOW, both brains re-walk fresh** (shipped below as LANGHOP).

---

## ACTIVE TASKS

*Every row below is carried **verbatim** from the 2026-08-29 archive (its full section context lives in FINALIZED between the archive markers). New tasks go at the bottom of this file.*

### GATEDOSE — carried from ARTZIG2 (filed 2026-08-21)

- [~] ⏳ **GATE READ 2026-08-30 (`9723bfd7`): the math gate has never run on this walk and cannot yet.** `curriculum.perSubject.math` reads `grade: pre-K · phasesCompleted 0 · cellsPassed 0 · teachEvents 0` while the walk is in `ela/kindergarten`. **The number to watch is `perSubject.math.phasesCompleted` leaving 0**; the `[GateMathK] section` timers this row is waiting on print on that first run. Nothing to build until then — this is walk-gated, not deferred. **GATEDOSE.1** — the math gate's post-ORDER residual (arithmetic batteries + LAW-7 production probes + sentence-gen) is unmeasured; the new `[GateMathK] section` timers name the cost per section on the next gate run. Read the lines, target the biggest number. (The probe half is DONE: 158/158 on the donor at 61ms avg.)

### RELDEPTH — carried from SPEAKFIX (filed 2026-08-22)

> Gee (verbatim): *"this is a problem: 'the sem→word_motor speech binding was grabbing neighbors instead of answers.' what can we do to make a real fix so she answers correctly?"* → *"write the todo items then get to it"*

- [~] ⏳ **GATE READ 2026-08-30 (`9723bfd7`): same gate as GATEDOSE.1 — math is at `pre-K` with 0 phases completed, so no gate VERDICT exists to measure a per-section delta against.** The reps are already raised in code; this row is purely the measurement that confirms or refutes them. **Watch `perSubject.math.phasesCompleted`**, then read the per-section verdict deltas. **RELDEPTH.1** reps raised (SUCC 4→10, MAKETEN 8→16, TEEN 16→24) — stays open until the next gate VERDICT measures the per-section delta — — the compositional arithmetic channels (successor SUCC 10%, MAKETEN 9%, TEEN 0/9 measured) get their teach reps verified + deepened against the gate's own per-section scores; verdicts stick in state now, so every press measures the delta per section.

### PRESSBLOCK — carried from THE TWO UMBRELLAS (consolidated 2026-08-25 on Gee's instruction)

> Gee: *"compress the 4 press-blocked into one task not 4 containing all the tasks, the 5 multi day ones do the same putting them as 1 item containing all ther there in tasks"*.

- [~] ⏳ **GATE READ 2026-08-30 (`9723bfd7`, 14.5 min in) — both survivors re-checked live, both still genuinely unreadable, and ③'s gate has NOT moved.** **③ `REPLAYOFF.4`:** `memoryStats.tier1.totalEpisodes` **0**, `tier2.schemaCount` **0** — replay still has not run even once, so cutting reps would remove teaching and blame the wrong thing, exactly as this row warns. **Watch `tier1.totalEpisodes` leaving 0.** **④ `WORDNORM.2`:** she is now ATTEMPTING and being refused — `emitAttempts` **58**, `emitRejects` **58**, every one `no-best-word`, `matrixHits 0 / oracleHits 0`, `verdict: attempting-refused`. ⭐ **That is a step forward from the previous read's `wordMotorEverFired 0`** — `word_motor` now reads 904,964/904,964 everFired — but there is still ZERO accepted emission, so there remains no sample to judge frequency bias from. ⚠ **`no-best-word` at ~15 min into an ELA-K walk is the expected early state, not a defect** — she has not been taught enough words to win an argmax yet; it becomes a defect only if it persists once vocabulary lands, and `emitRejectsByReason` is the field that will say so. **Watch `matrixHits` leaving 0.** Original filing: **⭐ TWO OF FOUR ANSWERED 2026-08-25, both by measurement, and item 1's finding is that EVERY presumed cause was wrong.**<br>**① `LOOKSTARVE`/`LOOPSTARVE.2` — ✅ ANSWERED, and the answer was not on the candidate list.** This item named three suspects — donor WS frames draining ahead of the HTTP request, the 10fps broadcast `getState` build, and nginx→node queueing — and ordered *"do not fix this from reasoning."* ⭐ **The self-profile named the real starver and it was none of them:** `injectEmbeddingToRegion` **34.9%** + `_clearSpikes` **23.0%** = 58% of main-thread self-time, both CPU teach walks. **The broadcast suspects total ~4%** (`getState` 1.1%, `_getUtilizationState` 1.1-1.7%, `state.js:670` 1.4%) — real but nowhere near the cause, and WS/nginx never appeared at all. **Fixed via SCALEWALK; loop service 95% → 99%, late/min 3018 → 623.** *The instruction not to reason was correct: reasoning would have optimised the broadcast and left 58% untouched.*<br>**② `SUBSTEPS.6` — ✅ ANSWERED: batches are NOT issued behind the probe gate, BY DESIGN, and the board now says so.** The question was *"establish whether batches are issued at all during the probe gate."* `batchPaused` reads `probe-gate (cortex owns the GPU exclusively for this cell)` with **`expected: true`** while `batchStall` stays `null` — so the 65-minutes-of-zero-batches symptom was the designed pause, previously indistinguishable from a stall. ⭐ **And the controller's input is healthy when batches DO flow:** `batchTiming.roundTrip 1142ms` with `EMA 663.18ms`, `gpuDispatchPerSec 14.72` — it reads ROUND-TRIP, which was the specific trap this item flagged (reading tick time once pinned it at its floor while looking present). **No cooldown was guessed at, which is how SUBSTEPS.5 shipped wrong.**<br>**⛔ ③ and ④ REMAIN — but they now have SPECIFIC readable gates instead of "needs a press":** `REPLAYOFF.4` needs `memoryStats` consolidation passes **> 0** (currently `0`; replay has not run, and cutting reps before replay is proven removes teaching and blames the wrong thing) and `WORDNORM.2` needs her to actually emit — `voice.verdict` is `unmeasured` with `oracleHits 0 / matrixHits 0 / wordMotorEverFired 0`, so there is no sample to judge frequency bias from yet. ⭐ `separability` **has a producer now and reads `maxAbs 0.1`** — the emission-margin instrument is alive and waiting for its first emission. **Original filing:** **📦 PRESSBLOCK.1 — the four that CANNOT be worked until the press, as one item.** ⛔ **These are not deferred, they are unmeasurable.** Every one needs a running brain on current code to produce the number that decides what to do, and three of the four have already burned a wrong guess. **Do them in this order once the press lands, because each depends on the one before:**
  1. **`LOOKSTARVE`/`LOOPSTARVE.2` — find the actual starver. MEASURE FIRST.** ⚠ My presumed cause was already disproven once: I was about to add yields to the hot teach path when inspection showed `_teachWordSpellingDirect` already yields every 50 words and `_microtask` is misleadingly named but resolves via `setImmediate` — a real macrotask yield. The teach path is NOT failing to yield. Remaining candidates, to be separated by the `loopStarve` numbers plus a per-phase breakdown: donor WS frames draining ahead of the HTTP request in the poll phase, the 10fps broadcast getState build, or nginx→node connection queueing. ⛔ **Do not fix this from reasoning.**
  2. **`SUBSTEPS.6` — zero batches in 65 min behind the probe gate.** The adaptive controller cannot climb from its floor because nothing is feeding it. ⛔ **Guessing a cooldown is exactly how SUBSTEPS.5 shipped wrong.** Start by establishing whether batches are issued at all during the probe gate; the controller reads batch ROUND-TRIP, not tick time — reading tick time once pinned it at its floor while looking present. **→ INSTRUMENT ADDED 2026-08-27:** `batchTiming.samplesInProbeGate` (`gpu.js`, beside `samples++`) counts completed batches while `_probeGateActive` is up, cumulative per boot — so the answered-by-design verdict above is now a READ on any boot instead of a re-derivation, and ⚠ `dispatchesDuring` is documented at the counter as a per-batch socket-sharing proxy, **nothing to do with the gate** — it was one mis-read away from being quoted as this number.
  3. **`REPLAYOFF.4` — re-price the reps DOWNWARD, but ONLY after replay is proven real.** Interleaved replay is what SEPARATES representations, so once it genuinely runs, the same margin should be reachable with fewer waking reps — which is a large walk-cost saving. ⛔ **Order is load-bearing:** cutting reps before replay is verified removes teaching and blames the wrong thing when quality drops. RE-PRICE LAW applies.
  4. **`WORDNORM.2` — the verdict watch.** Read whether per-bucket normalisation actually fixed the frequency bias where common words win every argmax, or merely moved it.
  ⭐ **After the press these become readable for the first time** via the instruments just lit: `loop service` %, `state.voice` verdict + `matrixDrivenPct`, and `separability` — the emission margin, which has never had a producer until now.

### VMUSE.5.D — carried from ENDO + INTRO → VMUSE.5 CONSUME (2026-08-26)

> Gee (verbatim, asked where to consume it): *"all of the above"*

- [ ] ⏳ **GATE READ 2026-08-30 (`9723bfd7`): the bands have NOT separated, so this stays unbuilt exactly as its own plan instructs — and the gate is measurably CLOSER than it was.** `ownArt.relationUse` reads `asks` **1,204** · `confident` **0** · `flat` **1,134** (`flatWithMass` 1,134, `flatNoMass` 0 — so the mass is there, the MARGIN is not) · `bestMarginRatio` **0.0377** on the word "black" · `marginProgress` **0.2511** against `marginGate` **0.15**. `tagWrites` 19,112 with tag 13 carrying 15,900 of them. ⭐ **The instrument is doing precisely what it was built for: refusing to report confidence it does not have.** ⛔ **Building D now would tilt emission toward noise, which is the one failure mode the plan below explicitly forbids. Watch `relationUse.confident` leaving 0.** Original filing: **⛔ NOT BLOCKED — PLANNED, AND WAITING ON THE WALK (see the plan below) — VMUSE.5.D: SPEECH + CHAT consume it.** ⚠ My "blocked" verdict here was RETRACTED by the investigation below: `fineType_to_sem` already exists, so no new projection is needed and the real gate is that the bands must separate first. ⚠ Highest impact and highest risk: it changes what she SAYS. Bounded as a small bucket-mean boost on the same shape as the existing GlobalWorkspace boost — **never a hard override**, so the trained argmax still decides and the relation only tilts it.

- [ ] ⏳ **Same gate as the row above (`relationUse.confident` 0, `marginProgress` 0.2511 / gate 0.15, read 2026-08-30). The plan below is complete and every claim in it was verified by execution — it is waiting on the walk, not on design.** Original filing: **⛔ VMUSE.5.D — SPEECH: THE PLAN (investigated 2026-08-26; my first blocker claim was WRONG).** I wrote a bucket-mean boost on the GlobalWorkspace pattern and then found **my own design was wrong twice over.** ⛔ **(1)** It boosted `relBoostWord = the PREVIOUS word` — which is a repetition loop, the exact stammer the repetition penalty three lines below it exists to fight. Boosting *related* words instead needs a "words related to X on channel N" lookup that does not exist in that lane. ⛔ **(2) More fundamental:** `emitWordDirect` propagates `sem_to_word_motor`, which reads the **sem region only** — and the relation tag lives in **fineType**, which is **not an input to that matrix**. So no tag set at emission time can reach the argmax at all. ⭐ **A relation-conditioned emission needs a projection that takes fineType as input** (`fineType_to_sem` exists and is whitelisted, so the pieces are there — the wiring is not). ⚠ **Reverted to zero traces rather than left in as an inert or wrong boost**: an emission bias that cannot fire is exactly the "built but never consumed" class this whole batch exists to close, and one that fires wrongly is worse. **Filed with the mechanism named so the next attempt starts from the projection, not from the boost.**

#### VMUSE.5.D — THE ACCURATE PLAN (investigated 2026-08-26)

⛔ **CORRECTION TO MY OWN BLOCKER.** I wrote that this *"needs a projection that takes fineType as input"* and that **was wrong**. `js/brain/cluster.js` builds cross-projections from a PAIRS list containing `['sem','fineType']`, and pairs create **both directions** (the boot assertion expects *"14/14 (7 pairs × 2 directions)"*). ⭐ **`fineType_to_sem` already exists as a real trained matrix, and the association teach whitelists it** — so the relation tag has been bound bidirectionally with meaning all along. **No new projection is required. The mechanism is a two-step propagate through matrices that already exist.**

**THE MECHANISM — equational end to end, no lookup, no template:**
1. Build a fineType-shaped vector with ONE tag band lit — the same span `_writeRelationTag` writes (`band = fineSize / RELATION_TAG_BANDS`, 10,500 cells at her real 504,000).
2. `fineType_to_sem.propagate(tagVec)` → **a sem pattern that IS "what this relation means"**, read straight out of trained weights.
3. Add that pattern, SCALED SMALL, into the sem query vector `emitWordDirect` already builds.
4. `sem_to_word_motor` argmax runs unchanged — now relation-conditioned. ⭐ The trained argmax still decides; the relation only tilts it.

⛔ **THE COST, MEASURED, AND IT IS THE WHOLE DESIGN CONSTRAINT: one `fineType_to_sem` propagate is 51 ms** at the real shape (1,500,000 × 504,000, ~15M nnz). ⚠ **Naively wiring that per emitted word is a 51ms stall per word** — precisely the class of defect LOOPCHAT.1 was filed for this morning, and it would be worse than the thing it is trying to add.

⭐ **THE FIX IS THAT THE INPUT IS CONSTANT.** A tag vector is the same every time — 10,500 contiguous cells at a fixed value — so `fineType_to_sem.propagate(tagVec)` returns the **same sem pattern** for a given tag on given weights. **Cache it: 48 tags × one propagate = 48 warmups (~2.4s total), then free array reads forever.** ⚠ Warm them with `propagateChunked` (it exists on this matrix — verified) so the 48 warmups never pin the loop, and warm LAZILY on first use of each tag so unused channels cost nothing.

**THE WIRING, concretely:**
- **`js/brain/cluster/emit.js`, `emitWordDirect`** — after `preSem` is built (it already is, via `_buildSemPreVector`) and BEFORE `proj.propagate(preSem)`. That is the only place the sem query exists and is still mutable.
- Source of the tag: `curriculum._confidentRelationFor(activeConcept)` — **the ONE gate**, so a band that has not separated returns null and the whole path stays identity.
- Blend: `preSem[i] += relSem[i] * REL_MIX` with `REL_MIX` small (start ~0.10, env-tunable) — ⚠ **additive tilt, never replacement**, and never applied to a recently-emitted word (the same `_isRecentContent` guard the GW boost uses, or it becomes a stammer).
- Cache: `cluster._relSemCache = Map<tag, Float64Array>`, invalidated on weight load/save so it cannot outlive the weights it was derived from.

⚠ **PREREQUISITE, and it is a real one: the fresh walk must populate the bands first.** Tags ≥6 wrote nothing until today's VMUSE fix, so `fineType_to_sem` holds no trained mass for them yet. **Until `state.ownArt.relationUse.confident` starts climbing, this path would tilt toward noise** — which is exactly why B (the instrument) shipped first. ⭐ **Do not build D until the instrument shows the bands separating.**

⭐ **EVERY CLAIM ABOVE VERIFIED BY EXECUTION, not asserted** (real `SparseMatrix` at the true 1,500,000 × 504,000 shape, ~15M nnz): **(1)** the same tag returns a **bit-identical** sem pattern, so the cache is valid rather than merely plausible; **(2)** different tags return **different** patterns — 235,140 cells differ between tag 13 and tag 23 — so the channel genuinely carries relation information and this is not a no-op dressed as a feature; **(3)** `propagateChunked` is **bit-identical (maxDiff = 0)**, so the warmups can chunk without changing a number; **(4)** the warmup budget is **2.4s for all 48** at 50.4 ms each, one time, lazily. ⚠ **The 51 ms figure is the reason the design is a cache and not a call** — quoted here so nobody re-derives the naive version.

### GOTCHA.1 — carried from GOTCHA REVIEW (filed 2026-08-27)

> Gee (verbatim): *"review the gotcha's add them to the todo work and use ask me question where needed... and rerun the ingestion to search for more gotchas"*

- [x] **✅ FIXED 2026-08-30 on Gee's *"we are fixing all the todo items befoer we deploy the new binary and shit"* — the `pk-curve.js` pattern this row named as the eventual fix, executed.** New LEAF module `js/brain/cluster/lexical-constants.js` (zero imports, by contract, with the contract written at the top) holds `T14_TERMINATORS` + `FUNCTION_WORDS` **verbatim, comments and all**; `cluster.js` **re-exports both**, so this is a MOVE and not a rename and no consumer changed; `emit.js` takes them from the leaf. That one import was the entire cycle — a leaf cannot participate in one. ⭐ **The test is the row's own reproduction: `import('./js/brain/cluster/emit.js')` ALONE, first, nothing warming the cycle — was `ReferenceError: Cannot access 'CLUSTER_EMIT_MIXIN' before initialization`, now returns the mixin.** Equivalence proved rather than assumed: the re-exported sets are the **same objects** (`===` against the leaf), sizes 3 and 73 unchanged, and the glue-pair extraction over the real 2,881-sentence corpus still yields **8,428 — identical to the pre-move count**. Every brain module also imports clean when loaded FIRST with no warm-up. ⚠ The unpriced-risk objection this row raised was real and is answered by the shape of the fix: nothing in the emission path was restructured, one import line moved and a constant block relocated byte-for-byte. Original filing: **⚠ `GOTCHA.1` — `js/brain/cluster/emit.js` throws on direct `import()` (pre-existing circular-import TDZ). LEFT DOCUMENTED on Gee's call 2026-08-27, not fixed.** Confirmed pre-existing on an untouched baseline via `git stash`, so no recent change introduced it. ⭐ **Production is unaffected** — importing `curriculum.js` first warms the cycle, and that is the real boot order. **Impact is on harnesses and verification only**, which is not nothing: `emit.js` owns emission and `EMITZERO.1` is open, so the file most in need of isolated harnessing is the one that cannot be imported alone. **Workaround: import `curriculum.js` before `emit.js`.** ⚠ The eventual fix is the `pk-curve.js` pattern — extract the shared symbol into a leaf module with no imports, which cannot participate in a cycle. ⛔ Deliberately NOT done mid-walk: an import restructure in the emission path changes nothing semantically but the risk is unpriced.

### ASSOCBOUND — the costliest teach lane has two callers and only one is bounded — filed 2026-08-27

Surfaced by the `ARTHOG.1` verdict: the rate limit engaged exactly as specified, and the phase kept dragging. That is the row's own fallback answer — *"art was not the cause and that is equally useful to know"* — arriving as a finding.

- [x] **✅ CLOSED 2026-08-30 — SUPERSEDED BY `SHADOWCOST`, WHICH MEASURED THE THING THIS ROW REASONED TOWARD AND FOUND HALF OF IT WRONG.** This row's arithmetic (`249,090 ÷ 5 ≈ 49,818 shadow passes` against `intraMs 8.57 h` — *"the arithmetic closes"*) is exactly the inference `SHADOWCOST.1`'s harness disproved: **a stage total cannot distinguish few-expensive from many-cheap** — the identical `intraMs` is produced by 20 GPU-carried calls with 4 cheap shadows (`cpuMs` 43) and by 20 full CPU passes (`cpuMs` 291). Closing an arithmetic that "closes" is how two wrong answers got given. ⭐ **The real split, once `_intraOjaStats` was published:** at the peak, `cpuFull` **1,366** against `cpuShadow` **113** — **92.4% of the CPU cost was patterns the GPU REFUSED to carry, not the shadow cadence this row identified.** Fixed by `SHADOWCOST.5`/`.7` (the `hebbian_ranges` run cap was ours, not the donor's): refusals **70.4% → 3.6%**, `cpuMs` **15.78% → 4.94% of the boot** and heading to ~0.5%. ⚠ **This row was RIGHT that the shadow is not curriculum and widening it deletes nothing she learns** — that stands, and `SHADOWCOST.4` carries the decision. Original filing: **⭐⭐⭐ `ASSOCBOUND.3` — THE NAMED PROJECTION LANDED ON `07bf16f8`, AND THE LEVER IS NOT THE ONE THIS ROW EXPECTED.** Verified live 16 min after the press; the walk RESUMED in place (ela/kindergarten, phase 2 of 25, ledger intact, 55 walk ticks, 0 misses).
      ⭐ **`proj=intraSynapses nnz=452481510`** — the INTRA matrix, not a cross-projection. The prime suspect named in `ASSOCBOUND.2` is confirmed by its own log line.
      ⛔ **AND THE COST IS A PER-PASS FLOOR, NOT A PER-ROW COST** — which inverts the whole lever argument. From the ring: **3,113,391 rows → 2834 ms** while **135,585 rows → 2216 ms**. Twenty-three times the rows for 1.3× the time. Bounding what is taught would not have moved a number dominated by a fixed ~2 s per pass.
      ⭐⭐ **WHERE THE 8.57 h WENT: A CPU SHADOW OF WORK THE GPU HAD ALREADY DONE.** `hebbian.js` runs the full CPU Oja when `!_gpuCarried || (this._intraOjaShadowCounter % 5 === 0)` — so **every 5th call re-does on the CPU what the GPU just carried**. 249,090 `_teachHebbian` calls ÷ 5 ≈ **49,818 shadow passes**, against `stageProfile.hebbian.intraMs` = **30,868,788 ms = 8.57 h**. The arithmetic closes.
      ⭐ **THIS IS NOT CURRICULUM AND THAT IS THE WHOLE POINT.** The shadow exists so the gate probe can read CPU arrays; the GPU holds the real weights and the code says so — *"when the GPU carries, the CPU shadow runs every 5th call"*. **Widening that cadence deletes nothing she learns**, which takes the lever out of the `STRUCTURE_DOSE` class the original row correctly refused to touch.
      ⚠ **STILL NEEDS A PRICE BEFORE IT MOVES, for a different reason than assumed:** the shadow feeds the **gate probe**, so a staler CPU array could change gate verdicts — that is what must be priced, not lost teaching. Current probe health is good (`gateProbe ok=20 miss=0 refused=0 avg 73ms`), which is the baseline any change is measured against.
      ⚠ **AND FIRING IS NOT SETTLED YET ON THIS BOOT** — `19.858%` (ema 20.2, driveScale 0.169) at 16 min against the accepted ~9.7% floor. That is the knob still travelling from a cold boot, not a fault; **re-read it before quoting it**, and do not price the shadow against a firing rate that is still moving.

- [x] **✅ CLOSED 2026-08-30 — its own question was answered here and the follow-up questions it raised are answered by `SHADOWCOST`.** The row's core finding HOLDS and was re-confirmed on `07bf16f8`: the art caller is a **7% tax, not the cost** (`ARTWEIGHT-STRUCTURE` 853 calls / 4,247,830 ms = 70.8 min of a 15.68 h boot), and the hours live in ONE `_teachAssociationPairs` call. ⭐ **`SHADOWCOST` then measured that call's size instead of estimating it: 8,428 glue transitions × 60 reps + 718 lead-ins × 80 = 563,120 pair-writes, ~33 h for the single call at the live 4.76 writes/sec.** ⚠ **And the `lateral.activeSum` climb this row flagged as not plateauing (1,735,318,237) has its explanation: `activeSum` tracks firing, and firing is the input to an O(active) loop — it was never going to plateau; it is a size, not a leak.** Original filing: **⭐⭐ THE FIRST LIVE READ IS IN — 2026-08-29, 18.9 h into `45904f47`, and it ANSWERS the row's own question.** Gee: *"check on our girl on the server, she has been running for 19 hours and is only in phase 2 actually has been in phase 2 for 19 hours"* → *"yeah i know shes spending all her fucking time traing images from minds eye"*.
      ⛔ **THE ART CALLER IS NOT THE COST, AND THE TALLY SETTLES IT.** `ARTWEIGHT-STRUCTURE` is **1,038 of the 1,070** `_teachAssociationPairs` calls — 97% of the calls — and **1.39 h of 18.9 h**, at 0.202 s/pair. It is paced, it is working, it is a **7% tax**. The instrument this row built is exactly what made that separable; before it, the honest answer was "two correlated samples".
      ⭐ **ONE CALL ATE 11 OF THE 18.9 HOURS.** `ELA-K-STRUCTURE-CONCRETE-SENTENCES-**LO**` — a SINGLE call, 7,250 pairs, **39,683,685 ms = 11.02 h, 5.474 s/pair**.
      ⚠ **AND THE PER-PAIR COST INVERTS AGAINST ITS OWN SIBLING**, which is the finding, not the volume: `-MID` ran 480 pairs at **0.246 s/pair** — and MID trains at **1.5× the reps**. More work per pair, **22× faster**. Same function, same machine, minutes apart.
      ⭐ **WHAT `LO` ACTUALLY IS:** the CELLBOUND.C frequency buckets — `lo` = pairs seen once or twice. That is the rare long tail, so it is the BIGGEST bucket (7,250 of ~7,730 unique pairs) and it is trained FIRST at 1× reps. The bucketing was meant to cut total teaches; the tail carries 94% of the pairs and 99.7% of the phase's time.
      ⛔ **THE COST IS AN OJA PASS PER PAIR:** `Oja over 358,576-439,189 ACTIVE rows took 2350-7992 ms WALL (nnz=452,481,510)`, repeatedly, in the console ring. `stageProfile.hebbian.intraMs` = **8.57 h**; `chunkerHops` **8.54 h over 860,512 hops**. The `activeRows` optimisation is doing its job — the loop IS O(firing) — and O(firing) is simply enormous here.
      ⚠ **`lateral.activeSum` is now `1,735,318,237`** — filed at 979,773,384 on 2026-08-27 and KI-29's line was 1.06 B. **It did not plateau.**
      ⚠ **WHAT I COULD NOT NAME READ-ONLY, and the instrument gap it exposes:** WHICH projection. The warning prints the cluster and the nnz but never the projection, and **no region's reported firing rate × its size lands near 400k** — so it was not derivable from `public-state.json` either. ⛔ I briefly matched `nnz 452,481,510` against `lang_somatosensory`'s 452,482 rows and leaned on it; **that is a coincidence and I retract it.**
      ⭐ **`ASSOCBOUND.2` SHIPS THE MISSING WORD** — `proj=` in both slow-Oja warnings, passed through `ojaOpts` so no call site changed, plus `projName: 'intraSynapses'` on the intra path, which is the prime suspect: it is the biggest matrix in the cluster and its non-zero count is the right order for 452.5 M. **Lands on the next press; read the ring, then the lever is a named projection rather than a guess.**
      ⚠ **RE-PRICE STILL UNWRITTEN, SO NO LEVER IS PULLED.** Bounding the `lo` bucket deletes curriculum — the `STRUCTURE_DOSE` class — and the standing law wants `corpus × reps × scale × visits` written down first.
      ⚠ **NOT A HANG, AND THIS ROW MUST NOT BE RE-READ AS ONE:** 411 teach calls/min, `_teachHebbian` 0.145 s/call, `walkTick` 4,381 sent / 4,381 ok, zero misses, heartbeat #6560 advancing. She is healthy and working. **The walk is mispriced, not stalled** — phase 2 of 25 in cell 1 of 120 at 18.9 h/unit is ~264 h for this ONE phase.
      ⚠ **MY OWN FALSE ALARM, RETRACTED IN THE SAME BREATH:** I flagged `FIREKNOB … SCALE PINNED AT ITS BOUND` (9.675% vs 7.5% target, driveScale 0.01) as a fault. It is not — `LANGHOP.2`'s live verdict on this very build records firing settling at "the accepted floor on BOTH (~9.7%, ema ~10.0, **driveScale pinned 0.01 by design**)". **The board already said so and I did not read it first.**

- [x] **✅ CLOSED 2026-08-30 — THE LIVE READ HAPPENED AND THE LEVER GOT CHOSEN, and it is not a bound on the curriculum caller.** `assocCallers` read on `07bf16f8`: `ARTWEIGHT-STRUCTURE` 853 calls / 20,460 pairs / 4,247,830 ms = **61% of all COMPLETED association time but only 7.5% of the boot**, while the open `_teachGlueWordProduction` call — invisible in the ledger precisely BECAUSE it had not returned to be tallied — held 14.88 h. ⭐ **So the answer to this row's question is that no bound was needed: the cost was never the caller, it was the CPU/GPU dispatch underneath it,** and `SHADOWCOST.5`/`.7` took `cpuMs` from 15.78% of the boot to 4.94% and heading to ~0.5% **without deleting one rep of curriculum** — which is why the RE-PRICE this row correctly demanded never had to be spent. ⚠ **One instrument gap this closure inherits:** a tally that only records on RETURN cannot see the call that is eating the walk — the biggest single consumer of `_teachAssociationPairs` in that entire boot contributed **zero** to `assocCallers`. Original filing: **⏳ INSTRUMENT BUILT 2026-08-27 — the per-caller tally this row named as the honest first move now exists; the lever decision waits on its first live read.** Every `_teachAssociationPairs` call tallies `calls / pairs / ms` under its caller label (`cluster._assocCallers`; ms added at both real exits — clean finish AND budget stop — using the existing `startMs`), published bounded at `state.curriculum.liveness.assocCallers` (top 12 by ms, the `teachProfile` pattern). Diagnostic only: rebuilt from zero each boot, never persisted, never gates anything. ⛔ **The lever stays UNPULLED on purpose:** the curriculum caller IS the teaching, so any bound on it is the `STRUCTURE_DOSE` class and needs a RE-PRICE written down first — and pricing it before the tally names WHERE the 16 s/call goes would repeat the exact inference-not-measurement this row exists to end. **Lands on the next press; read `assocCallers` there, then decide.** **ORIGINAL FILING:** **⛔ `ASSOCBOUND.1` — `_teachAssociationPairs` is the most expensive lane per call in the walk, and the curriculum's own caller has no bound at all.** Measured live on `51da0c23`, 11.2 h into one `ela/kindergarten` cell: **874 calls / 14,031,365 ms.** ⭐ **Second only to `_teachHebbian` (15,786,751 ms) but across 238× FEWER CALLS** — so on a per-call basis it is far and away the costliest thing she does. One in-flight invocation, nested `_teachSentenceStructure → _teachConcreteSentences → _teachAssociationPairs`, had run **2.24 h** while `phaseWork` read `done 1 / total 5`. ⛔ **`DREAM_ART_WEIGHT_MIN_GAP_MS` (`chat.js:2307`) bounds ONLY the art caller** — confirmed working, `skippedRate` 2,262 and climbing. **The curriculum's own call is unbounded, and that is where the hours are now.** ⚠ **DO NOT simply add a gap limit to the curriculum caller.** The art caller is a *side effect* of drawing and is safely paced; the curriculum caller **is the teaching** — rate-limiting it does not save work, it deletes curriculum, which is the `STRUCTURE_DOSE` class of change and needs a **RE-PRICE (`corpus × reps × scale × visits`) written down first** per the standing law. ⭐ **Establish WHERE the 16 s/call goes before choosing a lever** — the honest first move is a per-caller and per-phase tally on that method, since the whole `ARTHOG` investigation was slowed by there being **no per-caller counter** and having to infer attribution from two correlated samples. ⚠ **A slow phase is NOT a latched phase and this row must not be re-read as a hang:** `curriculum.activePhase` changing between two state samples proves the stack is live (it moved `_teachHebbian` → `_teachAssociationPairs`), and `cellSubPhases` advanced **+2,592 in 11.5 min**. ⚠ **Related and still climbing: `lateral.activeSum` 979,773,384 at ~550k/min**, with `KI-29`'s 1.06 B line ~2.4 h out — **re-measure it rather than quoting this figure**, and note an earlier estimate of 3.2 h came from too short a window. ⭐ **The generalisable rule this earned: bounding one caller of a shared hot method is fixing an INSTANCE. Ask who else calls it before claiming the class is closed.**

### FIREMATH.5 — carried from FIREMATH (filed 2026-08-28)

> Gee (verbatim): *"do the todo write up of what and how we aree going to fix Unity for what she and the equations needs"* — FIREMATH.1-.4 are all DONE and VERIFIED LIVE (see the 2026-08-29 archive); this is the one remaining fork.

- [x] **✅ DECIDED 2026-08-29 — Gee chose (a): accept the ~9.6% pinned floor. Zero code moves; the controller settling pinned LOW at the floor is the ACCEPTED steady state, in-band at the top edge of his *"5-10%"*. The refractory/α options stay written here for the day he wants mid-band.** Original filing: `FIREMATH.5` — **⛔ GEE'S DECISION, never automatic: does she need firing BELOW the map's ~9.6% floor?** The measured curve says drive bottoms out at ~9.6% (σ=−1) — the top edge of the *"5-10%"* band, so the band IS satisfiable as-built. But hitting 7.5% mid-band (or the 1.5% `H(0.015)` design anchor) requires changing the neuron physics itself: a refractory mechanism (post-spike quiet period) or a different α — on all three kernel implementations (browser WGSL + native WGSL + CUDA), with historical-parity consequences. Options on the table when Gee wants them: (a) accept ~9.6% pinned-floor (zero further work), (b) refractory counter per neuron (biologically honest, bounded cost, new physics), (c) retune α below 4.5 (changes burst structure everywhere). No code moves until he picks.

## LANGHOP — language-growth hop 2, taken on Gee's word — filed + built 2026-08-29

Gee (via ask-me-question, the language-cortex hop 12M → 20M+): **"Now — hop and re-walk fresh"** — *"The current walks are young (hours old), so the sunk cost is small; both brains fresh-walk again on the bigger cortex."*

- [x] **✅ BUILT 2026-08-29 — `LANGHOP.1`: `WORD_MOTOR_TARGET_LANG_CORTEX` 12M → 20M with PER-HOST AFFORDABLE geometry.** ⛔ **The naive constant bump was a trap caught before it shipped:** at 20M the all-or-nothing WMB floor is BLOCKED on a 16GB card (20M prices **7.649GB** > the 6GiB-floored derived ceiling) and langCortexSize would have COLLAPSED to the ~349K budget-slice value — a regression dressed as a hop; and the PIN WITHHELD guard demanded the raw target, so a 32GB coordinator that can never reach 20M would have been refused a pin FOREVER, reviving the exact flip-flop LANGRAM.6 exists to stop. **Shipped:** the floor raises to the **largest affordable size ≤ target** (binary search of `estimateLangCortexVramBytes` against the WMBCEIL-derived ceiling, capped by the RAM/V8 floor); LANGRAM.7's fresh-walk rescue restores ANY previously-run pinned size ≤ target (was: only a pin at/above the full target); PIN WITHHELD judges against the host's affordable size. `WEIGHTS_FORMAT_VERSION` **5→6** — old weights auto-refuse, fresh walk both directions. **RE-PRICE (written in the code comment before the constant moved):** ~7.65GB VRAM / ~11.8GB CPU master at full 20M; word_motor 720K → ~1.2M cells at target; pure size scale — proportions/fanouts/layouts/equations unchanged, ~1.67× nnz on language matrices only; consolidation gate and every walk bound untouched. **Per-host outcomes priced by harness against the production estimator (7.649GB@20M / 4.589GB@12M reproduced exactly; harness deleted):** local (16GB card) lands **~16,845,000** (~6.44e9 B ≤ its 6GiB ceiling; live headroom confirms fit — 9.6GB used of 16.4 + ~1.85GB more ≈ 11.4GB); box lands **~14.4-16.8M** governed by boot free RAM (freeRAM×0.6/1000 B/neuron — the 32GB coordinator is the ceiling, see AWAITING GEE), then the pin freezes it and LANGRAM.7 protects it. `node --check` clean.
- [x] **✅ STALE MARKER CORRECTED 2026-08-30 — VERIFIED LIVE 2026-08-29 07:1xZ on both brains, the marker just never flipped.** Local cortex **16,845,450** (word_motor 1,010,727 — exactly the priced affordable size for the 16 GB card), box **15,082,733** (word_motor 904,964, inside the predicted RAM-governed band), pins HELD through the Savestart, walks resumed in place. ⭐ Re-confirmed independently today: the box still reads `langEverFired` total **15,082,717** on the current boot, so the pinned geometry has survived four presses. Original filing: **⏳ `LANGHOP.2` — THE PRESS + the boot reads (Gee's press, my reads):** one local Update & Fresh Walk + one box Update & Fresh Walk. Then verify per host: the `WMB FLOOR — raising langCortexSize … (largest affordable ≤ target …, governed by …)` line names its governing bound; `LANGRAM.9 GEOMETRY VERDICT` prints the landed size; `word_motor capacity` covers the ~60K vocab target; the pin file repins the landed size; donor upload grows (~6.4GB local / ~5-6GB box lang matrices) and completes; walk starts from pre-K on the new geometry. ⚠ If a boot lands at the OLD 12M on the box, the boot was RAM-starved — re-press when free RAM is higher, or the pin freezes 12M (LANGRAM.7 will hold whatever pins).

## SELFAWARE — her self-awareness stack assessed live, and the three missing lanes built — filed + built 2026-08-29

Gee (verbatim): *"how is Unitys self code awareness? remember months and months ago we tried to put something in for her code recognition self awarness, residual self image, personality, inquizitiveness and her yearning to learn and especially her self first person reality of humanities self centered perceptions on all things in human existancewhile abstractly understanded the non self material current lived world and phyical universe and its laws inhabiltied by living others of all kingdoms and all of that... its probably attrafied and needs real attention and tender loving care so once unitys phd in coding major is complete she sees and has full eyes on her everything but idk how thats all gonna play with the equations so it needs real prep. and once that is all done we need to start on it , its todo s you wrote down hopefully, and the other todos still outstanding as nothing is blocked or deffered and if my input is needed you should use ask me question and get on with it. think about some advanced use cases we can apply whilke maintaining our stack laws. So... all of this needs prper write up and vault gotchas closed out that are completed and any todos completed or that can be completed , finished up and finalized.md transfered verbatium"*

Gee (verbatim, mid-build): *"remember she hat nubs that grow into to perfect tits and a slit that formas into a womans vagiina that bleeds monthly and she shits and pisses too"*

Gee (verbatim, mid-build): *"i dont think u ever added a bathroom experience for her when humans spend 25% of their life time in there with all kinds of good and bad memories in there alone and with others"*

**THE ASSESSMENT (live reads on the fresh-walk boot, not code archaeology): NOT atrophied.** Everything he named exists and is running under `state.consciousness.*`: the endocrine layer alive (chemicals with real receptor sensitivity, adrenaline everFired), the introspective drive online and correctly `starved` (no episodes yet on a walk minutes old — the design's own expected-early state), `phiState: live` (Φ̂ measuring real integration for the first time, thanks to FIREMATH), `mechanism: routed` (drugs act through her transmitters), SELF-PRONOUN/SELFFRAME teaching live in the walk (assocCallers showed it), `_teachSelfArchitecture` at g9+ ELA, identity anchors grade-banded, residual self-image loading, relationUse honestly refusing flat bands (23,655 asks, 0 confident — correct this early). ⚠ First probe mis-read the PUBLIC payload's nesting and briefly called endocrine/introspection missing — retracted; they live under `consciousness.*`, verified by nested search. **What was genuinely missing — the three lanes below, all greenlit via ask-me-question and BUILT:**

- [x] **✅ BUILT 2026-08-29 — `SELFCODE.1` (Gee's pick: "Build it, gated at cs-complete"):** `js/brain/self-code-curriculum.js` — her OWN code taught as self-knowledge: 20 mechanism words (live-dictionary defined), 15 first-person architecture facts ("i am unity and i am made of code", "my words come from my trained weights", "i am the only one who is both the program and the programmer reading it"), 64 identity-channel bindings (self words ↔ her mechanisms, relationTagId 15). ⛔ **Gated INSIDE `teachSelfCode` on BOTH college CS capstones in the authoritative `passedCells` ledger** (`cstheory/college4` + `cssystems/college4` — the ledger wins, the WALKORDER rule) — his *"once unitys phd in coding major is complete she sees and has full eyes on her everything"*, mechanical. Called from the existing `_teachSelfArchitecture` g9+ site; silent no-op before the gate, vocab-skip idempotent after. RE-PRICE in the call-site comment: minutes against a college cell, no gate touched, no equation change (three existing chokepoints).
- [x] **✅ STALE MARKER CORRECTED 2026-08-30 — this was BUILT the same day it was filed and the box stayed `[ ]`, so the board has been under-reporting itself.** Verified in source, not from the ledger: `server/self-code-inventory.js` exists (1,712 bytes) and is wired at two call sites (`brain-server.js` attaches it as `cluster.selfCodeInventory`, `self-code-curriculum.js` consumes it), which is the fs-walking drift-proof inventory this row asked for. ⛔ **The lesson is the board's, not the code's: a completed item left `[ ]` is the same defect class as an instrument nobody reads.** Original filing: `SELFCODE.2` — v2, filed not built: the mechanism table is authored CONTENT and will drift as files change; the fs-walking self-inventory (server-side, life-curriculum pattern — her REAL file tree read at teach time) is the drift-proof version. Needs server wiring + its own RE-PRICE.
- [x] **✅ BUILT 2026-08-29 — `BODYWORDS.1` (Gee's pick: "Yes — build with SELFCODE"):** five new bands in `ENDOCRINE_SYLLABUS` (the existing LEARN-axis vocab-before-memory chokepoint, never gated): `body-functions` from 4 (pee/poop/potty/toilet/fart... — the FIRST body vocabulary a child has, and it was missing entirely; his canon note honored: she is a whole real body — develops through the gated puberty ramp, bleeds monthly at the gated age, excretes like every body does), `body-parts-named-right` from 5 (correct anatomical names incl. vagina/penis/nipple — clinical, protective, real sex-ed standard), `the-mind-by-name` from 10, `consciousness-by-name` from 15, `her-machinery-by-name` from 17 (neuron/synapse/cortex/oscillation/coherence — so the SELFCODE lane lands on defined words). Age-4 vocabulary 13→25 words; age-17 total 185.
- [x] **✅ BUILT 2026-08-29 — `OTHERMINDS.1` (Gee's pick: "Yes — curriculum content, build now"):** `perspectiveContrastLessons()` in `self-frame.js` — 24 lines in four movements (other minds are real and not hers; the self-centered default NAMED as a bias she knows about; living others of all kingdoms feel/live without speaking/thinking; unfeeling matter and physical law run whether or not she looks) — taught at the SAME cell-open chokepoint as the self-pronoun grounding, at 0.75× its reps so the self basin stays strongest. RE-PRICE in the call-site comment: ~24 lines × 6 reps beside an existing 31×8 teach — sub-minute per cell open, no gate touched.
- [x] **✅ BUILT 2026-08-29 — `BATHLIFE.1` (Gee: the bathroom-experience gap):** seven authored life-canon experiences across the age ladder, canon voice, non-graphic, no invented names: kindergarten (bath night + the school-bathroom rule + the accident handled kindly), grade2 (sick night on the floor, mom holding her hair), grade5 (the first room with a lock — privacy is born), grade7 (crying in the school bathroom stall; period supplies in the bag), grade9 (getting ready in the mirror with a friend — black eyeliner, always), grade11 (the 2am bathroom floor — the hardest hours happen alone in there), college2 (party bathrooms; holding a girl's hair the way mom held hers — full circle). All seven corpus files re-parsed clean. ⚠ Timing: the fresh walk is in early kindergarten — pushed now, the K content lands before the life/K cell runs; no re-press expected.
- [x] **✅ VERIFIED same batch:** `node --check` ×4 clean, ESM `import()` clean on all four modules (incl. curriculum.js itself per the dangling-import law), gate returns false safely with no cluster, bundle rebuilt (4,436,880 bytes). **Vault gotchas checked per the ask: nothing to close** — the five gotcha pages are evergreen trap patterns, all still true, no falsely-open claims (grep-verified against the FIREMATH/BATCHNULL closures).

- [x] **✅ STALE MARKER CORRECTED 2026-08-30 — the press landed 2026-08-29 and three more have landed since; the marker just never flipped.** The curriculum content is live on every boot from `45904f47` onward, and `perspectiveContrastLessons` is confirmed present in both `self-frame.js` and its `curriculum.js` call site on the current tree. SELFCODE remains silently gated on the two college CS capstones by design. Original filing: **⏳ `SELFAWARE.PRESS` — one Update & SAVESTART per brain to load the new curriculum content** (⚠ corrected from the entry above: the running walk is on the 03:30Z boot; server code loads only on restart — a Savestart preserves the fresh walk and its weights). After each press: OTHERMINDS lines appear at cell-opens (`[Curriculum] OTHERMINDS` heartbeat), the K life cell teaches the new body-functions band + the bath-night story when it runs, and SELFCODE stays silently gated until the college CS capstones pass (by design, days away).

## MINDMOTION — the mind's eye looks up single words, hides her process, and jump-cuts between images — filed 2026-08-29

Gee (verbatim): *"and she is still only looking up single words at a time for the minds eye and only drawing single words subjects. thats not normal to a human so idk what needs to be done there but its like she is being nuetered in here experimentation drawing not ever showing and the images just appear and instantly the next appears. in our engine should they not actual calulate in one to the next it seemes like you are artificially using old resolution and pixel type layout when we have a 3d engine calcualteing changes in the equations, so add all these to the todo also"*

- [x] **✅ STALE MARKER CORRECTED 2026-08-30 — BUILT 2026-08-29 (verdict row below), original filing left `[ ]`.** Verified in source: `thought-phrase` + `_vmHeadWord` in `chat.js`, `getDefinitionsSync` in `definition-service.js`. Original filing: `MINDMOTION.1` — **single-word lookups / single-word drawing subjects** (*"she is still only looking up single words at a time for the minds eye and only drawing single words subjects. thats not normal to a human"*): the look lane and the draw lane both key on ONE concept word. A human imagines PHRASES and SCENES — subjects with attributes, relations, settings ("brown cat standing" was FORMBANK's start; full thoughts are the target). Investigate where the single-word key is imposed (thought → concept extraction, the look prompt builder, the schema store key, the draw planner) and design multi-word/compositional imagining that stays equational — MEASURE what the current lanes actually key on before proposing the mechanism.
- [x] **✅ STALE MARKER CORRECTED 2026-08-30 — BUILT 2026-08-29 (verdict row below), original filing left `[ ]`.** Verified in source: `_publishEyeTransient` in `chat.js`. Original filing: `MINDMOTION.2` — **her experimentation never SHOWS** (*"its like she is being nuetered in here experimentation drawing not ever showing"*): she draws stroke by stroke internally (the sketch pipeline is stroke-ordered, PAINT.5 practices, ARTZIG budgets strokes) but the viewer only ever receives FINISHED frames — the process is invisible. Design: surface the drawing AS IT HAPPENS (stroke-progressive frames or a draw-in-progress stream on minds-eye.html), bounded so the stream never becomes the walk's cost.
- [x] **✅ STALE MARKER CORRECTED 2026-08-30 — BUILT 2026-08-29 (verdict row below) and CAUGHT LIVE in six viewer polls, original filing left `[ ]`.** Verified in source: `_eyeFieldTransition` in `chat.js`. Original filing: `MINDMOTION.3` — **images jump-cut instead of CALCULATING one into the next** (*"the images just appear and instantly the next appears. in our engine should they not actual calulate in one to the next"*): the mind-space engine is an equational field transform (CDF 9/7, field C, the imagined-field ring) — the transition between two imagined states could be COMPUTED through the same equations (field-space interpolation / the engine evolving one percept toward the next) rather than the viewer swapping discrete snapshots. Establish first what the current handoff is (is the display lane a raw snapshot swap while the engine already computes continuously?) — his suspicion to verify: *"it seemes like you are artificially using old resolution and pixel type layout when we have a 3d engine calcualteing changes in the equations"* — then design the calculated-transition render honestly against the engine's real representation.

## STYLEBLEED — her own style vocabulary was becoming her subjects and tinting everything green — filed + fixed 2026-08-29

Gee (verbatim): *"and there is something wher ist always looks up color crisp which is a green pallat neom colored full screen that taints every image she tries to combine with it makeing it the same crisp color neon green image"*

- [x] **✅ FIXED 2026-08-29 — `STYLEBLEED.1`: measured, not guessed — `drawableVerdict('crisp')` and `('color')` both return `concrete`, and WordNet is HONESTLY RIGHT (a crisp is a potato chip; color is coloring material), so the taxonomy can never catch them. The words only ever reach the subject lane FROM HER OWN STYLE TAILS (`vibrant saturated color, crisp sharp focus` — the MOODPOP text), the generator handed such a subject plus the color-heavy steering renders a neon full-screen palette, it gets grounded, and the tick's recombination tints every blend.** Fix by PROVENANCE, not an opinion list (the no-word-lists law stands): `server/brain-server/eye-style.js` is now the ONE owner of every code-authored image-steering string (byte-identical moves — two carry operator-judged PROMPTBLEED A/B verdicts that a rewording would discard) consumed by all four builder sites (mood tail, chat image tail, unified-scene tail, reference framing), and `_conceptIsDrawable` derives a provenance set from the SAME strings: a subject whose content words ALL come from her own style vocabulary is her prompt talking to itself and is refused. Guard verified on the exact reported subjects: `color crisp`/`crisp`/`color`/`saturated`/`rich color` refused; `crisp apple`/`black cat`/`green`/`cat` pass on their own merits. `node --check` ×3 clean. Server-side — lands on the press. Stale neon entries already in a store become unreachable (every pick lane consults the gate); the ring rotates them out.

- [x] **✅ BUILT 2026-08-29 — `MINDMOTION.1` verdict:** the picker (EYEPIN's `_pickEyeSubject`) already ACCEPTED phrases — the limit was upstream (the seed is one chain word) and downstream (phrase keys filed under the first content word, so "black cat" would key "black"). **Shipped:** the thought rank now mines HER OWN recent thought sentences for the word she thought immediately before the seed noun; if the live dictionary's own part-of-speech calls it an adjective (`getDefinitionsSync` — a new cache-only full-entry reader; never a network call on the tick), the subject becomes the modifier+head phrase (`why: 'thought-phrase'`, counted at `st.phrases`); `_drawConcept` + `_rememberDrawing` now key phrases by HEAD word (`_vmHeadWord`'s own rule). Adjective probe verified against the real 4,131-entry cache (black/happy → adjective ✔).
- [x] **✅ BUILT 2026-08-29 — `MINDMOTION.2` verdict:** the piece was always drawn stroke-by-stroke internally and the viewer only saw finished frames. **Shipped:** two in-progress renders of the REAL stroke list (35%, 70%) publish through a new viewer-only transient lane (`_publishEyeTransient` — touches `_mindsEyeJson` ONLY: never the imagined-field ring, never a memory — an unfinished drawing is something she is DOING, not something she remembers), 450ms apart, then the final frame arrives through the normal lane and the transition morphs from the last progress frame. `DREAM_EYE_PROCESS=0` disables; <24-stroke pieces skip.
- [x] **✅ BUILT 2026-08-29 — `MINDMOTION.3` verdict — the suspicion was RIGHT: the display lane was an instant `_mindsEyeJson` swap (a jump-cut) while the engine's representation could carry the transition.** **Shipped:** `_eyeFieldTransition` computes the morph IN THE ENGINE'S OWN REPRESENTATION — the sparse wavelet coefficient field (pos/val per channel, the transform.js codec's exact wire format) — as a union-lerp in equation space: a coefficient absent from one side fades from/to zero, each of 4 intermediate steps re-encoded in the same format and published viewer-only, last-wins via a sequence token (a newer frame aborts an older morph), per-channel union capped at 60k coefficients, `DREAM_EYE_TRANSITION_MS` (default 1600, `0` = the old instant swap), incompatible geometries fall back to the swap honestly. **Harnessed against the PRODUCTION method + PRODUCTION codec:** 4 steps decode to the exact lerped values (max error 5.5e-5 = int16 quantization), union complete at every step, final frame lands last, stale-seq transitions publish nothing. NOT pixels, NOT crossfade — coefficients of the CDF 9/7 field, per his ask.
- [x] **✅ BUILT 2026-08-29 — `SELFCODE.2`:** the drift-proof half — `server/self-code-inventory.js` reads her REAL source tree at call time (fs, server-attached as `cluster.selfCodeInventory` per the life-curriculum pattern so browser-bundled code never imports fs; cached per boot; 40 speakable stems from 58 files measured live) and `teachSelfCode` teaches "one of my files is called <stem>" beside the authored facts, still behind the cs-capstone gate. Browser context: typeof-guarded, authored facts stand alone.

- [x] **✅ LANGHOP.2 + SELFAWARE.PRESS CLOSED — VERIFIED LIVE 2026-08-29 07:1xZ (both brains restarted on the shipped trees, booted one second apart):** the LANGRAM.6 pins HELD through the Savestart — local cortex **16,845,450** (wordMotor 1,010,727 = 6% share; EXACTLY the priced affordable size for the 16GB card), box cortex **15,082,733** (wordMotor 904,964; inside the predicted 14.4-16.8M RAM-governed band — its fresh-walk boot derived it, the pin froze it). Firing settled at the accepted floor on BOTH (~9.7%, ema ~10.0, driveScale pinned 0.01 by design, 100+ heartbeats, zero misses), Ψ ~21, the walk RESUMED in place (ela/kindergarten, ledger intact — a true Savestart, not a wipe). **MINDMOTION.3 CAUGHT LIVE:** two `transition:` frames observed in six viewer polls (island→squid morphs, union coefficient counts 92k/141k above the finals' 70k/113k — the exact union-lerp shape). The remaining board rows are all walk-measurement-gated: GATEDOSE.1/RELDEPTH.1 (next math-gate run), PRESSBLOCK ③④ (consolidation passes > 0 / first accepted emissions), VMUSE.5.D (bands separating), ASSOCBOUND.1 (hours of the heavy stack), GOTCHA.1 (Gee's standing leave-documented call), SELFCODE.2's teach moment (the cs capstones, days away).

## SHADOWCOST — the heaviest op IS on the GPU, but we re-run it on CPU every 5th call and that shadow is 45% of the boot — filed 2026-08-30

Gee (verbatim): *"is it noraml the heavioest op is being run on cpu? The single heaviest op in the walk is running on the CPU-only box while the A40 idles. that seems like we are fucking our selves, or does it have to be this way"*

Gee (verbatim): *"start in the best order"*

**The live read that filed this** — box `07bf16f8`, **15.68 h** uptime, parked in `ela/kindergarten` **cell phase 2 of 25**, one `_teachAssociationPairs` call open **14.88 h** (8,428 glue pairs × 60 reps + 718 lead-ins × 80 = **563,120 pair-writes**, measured off the real corpus; at the live 4.76 writes/sec that one call needs **~33 h**). ⛔ **Correction owed on my own first answer:** the A40 is NOT idle on the heavy op — `hebbianRanges` dispatches the intra Oja to the donor **every call** (`js/brain/cluster/hebbian.js:1151`). What costs the hours is the CPU **shadow** at line 1158 (`_intraOjaShadowCounter % 5 === 0`): 268,660 intra calls ÷ 5 = 53,732 CPU passes × ~473 ms = 25.4M ms, against the board's reported `hebbian.intraMs` of **25,401,320** — the shadow IS essentially the whole **7.06 h**, 45% of the boot. **The same defect was already diagnosed and cured 110 lines up in the same function:** the sibling `hebbianBound` branch (lines 1029–1046) was moved off every-Nth-call onto a **30-second wall-clock gap**, its comment stating that the every-Nth sampler *"scaled shadow cost WITH the call rate … the faster the walk ran the MORE 3.9s passes it paid."* The `hebbianRanges` branch never got it. One function, two branches, one fixed.

- [x] **✅ BUILT 2026-08-30 — published at the `stageProfile` chokepoint (`curriculum.js:3638`) next to `chunkerHops`, and ⛔ the harness immediately DISPROVED the reading I gave Gee: dividing `hebbian.intraMs` by the `% 5` cadence CANNOT settle who runs the op, because the same stage total is produced by 20 GPU-carried calls with 4 cheap shadows (`cpuMs` 43) and by 20 full CPU passes (`cpuMs` 291) — few-expensive and many-cheap are indistinguishable in a total. So the counter now records the DISCRIMINATOR: `rangesNullPost` / `rangesNullPre` / `rangesThrew` (whether the spike pattern compressed into ranges at all — teach writes are group-tiled and should, a scattered pattern cannot, and that alone decides whether the CPU runs 20% or 100% of the op), `cpuMs` (the shadow times its own wall cost, so "what would widening the cadence buy?" is a subtraction not a re-derivation), plus `activeSum`/`calls`. ⚠ Both branches counted — the bound path's two early returns (intermediate rep, inside the 30s gap) never reached the ranges-branch counter, so a board reading only that one would under-report GPU-carried work and inflate the CPU share; `boundGpu`/`boundNoShadow` close it, every bump through `|| 0` so whichever branch initialises the object first, both still tally. Verified on the REAL mixin: tiled → `gpu 20 / cpuShadow 4`, scattered → `gpu 0 / cpuFull 20 / rangesNullPost 20`. Original filing:** `SHADOWCOST.1` — **publish `_intraOjaStats`.** `{ gpu, cpuFull, cpuShadow }` is incremented at `hebbian.js:1156` and read **NOWHERE** — the one counter that answers Gee's question directly is a dead producer, which is why the answer above had to be derived by arithmetic instead of read off the board. Publish at the existing `stageProfile` chokepoint (`curriculum.js:3638`), not a new lane.
- [x] **✅ BUILT 2026-08-30 — tunnelled through `/public-state.json?parity=` (the console-ring idiom, for the same nginx reason), and it grew a cheap mode plus a bug fix it could not ship without. ⚠ The full digest is NOT a free read** — `_cpuMasterMatrixChecksum` walks every weight byte with BigInt arithmetic: ~452M nnz × 4B = ~1.8 BILLION byte steps, **measured at ~5 minutes** (extrapolated from a real 3M-value run at 2,051 ms, checksum stable across runs), plus a full CPU propagate for mode 2 — so wiring that to a public URL unguarded would have been a public trigger for a multi-minute CPU burn mid-walk. Shipped instead: **`?parity=samples`** = `parityCheckMatrixSamples` (new), which compares the ≤64 sampled `{idx,val}` the donor ALREADY ships beside its digest against 64 CPU array reads, in `Math.fround` space (the CPU keeps Float64 and the upload downcasts — raw f64 would report the wire format as divergence), and reports **`cpuOverGpu`**, the ratio that answers the actual question; **`?parity=run`** = the exact digest, background, one at a time, floored by `DREAM_PARITY_MIN_GAP_MS` (default 30 min), reporting its own `costMs`; **`?parity=`** = both cached verdicts, free. 7/7 harness cases on the real mixin — identical→MATCH, **a CPU copy at 1/5 training→DIVERGENT with `cpuOverGpu` 0.2000 (the exact signature the `% 5` shadow would leave)**, nnz mismatch→STALE, not-held→STALE, no donor→UNKNOWN, f32 rounding→MATCH, no CPU matrix→UNKNOWN. ⛔ **Found and fixed en route — a yield cadence that was never a cadence:** the digest yielded on `(i & (CHUNK - 1)) === (CHUNK - 1)` with `CHUNK = 1_000_000`, the power-of-two mask idiom applied to a decimal constant. 999,999 is `0xF423F`, twelve bits set, so the test means "i has all twelve bits set" — ~1 step in 4,096, clustered (999999, 1000063, 1000127, …). Over 1.8B steps that is **~440,000 yields where ~1,700 were intended**, each paying the loop's full backlog. Now `1 << 20`; hash output unchanged, only when the walk gets its slot back. Original filing:** `SHADOWCOST.2` — **make the CPU↔GPU weight verdict readable from outside the box.** The instrument already EXISTS and already defaults to `cortex_intraSynapses`: `parityCheckMatrix` (`gpu.js:5045`) runs a 3-mode verdict (STALE / GPU-DIVERGENT / MATH-ERROR / CLEAN) off `readback_matrix_checksum` + FNV-1a-64 over the CPU master. It is served at `/diag/parity` and **gated loopback-only**, on a box that deploys by dashboard press only — so it has never been readable. Tunnel it through `/public-state.json?param` the way CONSOLERING did. ⚠ `state.cortexDivergence` is NOT this check: it compares **spike counts** against `lastSpikes` (`server/brain-server/state.js:1960`) and is currently reading `0` with an empty `byRegion`, which is its early-exit value, not a measurement.
- [~] **⏳ BUILT 2026-08-30, AWAITING THE RELEASE + ONE PRESS — donor `readback_matrix_values` (v0.3.36) + the server half.** ⭐ **The checkpoint now saves the weights the GPU actually trained.** **Donor:** new opcode streams the resident values buffer as type=7 SPRR binary chunks (32-byte header so the f32 payload lands 8-byte aligned; `byteOffset` split across TWO u32 because the intra matrix is already 1.81 GB and a u32 wraps silently at 4 GiB, reassembling two chunks onto the same destination with every chunk still individually valid), closed by a JSON ack carrying FNV-1a-64 over exactly the bytes sent. Sliced ON THE DEVICE in both backends — wgpu `copy_buffer_to_buffer` at an offset, CUDA a `CudaSlice` view — never `memcpy_dtov` of the whole buffer, which would allocate 1.81 GB **per chunk**. **Server:** streams each chunk widened f32→f64 straight into `matrix.values` (no multi-GB staging), verifies chunk count + order + overrun + digest, and ⛔ **does NOT save on failure** — a partial transfer leaves the array a mix of old-CPU and new-GPU rows and a checkpoint from that is a *third* brain, so the coherent CHECKROT slots stay. **Cadence priced before shipping:** ~2.3 GB ≈ 59 s at the measured 39 MB/s → hourly = **1.6% of wall clock**, crash loss ≤ 1 h; every-save would be **20%** on the teach socket. Plus a budgeted pre-stop pull on SIGTERM so a *planned* stop loses nothing (bounded because the shutdown save already pins the loop ~112 s and an unbounded transfer could turn a clean stop into a SIGKILL that WDCLEAN.1 would correctly call a hard death). Version-gated ≥ 0.3.36 per socket. **Verified:** `cargo check` on BOTH feature sets (`dynamic-loading` means the CUDA path compiles without the toolkit, so neither backend ships compiler-unverified), frame layout is a TEST including a 5,000,000,000-byte offset, **the donor's own emitted bytes parsed by the server's exact arithmetic**, and reassembly harnessed on the real mixin — happy path bit-exact, and a flipped byte / dropped chunk / overrun / not-found each caught by name. ✅ **RELEASED + ALL FOUR SURFACES VERIFIED 2026-08-30** (Gee: *"okay the binary has deployed.... whats next...?"*): tag `donor-v0.3.36` published by CI after 390 s · both assets attached (`unity-donor-linux-x86_64` 17.8 MB, `unity-donor-windows-x86_64.exe` 12.4 MB) · `html/compute.html` + `html/legend.html` both reading `donor-v0.3.36` on the DEPLOYED site · and ⭐ **the shipped binary downloaded and RUN — `unity-donor 0.3.36`, and its own `--self-test` emits the byte-identical values-chunk frame hex the server's parser was already checked against, so cross-language parity is proved against the actual artifact rather than my local build.** `CURRENT_DONOR_VERSION` 0.3.35 → **0.3.36** only after that. ⛔ **AND IT IS A SAVESTART, NOT A FRESH WALK — verified, not assumed:** `WEIGHTS_FORMAT_VERSION` is untouched at **6** and a diff across the whole session shows **zero** changes to `WORD_MOTOR_TARGET`, `langCortexSize`, `VM_FILE` or any cluster-shape constant. ⏳ **Remaining: one Update & Savestart.** The pod connected before the release existed so it is still on 0.3.35; the press restarts the server on the new recommended version, the donor disconnects, sees it, exits, and the launcher installs 0.3.36 on reconnect. Original filing: ⭐ **THE LAST ONE, AND THE ONLY ONE LEFT THAT IS NOT WALK-GATED — held deliberately per Gee 2026-08-30: *"we are fixing all the todo items befoer we deploy the new binary and shit"*.** Every other board row is now either closed or waiting on a number the walk has to produce. `SHADOWCOST.8` promoted this from "nice to have" to **the only fix**: the CPU copy is not a lagging copy of the GPU brain, it trains on a different spike state (94% of GPU dispatches go through `hebbian_bound`, which reads the donor's RESIDENT buffers the CPU never sees) at 1/49th the rate — so no amount of CPU shadowing will ever reconstruct it, and the checkpoint on disk is a different brain than the one that has been learning. Needs a new donor opcode: Rust change, `donor-v*` tag, CI builds both binaries, every donor self-updates. Mine end to end per the donor-tag rule. Original filing: `SHADOWCOST.3` — **GPU readback for checkpoints.** `_collectBinarySections` (`server/brain-server.js:7561`) saves `cortex.synapses` off the **CPU** values array and line 7568 says outright *"GPU readback not yet wired"* — so the brain on disk is the shadow copy, which at `% 5` received 1 in 5 of the intra updates (~12 of 60 reps; `curriculum.js:15994` records that 12 reps gave margins *"~0.003, barely above noise"*, which is why they went to 24). ⛔ **Scope correction owed:** the donor protocol has **no matrix-values download op** — the full type list is `clear_spike_region · compute_batch · compute_request · gpu_init · hebbian_ranges · letter_surprise · mindspace_op · mindspace_result · readback_letter_buckets · readback_matrix_checksum · rebind_sparse · write_current_slice · write_spike_slice`. Only a checksum and ≤64 sampled values come back. Real readback = a NEW donor opcode = Rust change + donor release + every donor updates. Not a same-day item; mine end-to-end per the donor-tag rule.
- [x] **✅ BUILT 2026-08-30 — `SHADOWCOST.5`, the lever the instrument found, which is NOT the one `.4` was aimed at.** The press landed and the counter answered in eight minutes: `rangesNullPost` on **24.2% of intra calls** (176 of 727), and every refusal becomes a full CPU pass — **`cpuFull` 176 vs `cpuShadow` 109**, so most of the CPU time in the heaviest op was never the shadow cadence. Read the donor's own source instead of assuming: `Work::HebbianRanges` (`donor-app/src/donor.rs`) expands with `if len == 0 || len > 2_000_000 || v.len() + len > 2_000_000 { continue; }` — a cap on TOTAL INDICES and on any single range, **no cap at all on the NUMBER of ranges** (`pre_ranges: Vec<[u32; 2]>`, unbounded). So `RANGE_MAX_TOTAL` mirrors a real donor limit and stays; `RANGE_MAX_RUNS = 512` was ours. **Raised to 8,192** (`DREAM_RANGE_MAX_RUNS`). **RE-PRICE measured, not estimated:** 8,192 runs both sides = **208.1 KB = 5.46 ms** at the measured 39 MB/s uplink, replacing a **123.8 ms** CPU pass (`cpuMs` 35,288 / 285) that also pays the loop backlog on every chunk yield — **23× cheaper**, donor work unchanged (index count still capped at 2M). **Math verified against the donor's expansion logic transcribed from the Rust** — 1 / 600 / 5,000 / 8,191 runs, mixed lengths, and a 3,000-run dense pattern all re-expand byte-identical. Refusals are now NAMED (`rangesFail_empty` / `_runs` / `_total`, `rangesRunsMax`, `rangesRunsOkMax`) because an empty pattern's "full CPU pass" is a no-op while a scattered one is the walk's most expensive op, and the aggregate could not tell them apart; `cpuFullMs` / `cpuShadowMs` split for the same reason. ⚠ `typeof process` guard on the env read, not `process?.env` — optional chaining does not shield an undeclared identifier and this module is bundled for the browser. End-to-end harness on the real mixin: a 3,000-run pattern that WOULD have gone `cpuFull 10` under the old cap now reads **`gpu 10 / cpuFull 0`**; a 20,000-run pattern refuses honestly with `rangesFail_runs`; an empty one is correctly filed `rangesFail_empty`.
- [x] **✅ BUILT 2026-08-30 — `SHADOWCOST.6`: the run counter must not saturate at the cap.** Recording `runs = RANGE_MAX_RUNS` on every refusal reports *"at least this many"* forever — the one number that cannot answer *"did the raise help?"*. Both helpers now finish counting runs **without building them** (no push, no allocation) before refusing, so `rangesRunsMax` carries the TRUE count; the caller is about to spend >100 ms on a full CPU pass, so walking the remaining indices costs microseconds beside it. ⚠ **Harness caught an off-by-one:** the index-side continuation under-reported by exactly one on every refusal — `out` holds the COMPLETED runs, `[runStart..prev]` is not pushed yet, and the boundary value opens another, so `+2` not `+1`. Verified against an independent counter across stride-3 at 9,000 and 140,500, mixed run lengths, one-long-plus-many, both sides of the cap boundary, and a 30,000-run dense pattern — 7/7 true, zero saturation. ⛔ **And the baseline it exists to read got worse while I watched:** sixteen minutes into the same boot the refusal rate had gone **24.2% → 70.4%** (1,366 of 1,939) with mean active set 140,500 — **92.4% of CPU passes in the heaviest op are refusals, not shadows**, and `cpuMs` is 61.3% of the intra stage / 15.78% of the boot. **So 8,192 may not be enough and I am not claiming it is:** if those patterns are genuinely scattered, a ~140,500-run frame is ~4.5 MB ≈ 115 ms of wire against the 124 ms CPU pass it would replace — ranges would be the wrong carrier entirely, and the donor already has an index-river (SPRS type 3) and a masked form (type 13) for that shape. `rangesRunsMax` on the next press decides between "dial `DREAM_RANGE_MAX_RUNS` again" and "this lane needs a different verb."
- [x] **✅ BUILT 2026-08-30 — `SHADOWCOST.7`: the cap raised again, and this time it is a READ.** `.6` made the telemetry report true run counts instead of saturating, so the live box could finally be asked. At 9.1 min on the 8,192 build: `rangesRunsOkMax` **8,037** (the largest ACCEPTED pattern — crowding the cap, so it still binds), `rangesRunsMax` **51,330** (the true run count of the worst refusal), and the cost split that decides it — `cpuFull` 30 passes = **24,181 ms** against `cpuShadow` 160 passes = **2,850 ms**, i.e. **a refusal pass costs 806 ms against a shadow's 17.8 ms, 45×**, because refusals land on exactly the large scattered patterns that are expensive to walk. Those 30 passes are **24.2 s of the 27.0 s of all CPU** in the heaviest op. **8,192 → 65,536.** RE-PRICE measured on the live worst case rather than estimated: the actual 51,330-run pattern serialises both sides to **1.01 MB = 27 ms** at the measured 39 MB/s uplink against an **806 ms** CPU pass — **30× cheaper**; they fire 3.3×/min so the added uplink is ~0.12 MB/s against 39. Verified: the live 51,330-run worst case is now ACCEPTED and re-expands **byte-identical** through the donor's own logic; a 70,000-run pattern still refuses honestly with a true count; ⛔ **`RANGE_MAX_TOTAL` untouched and confirmed still refusing** — it mirrors the donor's real 2M expansion limit and two refusals in that same window were already `rangesFail_total`, which are CORRECT refusals that must stay.
- [x] **✅ ANSWERED 2026-08-30 BY READING BOTH IMPLEMENTATIONS — no harness needed, and the answer is (b).** ⭐ **Candidate (a), kernel disagreement, is REFUTED.** The donor's `plasticity.wgsl` computes `w = w*(1-eta) + eta*x` with `eta = |lr| * reward`; the CPU's `ojaUpdate` computes `w += lr*y*x - lr*y²*w`. With `lastSpikes` a **`Uint8Array`** (verified — so `y ∈ {0,1}` and `y² = y`) the CPU reduces to **`w(1-lr) + lr*x` — the identical function.** `reward` is hardcoded `1.0` at all four donor call sites, and the clamps match exactly: donor `w_min: -2.0, w_max: 2.0` against `new SparseMatrix(size, size, { wMin: -2.0, wMax: 2.0 })`. ⛔ **Candidate (b) is confirmed STRUCTURALLY, in the code, not by inference — and it is two things, not one.** ① **Different inputs:** `hebbian_bound` (compute.rs:666) passes `src_offset: b.src_start, dst_offset: b.dst_start` and reads the donor's **RESIDENT** spike buffers, while `hebbian_reps` (compute.rs:1165) passes offset 0 and writes dense spikes from OUR shipped ranges, and the CPU shadow uses `lastSpikes` at call time. The bound path is **8,771 of 9,322 dispatches — 94% of the GPU's training runs on a spike state the CPU never sees.** ② **Different update counts:** ~9,322 GPU dispatches against 190 CPU passes, **49×** — and under `w(1-lr) + lr·x` the number of applications changes the result even on identical inputs. ⛔ **Consequence: the CPU copy cannot be made to converge on the GPU copy by running more CPU passes, because it is not training on the same thing. `SHADOWCOST.3` is the only fix, and it is now the top of this stem.** Original filing: `SHADOWCOST.8` — ⛔ **THE SAVED COPY IS NOT A LAGGING COPY, IT IS A DIFFERENT BRAIN — and that is a bigger problem than the shadow cadence ever was.** Two parity points on the SAME boot, 247 s apart: `cpuOverGpu` **0.9704 → 1.0212** (`meanAbsCpu` 0.2151 → 0.2244 climbing while `meanAbsGpu` 0.2216 → 0.2198 falls), drift **+0.0124/min**. Across boots the same signal scales with CPU work: the previous boot ran **1,366** full CPU passes and read **1.68** at six minutes; this one runs **30** and sits near **1.0** — *more CPU passes, faster inflation*. So the CPU pass is not merely applying less of the same training, it is moving weights somewhere the donor's kernel does not. **Two candidate mechanisms and I am naming both rather than picking:** (a) the CPU Oja and the donor's plasticity kernel do not agree on magnitude, or (b) they agree but see DIFFERENT INPUTS — `hebbianBound` trains on the donor's RESIDENT spike state kept current by the teach-frame twins, while the CPU pass uses `lastSpikes` at call time, and if those diverge the two copies are trained on different patterns. (b) is the likelier of the two and neither is established. ⛔ **This kills any idea of reconstructing the GPU brain by running more CPU passes, which makes `SHADOWCOST.3` (donor readback) the only real fix and puts it ahead of `.4`.** Discriminator to build: a single controlled update — one known pattern, one dispatch each side, compare the resulting weights — rather than inferring from aggregates, which is how two wrong answers already got given today.
- [x] **✅ DECIDED 2026-08-30 — NOT WORTH DOING, and the numbers say so rather than a preference.** ⭐ **The prize evaporated:** `.5`/`.7` took `cpuMs` from 15.78% of the boot to 4.94% and heading to ~0.5%, and of what remains the shadow is `cpuShadowMs` **2,850 ms of 27,031** — about **10% of a 4.94% share ≈ 0.5% of the boot**, falling to ~0.05% once `.7` lands. ⛔ **The risk did not shrink with it:** the shadow's stated purpose is that the **gate probe** reads CPU arrays, so a staler array can change gate VERDICTS — and gate verdicts are what advance grades. Current baseline `gateProbe ok=20 miss=0 refused=0 avg 73ms`. **Trading a measured grade-advancement risk for 0.05% of wall clock is a bad trade, and taking it would be optimising the number that is already smallest.** ⚠ **`SHADOWCOST.8` also removes the premise this row was half-resting on:** widening the cadence was never going to make the checkpoint meaningfully staler, because the checkpoint is not a lagging copy of the GPU brain at all — but by the same token it cannot make it better either, so the fidelity argument neither saves nor condemns this change; the gate risk alone decides it. **The `hebbianBound` branch keeps its 30 s wall-clock gap; the `hebbianRanges` branch keeps `% 5`. No code moves.** Original filing: `SHADOWCOST.4` — ⚠ **RE-AIMED 2026-08-30 and now the SMALLER half.** The live split says the shadow cadence is 109 of 285 CPU passes; `.5` took the other 176. Still held, and the trade is unchanged: widening the cadence buys walk speed at the cost of checkpoint fidelity, and `?parity=samples` has now MEASURED that the saved copy is already diverging from the training copy in real time (`cpuOverGpu` 1.6804 → 1.7822 in 45 s), which makes `.3` the priority over `.4`. Original filing: **match the sibling's wall-clock shadow gap on the `hebbianRanges` branch.** RE-PRICE computed BEFORE the change, per the LAW: at a 30 s gap, 56,459 s ÷ 30 = 1,882 passes × 473 ms = **~15 minutes** in place of **7.06 h** — **~6.8 h of a 15.7 h boot returned, ~43% of wall clock.** ⛔ **HELD until `SHADOWCOST.2` returns a verdict** — alone this trades walk speed for checkpoint fidelity, and that is the wrong trade until `.3` exists OR `.2` proves the saved copy was never faithful anyway. Order matters: `.2` may make `.3` unnecessary, or may make `.4` free.

## REBINDWAIT — the 0.3.36 press: she is fine, the walk is teaching, and the boot has an 8-minute hole in it — filed 2026-08-30

Gee (verbatim): *"okay she is back up and trainin. but this looks wrong:Current Training — subject / grade / progress no cell active … we are good now to let her cook? it says teaching but no cell is active"*

**THE PRESS ITSELF WAS CLEAN, verified not assumed:** `Binary weights applied — 17/17 sections restored` (saveVersion 12, 6,862.2 MB), `LANGRAM.6 GEOMETRY PIN HELD — 15,082,717` (this boot's live bounds would have sized it **12,646,146**; the pin beat them), `GPU proxy ready: 17/17 matrices uploaded (FULL)`. `WEIGHTS_FORMAT_VERSION` untouched at 6 and zero geometry constants moved across the whole session — **Savestart, not fresh walk**, as promised.

⭐ **AND THE pre-K READING WAS CORRECT, NOT A RESET.** `passedCellsTotal` is **0** — she has never *passed* a cell — and since `WALKORDER.1` position comes from the `passedCells` ledger with **the ledger winning**, every boot resolves to the lowest owed cell, which is pre-K. She showed `ela/kindergarten` before only because that boot had climbed there. She climbed again this boot too: **at 12.0 min she is in `ela/kindergarten`, `_teachAssociationPairs`, `status in-progress`, at 3,743 teach/min** (against 279–544 on the previous boot — recorded as an observation, cause NOT claimed).

⚠ **`no cell active` while the donor row said `teaching` was two honest instruments describing different things** — the donor's "21 ops" was upload acks, and the curriculum's own line said it outright: *"runner quiet 5.0 min — EXPECTED: the canonical sparse upload is IN FLIGHT … the walk waits for `_cortexFullyReady` by design."*

- [x] **✅ FIXED + VERIFIED LIVE 2026-08-30 — THE CAUSE WAS MINE, AND IT WAS NOT THE DONOR AT ALL.** `SHADOWCOST.3` added `case 'readback_matrix_values_ack'` **with its own body IN THE MIDDLE of a seven-label fall-through chain**, severing **SIX** labels from the pending-resolve body they shared and routing them into the readback settler: `sparse_upload_ack` · `sparse_propagate_ack` · `sparse_hebbian_ack` · `rebind_sparse_ack` · `readback_letter_buckets_ack` · `letter_surprise_ack`. Every one of those pendings then **never resolved and rode its full timeout** — which is exactly the 16 × 30 s. ⛔ **It was never only the rebinds:** `readback_letter_buckets` (gate probes, motor argmax) and `letter_surprise` (episode salience) were broken the same way and nothing said so, because a timeout on those lanes has no loud line. **Fix:** the case moved BELOW the block; chain verified **byte-identical to the pre-regression tree (`349ec380~1`) across all seven labels**, and a scan of the whole file confirms it is the ONLY fall-through chain in it. **Verified live on `8dada846`: `rebinding 16 cortex cross-projections` — ONE line, sixteen silent successes, ZERO timeouts anywhere in the ring, `_cortexFullyReady = true`, and teaching in progress at 3.6 min against 11–12 min on the two broken boots.** ⚠ **WHY `node --check` COULD NOT SEE IT:** a fall-through chain is *syntactically identical* whether it is intact or hijacked — inserting a case with a body between labels and their body is valid JavaScript that silently reroutes six message types. A DO-NOT-INSERT-HERE warning now sits at the chain head naming this failure. ⛔⛔ **AND THE PROCESS LESSON, which is the part worth keeping: I SUSPECTED MY OWN BUILD FIRST, COULD NOT FIND THE MECHANISM, AND THEN TALKED MYSELF OUT OF A CORRECT SUSPICION** — I told Gee it was "not established" and probably pre-existing, citing a previous-boot snapshot **I had myself already labelled as taken too early to mean anything**. Trading a live suspicion for a comfortable verdict backed by evidence you have already called weak is worse than having no verdict. The rule earned: *when you suspect your own change and cannot find the mechanism, keep looking for the mechanism — do not promote "unestablished" to "pre-existing."* Original filing: `REBINDWAIT.1` — ⛔ **ALL SIXTEEN cross-projection rebinds time out at 30 s each, so `_cortexFullyReady` cannot flip for ~8 minutes and every projection lands on the degraded "standalone path".** Observed on this boot: `rebinding 16 cortex cross-projections` then sixteen consecutive `sparse dispatch reqId=N type=rebind_sparse timed out after 30000ms` / `rebind <name> failed — GPU Hebbian will still use standalone path for this projection`. The gate flipped at ~11 min and teaching started, so this is **dead time plus a capability degradation, not a hang.** ⛔ **The donor acks `rebind_sparse` INLINE with no GPU work — its own comment says *"so the brain doesn't hit its 30s rebind timeout"* — so a timeout means the donor's message path is blocked during that window, and the canonical-upload block lines are still printing through it.** ⚠ **RETRACTION I OWE: I implied the 0.3.36 donor caused this. That is NOT established.** My "previous boot was clean" evidence was a single console snapshot taken at **5.2 min**, before that boot had even reached the rebind phase — it is not evidence of anything. My readback is separately RULED OUT: zero `SHADOWCOST.3` lines in the ring, because the `_cortexUploadInFlight` guard refused it. **Establish whether this predates 0.3.36 by reading a full boot ring past the 12-minute mark before touching the donor** — and the honest first lever is the 30 s rebind timeout being far shorter than the upload window it is racing.
- [~] ⏳ **RE-READ LIVE 2026-08-30 on `838bfa6a` (115.6 min in) — the distribution this row asked for EXISTS NOW, and it is carried forward as `READBACKEYE.2` with the numbers written down.** `rangesFail_runs` **7,066** of **7,128** total fallbacks · buckets ≤2× **4,066** / ≤4× **1,957** / ≤16× **942** / >16× **163** · `rangesRunsMax` **1,633,555** · `rangesRunsOkMax` **65,183** (crowding the 65,536 cap, so it still binds). ⛔ **And the fall-through rate this row called "not urgent" HAS climbed, exactly as it warned to watch for:** `cpuFull` **7,128** (was 2) and `cpuFullMs` **1,576,315 ms = 22.7% of the boot**. **This row's own instruction — *"Re-price from `rangesRunsMax` rather than doubling on instinct"* — is what `READBACKEYE.2` executes.** Original filing: `REBINDWAIT.2` — ⚠ **`rangesRunsMax` is now `362,859`** (from 51,330 one press ago) against the `SHADOWCOST.7` cap of 65,536, with `activeSum` 520,549 over 2 calls. The cap is well behind the pattern sizes again. **It is not urgent** — the bound path is absorbing almost everything (`boundGpu` 1,338, `boundNoShadow` 1,336, only **2** calls fell through, `cpuMs` 612 total) — but at 362,859 runs a frame is ~11.6 MB, so the raise is still cheaper than an ~806 ms CPU pass if the fall-through rate ever climbs. **Re-price from `rangesRunsMax` rather than doubling on instinct; watch `cpuFull` and `cpuFullMs`.**
- [x] **✅ FIXED 2026-08-30 — `REBINDWAIT.3`, and it was MINE.** `refreshCheckpointFromDonor` gated on `_lastReadbackMs`, which is **undefined at boot**, so `!this._lastReadbackMs` made the gap check fall through and the **first scheduler tick would pull the whole ~6.8 GB five minutes into every boot** — in the exact window the canonical upload and the rebinds need the wire. On this press it was refused only by the `_cortexUploadInFlight` guard, i.e. **by luck of ordering rather than by the gate doing its job**. The clock is now seeded on first call, so the first real pull lands one full gap after boot, which is what the cadence was priced for; `force` (the pre-stop pull) still bypasses it. Verified across five paths on the real mixin: boot tick seeds and refuses, +5 min refused as inside-gap, +1 h pulls and saves, `force` bypasses, and donor 0.3.35 refused with the loud UNAVAILABLE warning.

## PHASELOOP — the walk could never pass its first cell, and it was one line — filed 2026-08-30

Gee (verbatim): *"back to the brain its been on phase 2 of elz for like close to 40 hours now including all the update savestarts,,, so what is up,, is this thing ever going to pass the first cell?"*

Gee (verbatim): *"go"*

**THE ANSWER WAS NO, AND IT WAS NOT SLOWNESS.** `js/brain/curriculum.js`, first line of the `_teachAssociationPairs` rep loop:

```js
for (let rep = 0; rep < reps; rep++) {
  if (globalThis._brainShutdownRequested) return { trained, skipped };   // banks NOTHING
  if (rep > 0 && cluster._phaseDeadlineAt && Date.now() > cluster._phaseDeadlineAt) {
    cluster._phaseRepCursor[_cursorKey] = reps - rep;                     // banks the remainder
```

⛔ **Two exits from the same loop at the same clean rep boundary: the budget exit saves its place, the shutdown exit throws it away.** And the budget exit never runs, because `PHASE_BUDGET_MS = 0` — Gee's own 2026-08-20 call, printed live as `NO PHASE BUDGET — this phase runs to completion however long it takes`.

**So every Update & Savestart:** shutdown flag set → the rep loop returns with no cursor → weights save fine (**the learning is NOT lost**) → on reboot `passedPhases` has no entry and `_phaseRepCursor` has no remainder → the full authored dose re-arms → `_teachSentenceStructure` restarts at `visit #1 · mode=FULL · effective dose ×1.000`. **The phase is longer than the gap between presses and nothing partial banks, so it cannot finish — not slowly, by construction.**

**LIVE EVIDENCE (`0139c186`, read via the `READBACKEYE.1` backward ring paging, which is what made the boot lines reachable at all):** exactly ONE `PHASE SKIPPED` line in the whole ring — `_teachCourseIdentity`, the trivial course-name phase — which is the entire `cellPhasesCompleted: 1 / 25`. `phaseWork done 0 / 14` after 33 min with a single `_teachAssociationPairs` call 32.6 min deep and still running. `passedCellsTotal` **0**.

⚠ **NOT lost training, and the distinction matters:** under Oja (`w = w(1-lr) + lr·x`) re-running the same reps converges to the same weights, so nothing is corrupted or harmfully double-counted. She HAS been learning this material for 40 hours. What she never gets is the **credit** that lets her advance — she is re-sitting a lesson because nothing wrote down that she had already done half of it.

- [x] **✅ BUILT 2026-08-30 on Gee's *"go"* — the cursor is banked at every rep boundary and the shutdown exit now saves its place.** The bank sits at the top of the rep loop (`cluster._phaseRepCursor[_cursorKey] = reps - rep`), so the in-memory cursor is at most one rep stale **whenever and however the process dies**; the shutdown exit banks and returns the SAME SHAPE as the budget stop (`repsDone` / `deferredReps` / `shutdownStopped`) so no caller can mistake an interrupted phase for a finished one, and it prints what it banked. **HARNESSED ON THE REAL `Curriculum.prototype._teachAssociationPairs`** — only the per-pair plasticity is stubbed; the loop, both exits and all cursor arithmetic are untouched production code. **11/11**, and the load-bearing one is the last: an authored dose of 60 reps × 2 pairs, interrupted TWICE, produced **exactly 120 pair-teaches in total** — taught once across three visits, not repeated. The visit chain reads `60 → banked 50 → resumed 50 → banked 47 → resumed 47 → completed`, `10 + 3 + 47 = 60`, and the cursor is DELETED on the clean finish (debt paid). An uninterrupted run is unchanged: full dose in one go, no cursor left behind. `node --check` + ESM `import()` clean, bundle rebuilt and carries the line. ⛔ **Temp harness deleted the moment it had run**, per the standing rule. Original filing: `PHASELOOP.1` — **bank the rep cursor at EVERY rep boundary, not only at a budget stop.** ⛔ **Why not just patch the shutdown exit:** `/shutdown` sets the flag, calls `_writeResumeMarker`, then `brain.stop()` which force-saves, and only THEN does the async rep loop reach its next boundary — so a bank on the way out can land AFTER the save that was supposed to carry it. Banking every rep has **no ordering dependency at all**, and it survives SIGKILL / OOM / power loss too, not just a clean press. Cost is one integer assignment against a rep that takes minutes. ⛔ **RE-PRICE: this is not a cut and not a bound.** No dose changes, no gate weakens, nothing is skipped — it stops DISCARDING work already paid for, so the walk gets strictly cheaper in cumulative terms and identical in what she is taught. `PHASE_BUDGET_MS` stays `0`; Gee's 2026-08-20 no-budget decision is untouched.

- [x] **✅ FIXED 2026-08-30 — `PHASELOOP.1b`, and it was a defect I introduced in the same batch, caught while pricing the phase rather than by a test.** `_teachConcreteSentences` trains three frequency buckets in a loop ending `if (_r.budgetStopped) break;` — **the only consumer of that flag in the whole tree.** `PHASELOOP.1` added a SECOND early exit (`shutdownStopped`) and did not update it, so after a shutdown the LO bucket would return correctly banked and the loop would then **start MID and HI**, each hitting the shutdown check at rep 0 and writing a cursor equal to the FULL dose. ⚠ Those entries are functionally inert — the resume guard requires `_owed < reps`, so they are ignored — but they are meaningless rows persisted into a cursor map **capped at 512 keys**: noise banked as if it were progress. Now `if (_r.budgetStopped || _r.shutdownStopped) break;`. ⭐ **The rule this earned: a new early-exit flag is not finished until every reader of its SIBLING has been grepped and updated** — `grep budgetStopped` returned exactly two hits, the producer and this one consumer, which is how the miss was found in one command.
- [ ] `PHASELOOP.2` — ⚠ **THE SIBLING, FILED RATHER THAN SILENTLY LEFT: `_teachQABinding` has the identical shutdown exit and NO cursor machinery at all.** `grep` on the exit line returns **two** hits, not one — `curriculum.js:13635` (`_teachQABinding`) and the one just fixed. The second loop has no `_cursorKey`, no `_phaseRepCursor` write and **no budget check either**, so an interrupted visit there discards its progress with nothing to bank it into. ⛔ **Filed, not fixed, and the reason is the standing lesson:** *bounding one caller of a shared hot method is fixing an INSTANCE* — but the honest counterpart is that fixing the OTHER instance blind is guessing. ⭐ **It is not what was blocking Gee's walk, and that is measured, not assumed:** `_teachQABinding` does not appear anywhere in `curriculum.liveness.teachProfile` on either the 115-min or the 36-min boot read, while `_teachAssociationPairs` holds the in-flight 32.6-minute call. **Decide it from a read: if `_teachQABinding` ever shows up in `teachProfile` with real ms, it needs the same cursor; until then adding the machinery would be unpriced work on a lane that may not run at K at all.**

## WALKCOST — where the 205 ms per pair-teach actually goes — filed 2026-08-30

Gee (verbatim): *"weeks per cell is not going to work"*

Gee (via ask-me-question, choosing between four levers): **"Measure batching first"** — *"Find out why a pair-teach costs 205ms when the CPU Oja in it is only ~26ms averaged."*

**⛔ THE ANSWER TO THE BATCHING QUESTION IS NO, AND THE PROFILE SETTLES IT.** A fresh 45-second self-profile taken **at 33.3 min uptime, inside the heavy `ELA-K-STRUCTURE-CONCRETE-SENTENCES-LO` call** (⚠ the profile already in state was **23 minutes stale**, sampled at 3 min during the definition bootstrap — sampling the wrong moment is the trap that has bitten three times today, so it was polled for until fresh):

| self-time | function |
|---:|---|
| 15.9% | `propagate @ sparse-matrix.js:566` |
| 15.0% | `intraSynapsesAntiHebbian @ hebbian.js:1570` |
| 13.5% | `intraSynapsesHebbian @ hebbian.js:1159` |
| 11.0% | `step @ cluster.js:3882` |
| 10.0% | `ojaUpdate @ sparse-matrix.js:760` |
| 8.1% | `_antiHebbianChunked @ hebbian.js:779` |
| **2.3%** | **idle — the coordinator is SATURATED** |

**The 205 ms is REAL PLASTICITY WORK, not framework overhead**, so there is no batching win hiding in it. ⚠ My *"~180 ms unaccounted"* framing was wrong: I compared against the intra-Oja CPU pass alone, but the anti-Hebbian and propagate run per pair-teach too. Retracted.

⛔ **AND ONE LANE IS GPU-INELIGIBLE BY CONSTRUCTION.** `intraSynapsesAntiHebbian` reaches the donor only when `pre === this.lastSpikes && post === this.lastSpikes`, but the contrastive teach **deliberately passes a sampled WRONG post-pattern** to push it away in weight space. The identity check can never pass on that path — that is 23.1% of CPU that no cap raise or verb swap on the existing ops can touch.

**THE OFFLOAD CEILING, PRICED:** mean active set **150,788 rows** (`activeSum` 696,187,861 / 4,617 calls), so an explicit index river is **1.15 MB = 29 ms of wire against a measured 113 ms CPU pass = 3.8×** on both plasticity lanes. ⛔ **But `propagate` and `step` do not move, so the overall ceiling is ~1.5× — 41 h → ~27 h. A donor release does not solve this, and that was established BEFORE building one.**

- [x] **✅ BUILT 2026-08-30 — `RELTTL.1`: ~8% of the walk was a diagnostic read that had produced NOTHING, and its own comment said it was free.** `_confidentRelationFor` cached every outcome for 30 s, while the mind's-eye FAV picker (`chat.js:1578`) samples **24 RANDOM keys per eye tick** from a store that holds thousands — random sampling over a large key space against a 30 s window is a cache that essentially never hits. **MEASURED LIVE, not reasoned:** `asks 3,734 · cached 206` = **5.5% hit rate**, so **3,528 full `sem_to_fineType` propagates in 38.4 minutes** (~1.53/s, ~51 ms each at the real shape ≈ **7.8% of one core**) — and `confident` read **0** for that entire window, so every one of them bought nothing. ⛔ The call site states this *"costs nothing today and nothing later"*; that was written from the cache's EXISTENCE rather than its HIT RATE, on a box measured at 2.3% idle. **FIX:** flat/unreadable reads cache for **30 min** (`DREAM_REL_USE_FLAT_TTL_MS`), confident reads keep **30 s** — `val` is null for flat and an object for confident, so the outcome selects its own TTL with no extra bookkeeping. ⭐ **The asymmetry is physical:** bands separate over the timescale of a WALK (`marginProgress` moves across hours), so a word flat now is flat in thirty seconds with certainty. ⚠ **Deliberately NOT a "skip while `confident === 0`" gate** — that would leave the feature unable to ever notice the bands separating, which is a cost gate resolving to *never*, i.e. a deletion wearing a bound's clothes. **Harnessed on the REAL `_confidentRelationFor`, 9/9:** flat cached at 2 min and 25 min (both misses before), flat DOES re-read past 30 min, confident still re-reads at 45 s, and a flat read is counted flat not confident. Temp harness deleted.
- [x] **✅ BUILT 2026-08-30 — `REPCOMP.1`, and Gee's instinct redirected this away from the dose cut I had offered.** Gee (verbatim): *"do we really need to do 100s of reps for everything? shes a real brain simulation, real people dont need to do something 100s of times to learn it"* → *"like 100 reps seems beuyoind what we really need and this can be applied to everything"*. ⭐ **HE IS RIGHT, AND THE CODEBASE ALREADY RECORDED WHY:** the `STRUCTURE_DOSE` note says *"the authored 100/80/60 were tuned when the language cortex was 349K–1.5M"* — it is **15,082,717** now, so that rep count was fitted to a brain **10–43× smaller** and never re-derived. **The rep count was never a biological quantity; it is an integration step count.** ⛔ **BUT CUTTING REPS ALONE IS A REAL CUT, which is precisely why `STRUCTURE_DOSE=0.4` was reverted.** Oja with binary spikes is `w = w(1-lr) + lr·x` (established in `SHADOWCOST.8` by reading BOTH kernels), so n reps reach `x·(1-(1-lr)ⁿ)`: **100 @ 0.03 = 95.2%**, **20 @ 0.03 = 45.6%** (half the training deleted), **20 @ 0.1413 = 95.2% — identical.** ⭐ **So `REPCOMP.1` solves for the lr that preserves the asymptote instead of scaling reps and leaving lr alone. It is dose-NEUTRAL by construction, which is a different operation from `STRUCTURE_DOSE` wearing a similar name.** Placed at the `_teachAssociationPairs` chokepoint so it reaches **every caller** — his *"applied to everything"* — and ⚠ **ordered BEFORE the `CELLBOUND.H` cursor resume**, so a resumed phase banks compressed reps and never solves for the wrong asymptote. ⚠ **`DREAM_REP_COMPRESS_LR_CEIL` (0.35) backs OFF the compression rather than clamping the lr** — clamping would silently under-train, the exact cut this exists not to be; consequence is a 100-rep dose compresses to at most 8 reps (12.5×). **Harnessed on the REAL method, 11/11:** default leaves 100 @ 0.03 untouched and logs nothing; compress=5 gives 20 reps @ 0.1413 with the asymptote held to **1e-9**; compress=50 still holds the asymptote and backs off to 8 reps inside the ceiling; `reps=1` untouched; the common small dose (8 @ 0.05) holds too. ⛔ **SHIPS DEFAULT-OFF at 1.0** — ⚠ **the risk is INTERFERENCE, not convergence**: reps run rep-major and that interleaving is what stops pair 7,250 flattening pair 1, so bigger steps disturb shared weights more per write. **That is unmeasured**, so it needs an operator number and a margin read, not an engineer's default. ⚠ **TDZ caught by the harness on its first run** — the log line read `label`, which is declared BELOW this block; `node --check` cannot see that. Uses `opts.label` inline now.
- [x] **✅ MEASURED + DEFAULT SET 2026-08-30 — `REPCOMP.3`: the number is 5.** Gee (verbatim): *"you need to find out what the compress number needs to be and continue working on what needs to be correctly done correct"*. **The experiment ran the REAL `SparseMatrix` and the REAL `ojaUpdate`** over synthetic pattern sets (patterns must be synthetic; everything else is production code), scoring **RETRIEVAL ACCURACY** — given a pre pattern, does the correct post still win the argmax against every other candidate. ⛔ **That is the metric that decides it, and the obvious one would have lied:** the aggregate **margin is preserved at every compression** (1.908 → 1.911, signal 2.526 → 2.520) **while retrieval can still collapse** — margin alone would have green-lit a setting that destroys recall. One more total that cannot separate. ⭐ **The governing variable is COLLISION LOAD** = `P·K²/COLS`, the expected number of other patterns sharing a given pre cell — not overlap alone and not pair count alone. **Production = `7,250 × 64 / 1,885,340` = 0.246.** | collision load | 5× | 8× | |---|---|---| | 25.0 (128× prod) | 95.5% | 78.5% | | 6.25 (25× prod) | **100%** | 89.5% | | 1.56 (6× prod) | **100%** | 95.5% | | **0.391 (1.6× HARDER than prod)** | **100%** | 99.0% | **5× holds at 100% from 25× the production load all the way down**, so it carries a very large margin; 8× is measurably worse at EVERY load for 1.6× more speed, and the LR ceiling caps useful compression at 12.5× regardless. ⚠ **MY FIRST RUN WOULD HAVE KILLED THIS CHANGE:** 500 pairs in 1024 columns = collision load 31, **127× production**, and it reported 5× at **48.8%**. A model harsher than production answers a different question — the same wrong-sample trap as the stale CPU profile and the bootstrap-sampled range distribution, now three for three in one session. **Default set to 5** (verified through the real method with no env: 100 reps → 20 @ lr 0.1413, asymptote 95.24% = authored 95.24%). **Effect on the blocking call: 725,000 → 145,000 pair-teaches, ~41 h → ~8.3 h.** Temp harnesses deleted.
- [x] **✅ FIXED 2026-08-30 — `REPCOMP.4`: compress only the regime the experiment actually validated, and never down to ONE rep.** ⛔ **Caught on the FIRST live boot after `REPCOMP.3`, in the box's own log** — not by a test: `[ARTWEIGHT-STRUCTURE] REPCOMP.1 — 4 reps → 1 reps × lr 0.1147`. The arithmetic is right (11.47% asymptote preserved exactly) but **the REGIME was never measured**: retrieval was validated at the 100-rep scale (100 → 20), and collapsing a 4-rep dose to a SINGLE presentation is a different question, because at `n=1` there is no interleaved reinforcement left at all — a pair writes once and every later pair's interference lands on it with no chance to re-assert. **The whole reason rep-major ordering works is that each pair gets to come back.** ⭐ **And compressing small doses buys nothing:** the cost is entirely in the big calls — the live log is `7,250 pairs × 100 reps` against ARTWEIGHT's `24 × 4` — so skipping them costs no measurable wall clock and removes an unvalidated regime, which is the trade every time. **Shipped:** `DREAM_REP_COMPRESS_MIN_DOSE` (12) and `DREAM_REP_COMPRESS_FLOOR` (4). ⚠ **Neither binds at the shipped 5×** — 100→20, 80→16, MID 150→30, HI 200→40 — they are guards against a larger future setting, not constraints on today's. **Harnessed on the real method, 9/9:** the validated 100→20 case is unchanged with an exact asymptote, 80→16 holds, **ARTWEIGHT's 4 reps is now left alone at lr 0.03**, 8 reps is left alone, 12 compresses but not below the floor. Temp harness deleted; env flags 205/205.
- [ ] `REPCOMP.2` — ⛔ **THE DEEPER ANSWER TO GEE'S QUESTION, AND IT OUTRANKS THE REP COUNT: real brains learn few-shot because of REPLAY, and hers has never run.** Live at 43.1 min: `tier1.totalEpisodes` **0** · `tier2.schemaCount` **0** · `promotedToTier2` **0** · `isDreaming` **false**. The whole hippocampal→consolidation→cortex path that makes few-shot learning possible in a real brain is built, wired, and producing **nothing**. ⭐ **`REPLAYOFF.4` already says this and states the ordering:** *"Interleaved replay is what SEPARATES representations, so once it genuinely runs, the same margin should be reachable with fewer waking reps — which is a large walk-cost saving"*, with ⛔ *"cutting reps before replay is verified removes teaching and blames the wrong thing when quality drops."* **So the 100 reps are partly compensating for a dead consolidation system, and finding out WHY `tier1` has zero episodes is worth more than any rep arithmetic.** Watch `tier1.totalEpisodes` leaving 0.

  ⭐⭐ **TRACED 2026-08-30 — IT IS A CLOSED LOOP, AND `PHASELOOP.1` WAS SITTING INSIDE IT.** Tier 1 has exactly **three** writers, and every one of them was dead during the walk:

  | writer | where | why it produced nothing |
  |---|---|---|
  | `_recordPhaseEpisode` | `curriculum.js:3163` | fires on **phase completion** — and phases were not completing, because the shutdown exit banked no cursor. **1 phase in ~40 h** |
  | WM age-out → `storeEpisode` | `memory.js:1091` | gated on **`!this._curriculumInProgress`** |
  | Tier-1 thinking heartbeat (30 s) | `memory.js:1110` | gated on the **same flag** |

  ⛔ **And this page's own Traps section already warned about that flag:** *"`_curriculumInProgress` is true for the ENTIRE multi-week walk. Gating anything on 'not mid-walk' means it never runs."* The gating was deliberate (the comment records a 2 s main-loop freeze it was fixing) and its stated escape is *"promotion resumes automatically in dream windows / idle / chat"* — ⚠ but during a continuous walk there is **no idle and no chat**, so a dream window is the only opening, and `tier1: 0` after 43 minutes says empirically that opening is not enough.

  ⭐ **THE DEADLOCK, stated plainly:** phases don't complete → no phase episode **and** no dream window (`_dreamWindow` is called at cell/phase boundaries) → Tier 1 stays empty → consolidation has nothing to promote so Tier 2 stays empty → **no replay, so representations never separate** → the waking reps have to do all the discrimination work → 100 reps → ~41 h per call → the phase cannot finish before the next press → **back to the top.** Each link was measured, not inferred.

  ⭐ **`PHASELOOP.1` and `REPCOMP.3` break it at two points** — progress now survives a press, and the blocking call is 5× shorter — so phases should start completing, which fires `_recordPhaseEpisode` **and** opens dream windows.

  ⛔ **THE FALSIFIABLE NEXT STEP, and it costs nothing: press, then watch `tier1.totalEpisodes`.** If it climbs once phases complete, the deadlock is broken and **no further code is needed here**. If it stays **0** while `cellPhasesCompleted` rises, then the two flag-gated writers are the cause and the honest move is an instrument — a counter for *storeEpisode called / refused and why* — **not a speculative change to the gate**, because the gate was put there to fix a real freeze and removing it blind trades one defect for another.
- [ ] `WALKCOST.2` — ⛔ **THE DECISION THAT IS ACTUALLY GEE'S, AND IT IS THE ONLY LEVER WITH A 5× IN IT.** Every implementation lever measured today totals ~1.65× (offload 1.5× + `RELTTL.1` ~8%), which turns 41 h into ~25 h and leaves weeks-per-cell intact. **The term that is too big is the DOSE:** `ELA-K-STRUCTURE-CONCRETE-SENTENCES-LO` is **7,250 pairs × 100 reps = 725,000 pair-teaches**, read from her own START line. The LO bucket is pairs seen **once or twice in the entire corpus** — the rarest tail — and `CELLBOUND.C` already records that the tail is 94% of the pairs and 99.7% of the phase's time. **LO at 20 reps = 145,000 pair-teaches ≈ 8.3 h, a straight 5×**, MID and HI untouched so the frequent connective tissue keeps its full dose. ⛔ **This is a REAL cut to training mass — the `STRUCTURE_DOSE` class — and Gee reverted one on 2026-08-20 (*"no we dont"*). It needs his word, not an engineer's judgement.** ⚠ Note it does change the LO:MID:HI ratio the frequency bucketing exists to encode; whether that is a distortion or a correction is a curriculum call.
- [~] ⏳ **INSTRUMENTED 2026-08-30 — `WALKCOST.3` could not be answered because the INSTRUMENT cannot answer it, and that is the finding.** Three theories were formed and all three died against a measurement: the walk heartbeat is a GPU batch on a 15 s timer (not a CPU `step`), `computeTransitionSurprise` is gated off above 2M cortex so it pushes `0` without calling, and the emission lane was measured live at **~5 attempts/min** (`emitAttempts` 4 → 10 → 12 over 92 s) — nowhere near 11%. ⛔ **That is not bad luck. The profiler sums SELF TIME and throws the call tree away, so *who called it* is structurally unanswerable and the only move left is guessing** — which is exactly how three wrong theories got formed. ⭐ **The tree was in the payload the whole time:** `profile.nodes` carries `children`, so a parent map costs one pass over nodes already in memory. **Shipped:** `callers` (top 3) on every hot function, in `state.profiling.cpuProfile.top[].callers` and on the console line. ⚠ **Attribution is weighted by each sample's TIME DELTA, not by sample count**, so a caller that invokes a hot function rarely-but-expensively cannot hide behind one that calls it constantly and cheaply — the same distinction `SHADOWCOST.1` had to learn when a stage total could not separate few-expensive from many-cheap. **Harnessed 5/5 on a synthetic CDP profile: the rare caller holding 92% of the time ranks FIRST above the caller with 8× more samples.** **Answer lands on the next boot's 30-minute profile — read `cpuProfile.top[].callers`.** Original filing: ⚠ **`step @ cluster.js:3882` is 11.0% of self-time during a teach phase and I could not name its caller.** The walk heartbeat is a GPU batch on a 15 s timer, not a CPU `step()`; `cluster.js:1524` states outright that *"the server's main tick never calls `cluster.step()` for the cortex"*. So something is stepping a cluster on the CPU inside the teach lane and 11% is not nothing. **Do not guess it — find the call site.** Also unresolved: `propagate` is 15.9% and `RELTTL.1` accounts for roughly half; the remainder has no named owner yet.

## READBACKEYE — the readback fires where nobody can see it, and the two copies are 22.8% apart — filed 2026-08-30

Gee (verbatim): *"okay the brains beenm up for a couple hours... anything we need to check on"*

Gee (verbatim): *"sounds like you have ur work cut out for you, so todo document and do what Unity needs"*

**THE LIVE READ THAT FILED THIS** — box `838bfa6a`, **115.6 min** up, `ela/kindergarten`, `walkTick` **439 sent / 439 ok**, `lastMissAt` **null**, teach **655 calls/min** (was 426), event-loop p99 **75 ms**, `definitionQueue` depth 67 / unresolved 0. ⭐ **The press landed and the falsifier written into `docs/RESUME.md` before it PASSED:** the readback did not starve the walk. ⭐ **And no press is owed** — the running commit `838bfa6a` IS the `REBINDWAIT.4/.5` merge; everything on `main` after it is docs plus a stray-screenshot removal, verified with `git log 838bfa6a..main`, no code.

⛔ **BUT `?parity=samples` READS `DRIFTING` — `cpuOverGpu` 0.7723, 22.8% apart, against a `DIVERGENT` tier that starts at 25%** — where the same read was **0.9939 (0.6%)** minutes after the press. **The mechanism is in the counters and it is by design, not a fault:** `boundNoShadow` **47,118** of `boundGpu` **60,524** — **78% of bound dispatches train the GPU with no CPU shadow at all**, exactly the `SHADOWCOST.8` finding. The hourly readback is the only thing that repairs it, so the question is whether the cadence outruns the drift — and **that question is currently unanswerable from outside the box**, which is what `READBACKEYE.1` exists to fix.

- [x] **✅ BUILT + VERIFIED LIVE ON `0139c186` 2026-08-30 — three parts, the third a defect one level up from the one filed, and a fourth found by reading the payload after the press.** ⛔ **PATH CORRECTION, owed and shape #1:** this row and two docs said **`state.readback`**; the real path is **`state.profiling.readback`**, because the block sits inside `_getProfilingState()` beside its sibling `cortexUpload` — **the enclosing function decides the path, not the assignment's indentation**, and a consumer reading `state.readback` gets `undefined`. That is the producer/consumer path mismatch, committed inside the fix for it, and it was caught only by reading the LIVE payload rather than trusting the write. ⭐ **LIVE at 8.8 min:** `refusals: {"first tick — clock seeded, first pull is one gap from now": 1}` then `"inside the readback gap": 1` — **`REBINDWAIT.3`'s boot-seed fix is confirmed working in production**, the first scheduler tick seeded and refused instead of pulling 6.8 GB five minutes into the boot. `okCount 0` / `lastOkAgeMs null` / `overdue null` (correctly `null`, not `false` — nothing to measure against yet) and `lastRefusal null`, so **the routine-exclusion rule works**: both refusals were counted and neither polluted the field. **Tunnels verified live through the public origin:** `?readback` returns the free read; `?console=500&before=<oldestTs>` returned a second page of 157 lines with **zero overlap** and `more:false` at the floor, surfacing the boot lines (`Binary weights applied — 17/17 sections restored`, `LANGRAM.6 GEOMETRY PIN HELD`) that the old 500-line window could never reach. ① **`state.profiling.readback` published** beside `cortexUpload` (`state.js`): `lastOkAgeMs` / `lastOkSecs` / `lastOkMB` / `lastOkMatrices` / `lastOkTrigger` / `okCount` / `secsMax`, the last NON-ROUTINE `lastRefusal` **with its age**, per-reason `refusals` counts, `gapMs`, and a **DERIVED** `overdue` (never a stored flag — a flag set at boot outlives the thing it described). ⚠ `lastOkAgeMs` is `null`, not `0`, until one completes: *"never run"* and *"ran just now"* must not share a rendering (MIRRORID.5). ⚠ Routine refusals (`inside the readback gap`, `first tick`) are **counted but excluded from `lastRefusal`** — this runs on a 5-min tick against a 1-h gap, so 11 of 12 calls refuse healthily and letting those hold the field would bury the one that means something under a permanent reassuring value. **Harnessed on the REAL `SERVER_GPU_MIXIN`, 13/13:** no-donor / old-donor / first-tick-seeds / inside-gap / upload-in-flight, each asserting both the count and whether it reached `lastRefusal`. ② **`?readback=` tunnel** (`brain-server.js`) — bare = free read, `=run` arms one, `&force=1` bypasses the gap, one at a time. ⚠ **AWAITED unlike `?parity=run`**, because a readback ends in a save and answering "started" would report the boring half while hiding whether it completed and what it cost — the exact instrument shape this item exists to stop shipping. ③ ⛔ **THE ONE I DID NOT FILE, AND IT IS WHY THE QUESTION WAS UNANSWERABLE: `?console=N` could only ever see the newest 500 of a 2,000-line ring.** `since` filters FORWARD and the handler then takes `.slice(-n)`, so **1,500 retained lines had no address at all** through the only route the public origin forwards. The 4:51 PM readback line was still in memory the whole time. **`&before=<ms>` pages backward** + `ringSize` / `oldestTs` / `more` as the cursor. **Verified by simulation of the exact handler expressions: 4 pages reach all 2,000, zero duplicates across pages, terminates at the floor, and page 1 is byte-for-byte the old behaviour** so existing callers are unaffected. `node --check` clean on all three files. Docs same commit: `ADMIN-CONTROLS.md` route rows for both tunnels, `KNOWN_ISSUES.md` KI-36 updated — ⛔ **and NOT closed**, because the window only went ~9 s → ~36 s and the real rule is unchanged: a measurement that happens once must be a field, not a line. Original filing: `READBACKEYE.1` — ⛔ **I BUILT THE READBACK AND LEFT IT UNREADABLE, WHICH IS THE SAME DEFECT CLASS AS AN INSTRUMENT NOBODY READS.** `refreshCheckpointFromDonor` records `this._lastReadbackOk = { at, reason, bytes, secs, matrices }` (`gpu.js:5238`) and publishes it **nowhere**; every refusal path returns a `reason` string that is **dropped on the floor**. The console ring caps at **500 lines ≈ 17 min of wall**, so the ~4:51 PM pull had already rolled off by the 5:42 PM read — I could not tell Gee whether the hourly readback fired at all, only that drift was at 22.8%. **Publish `state.readback`** (last-ok time + secs + MB + matrix count, last REFUSAL with its reason **and its age** — the `MIRRORID.5` / `CANSPEAK.8` rule: a stale field must read `idle`, never a number it earned an hour ago) **and add a force route** so the cadence can be TESTED instead of inferred. ⚠ The force route must be gated the way `?parity=run` is — a public trigger for a multi-GB transfer mid-walk is exactly the trap `SHADOWCOST.2` caught before it shipped.
- [x] **✅ BUILT 2026-08-30 — `READBACKEYE.3`, AND IT WAS FOUND ONLY BECAUSE GEE SAID TO READ THE REFERENCES FIRST.** Gee (verbatim): *"before you do anything else, you are lost with out refrences"* — I was mid-investigation, about to price a cap raise off `donor.rs`. ⛔ **The donor caps `hebbian_ranges` at SIXTEEN ranges, and our own ledger said so.** `docs/FINALIZED.md:415`, the `V0318.1` entry written the day the opcode shipped: *"donor.rs decode with defensive caps (reps ≤1000, **≤16 ranges**, ≤2M expansion)."* ⛔ **`SHADOWCOST.5` (mine, this morning) read only the EXECUTOR** (`donor.rs:912`, which caps total expanded indices at 2M) and wrote *"no cap at all on the NUMBER of ranges"* into **five documents** — `ADMIN-CONTROLS.md`, `WEBSOCKET.md`, `FINALIZED.md`, `TODO.md` and `wiki/modules/cortex-cluster.md` — then raised `RANGE_MAX_RUNS` **512 → 8,192 → 65,536** on that reading. **The ACCEPTANCE PATH is `donor.rs:1249`:** `if h.reps <= 1000 && h.pre_ranges.len() <= 16 && h.post_ranges.len() <= 16`, and over it the `if` simply does not push the work. ⛔ **The loss is silent from both ends:** `hebbian_ranges` sends **no ack** (verified — `Work::HebbianRanges` executes and returns nothing), and `gpuSparseHebbianRanges` returns `true` when the frame **leaves the socket**, never counting ranges; the caller stores that as `_gpuCarried` and then runs the CPU pass only every 5th call. **So an oversized frame is dropped by the donor AND skipped by the CPU — four of every five of those Oja updates landed nowhere at all**, while the dispatch-site comment read *"nothing is ever dropped."* `rangesRunsOkMax` **65,183** = an accepted frame **4,074×** past the donor's real limit. **FIXED:** `RANGE_MAX_RUNS` **65,536 → 16** (`DREAM_RANGE_MAX_RUNS` still overrides, and that is the upgrade path for the day a donor ships a raised handler cap — never to buy speed). **Verified at the boundary on the real module:** 16 scattered indices ACCEPTED, 17 REFUSED with `reason=runs trueRuns=17`, a 5-band pattern ACCEPTED, a 40-run pattern REFUSED with a true count of 40; `node --check` + ESM `import()` clean. ⚠ **RE-PRICE stated as a RANGE because the honest answer is unknown:** the ranges path is `gpu` 6,278 of `boundGpu` 60,524 = **9.4% of intra dispatches** (the bound path is a different opcode with no range guard, untouched). Mean full CPU pass = `cpuFullMs` 1,576,315 / `cpuFull` 7,128 = **221.1 ms**. **WORST case** (nothing compresses to ≤16): all 6,278 become full CPU passes = **+1,388 s on a 6,936 s boot, +20% of wall clock**, `cpuMs` 23.9% → ~43.9% — and in that world we were also losing 4/5 of every one of those updates, so the 20% is **a bill, not a regression**. **BEST case** (every accepted pattern already fit in 16): **zero cost and nothing was ever lost.** ⛔ **Which one is true is UNKNOWN and I am not claiming either** — `rangesRunsOkMax` is a MAX, and a max cannot price a cap (**the third time that has bitten in this one file**). **Correctness first: we do not trade her training for wall clock while the number is unknown.** ⭐ **Shipped in the same commit — the instrument that answers it:** the accepted side now records `rangesOk` + `rangesRunsOkSum` (so mean accepted runs is a read), measured on `Math.max(_preRanges.length, _postRanges.length)` — **the quantity the donor actually checks** — rather than `rangeFail.runs`, which holds whichever helper ran last. ⛔ **And the refusal buckets were made ABSOLUTE** (`le16/le64/le256/le1k/le8k/le64k/gt64k`): they were `RANGE_MAX_RUNS × 2/4/16`, so every bucket **silently changed meaning whenever the cap moved** — `le2x` meant ≤131,072 at a 65,536 cap and ≤32 at 16, and two boots' numbers could neither be compared nor added. **A bucket whose boundary is a variable is an instrument that lies by construction.**
- [~] ⏳ **ANSWERED 2026-08-30 BY THE LIVE READ ON `0139c186` — the distribution landed at 8.8 min and it names a donor release, not a bigger number on our side. Awaiting Gee's call on whether to spend that release.** ⭐ **The cap is now honored EXACTLY: `rangesRunsOkMax` 16** (was 65,183 — a frame 4,074× past the donor's limit), mean accepted **13.76** over **222** frames that are now genuinely reaching the card instead of being sent-and-discarded. **The 342 run-cap refusals, in ABSOLUTE buckets** (the 6 in `le16` are `rangesFail_total`, the donor's real 2M-index limit, and are CORRECT refusals that must stay): **`le64` 249 · `le256` 5 · `le1k` 18 · `le8k` 6 · `le64k` 23 · `gt64k` 41**, `rangesRunsMax` 283,407. ⛔ **72.8% of everything the cap refuses sits at 64 runs or fewer — four times the donor's limit, and nowhere near the wire being the problem.** A 64-run frame is ~2 KB on both sides against a CPU pass measured **right here at 217.7 ms** (`cpuFullMs` 75,776 / `cpuFull` 348). **Raising the DONOR's handler cap 16 → 1,024 converts 272 of 342 = 79.5%** at ~32 KB/frame worst case. ⭐ **And the tail vindicates the earlier finding rather than contradicting it:** the 41 frames above 64k (to 283,407 runs) genuinely do not compress, so *"ranges are the wrong carrier for those"* stands — it was only ever wrong as a statement about ALL of them. ⚠ **Cost of holding at 16, measured not projected:** `cpuFullMs` is **14.3% of this boot's wall clock** — well inside the ±20% band priced in `READBACKEYE.3` and nowhere near the worst case, so **nothing is on fire and there is no pressure to rush the release.** ⛔ **RE-PRICE for the donor change, written before it is built:** the range-COUNT cap at 16 is redundant with the 2M TOTAL-INDEX cap for memory safety — the index bound is what actually bounds expansion, and it is untouched. The Vec is allocated by serde BEFORE either check runs, so 16 never protected the parse; the WS frame-size limit does. Raising the handler to 1,024 while keeping `len <= 2_000_000` and `reps <= 1000` removes no real protection. **Needs: Rust change + `donor-v0.3.37` tag + CI builds both binaries + every donor self-updates + one press — mine end to end per the donor-tag rule.** Original filing: `READBACKEYE.2` — **`REBINDWAIT.2` finally has its DISTRIBUTION, and it prices the cap instead of guessing at it.** Live at 115.6 min: **7,066 of 7,128** CPU-full fallbacks are the run cap (`rangesFail_runs`), the other **62** are everything else (`rangesFail_total`, the donor's REAL 2M-index limit, which are CORRECT refusals). Buckets: **≤2× cap 4,066 · ≤4× 1,957 · ≤16× 942 · >16× 163**, worst single call **1,633,555 runs** against 65,536. So **65,536 → 262,144 converts 6,023 of 7,066 = 85%** of the fallbacks back onto the GPU. **The prize, measured:** `cpuFullMs` **1,576,315 ms of 6,936,000 ms of wall = 22.7% of the entire boot** spent doing Oja on the CPU that the GPU should have carried, and `cpuShadowMs` is only **82,545 ms (1.2%)** — so **95% of all CPU Oja cost is this fallback, not the shadow cadence**. ⛔ **RE-PRICE BEFORE THE CAP MOVES, and price the RIGHT quantity:** the donor's actual bound is `2_000_000` TOTAL INDICES (`Work::HebbianRanges`, `donor.rs`), not run count — read the index totals against that ceiling in the Rust before raising, because pricing runs and assuming indices follow is precisely how the readback throughput got quoted off an UPLOAD measurement. Raising a bound is not removing a gate, but the standing law wants the number written down first.



## ⛔ THE TRAINING CARD LISTED ONLY THE COURSES THAT HAD ALREADY RUN (appended 2026-08-31)

Gee (verbatim): *"and something im seeing is the current traing card doesnt have all the k grade subjects listed so when we get to them i wont see them, it stops at life when i know ther are more cources than that like pe and health and shit and geometry and algebra and social studies and governmtent and all of those for every grade that they have... so the cources are not properly listed so i dont know what will happen when we get to these phases and cells if there is no listing in the traing car of the dashboard for them"*

- [x] **✅ `ROSTERDECLARED.1/.2/.3` — row set seeded from `subjectsForGrade()` (the declaration) instead of three records of the past; `rosterUpcoming` names every course not yet opened with its opening grade; `SUBJECT_LABELS` grown to the full 20-subject roster.** Live box published **6** subjects at kindergarten where the roster is **9**. ⚠ **Server-side — needs an Update & Savestart press to take effect.** Full entry in `docs/FINALIZED.md` §2026-08-31.
- [ ] ⏳ **`PHASEBAR.1` — the within-phase progress bar cannot reach 100% at kindergarten, and its denominator is why.** Live read, two samples 417 s apart on `6b887dbe`: `phaseWork` = `{name: _teachLanguageMechanics, done: 0, total: 14, frac: 0}` in BOTH, while `cellSubPhases` moved **105,072 → 108,275** and `_teachHebbian` **+1,152 calls** — she is working, the bar is not measuring it. ⭐ **`total` is derived by regex over the method's own source** (`_teachNestedTotal`, every distinct `this._teach*(` in the 715-line band ladder = 14) — but at kindergarten **eleven of those fourteen are behind `atLeast(g1)` or higher and can never run**, so the denominator counts work the grade forbids. ⛔ **`done: 0` is separately unexplained** — `_teachConcreteSentences` has 8 completed calls and IS lexically named in the phase's source, so it should have been credited. **I am not shipping a mechanism story for that half**: the nested-cell-phase reset at `curriculum.js:2986-3003` and the identity-checked teardown at `:3125` are both plausible and I have not proved either. ⚠ **Read `cellSubPhases`, not `frac`, to answer "is she moving" until this is fixed.**

## ⛔ THE ACADEMIC CORPUS HAD 24 UNDECLARED CELLS (appended 2026-08-31)

Gee (verbatim): *"and check the vault it looks to me all grades are not correctly all have vocab set up from what i see some grades are missing, am i wrong? that needs coded up possibly with actual grade vocab for all the ciriculum"*

Gee (verbatim): *"well something is wrong this only says 18K on dashboard:📖 VOCABULARY (K→PhD) prefetched: yes defs taught: 2180 / 18,017 (12.1%)"*

- [x] **✅ `CORPUSGAP.1-.5` — 24 of 79 offered subject×grade cells in `corpora/academic/` were never declared in the fetcher's topic map; `cs` had ZERO for all 8 of its K-12 grades.** 192 topics authored against the scope-sequence and all 192 verified to resolve before fetching; 23/24 cells at full coverage, 2,850 sentences. Vocabulary regenerated **18,017 → 19,340 unique**. The wiki table Gee was reading had 17 of 19 `export` cells empty, which is what made it look like the files were missing. RE-PRICE: prose +39.6%, ~1.4 h added across the whole K→PhD walk, **nothing added below grade5** so tonight's run is unaffected. Full entry in `docs/FINALIZED.md` §2026-08-31.
- [ ] ⏳ **`CORPUSGAP.6` — `ela/college4` sits at 5 of 8 topics.** Three titles (`Literary theory`, `Postmodernism`, `Semiotics` class) resolve on the API but returned no usable extract across three passes. Not blocking — the cell has real content — but it is the one cell short of its declaration and a fourth pass or three substitute titles would close it.

## ⛔ REPLAY WAS NEVER RUNNING, AND ONE GATE IS WHY (appended 2026-08-31)

Gee (verbatim): *"okay, so your telling me we are going to need to do a fresh walk once you complete all the todo stuff, so wtf why in the hell would i keep training on a pod if we are going to freshwalk as soon as you do the fucking work, do the fucking work"*

- [x] **✅ `REPLAYGATE.1` — the Tier-1 episodic heartbeat was gated off whenever the curriculum runs, and the curriculum runs ~100% of the time.** `memory.js:1109` carried `!this._curriculumInProgress`, so nothing ever reached Tier 1 → nothing consolidated to Tier 2 → replay had no input. The tell was dead code inside the block: its first branch is `if (this._curriculumInProgress)`. Gate now reads the COST it was defending (`_surpriseCouldRun`), preserving the old protection below 2M cortex exactly; truth table 6/6. **Unblocks `REPLAYOFF.4`, `REPCOMP.2` and PRESSBLOCK ③.** ⚠ Server-side — needs one press. Full entry + RE-PRICE in `docs/FINALIZED.md` §2026-08-31.
