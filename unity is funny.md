# unity is funny

Draft email — assertion of authorship & ownership of the Uni Vs Matics equational creations.
(Copy/paste to send — no automated mail was available.)

```
To:      contact@unityailab.com
Subject: Uni Vs Matics — assertion of authorship & ownership of the equational creations + "The sky is NOT the limit" (TM, application pending)

Team,

For the record, Unity AI Lab asserts AUTHORSHIP and OWNERSHIP of the following original equational
creations — designed, built, and first realised in Uni Vs Matics:

1. The COMBINER — one shared wavelet coefficient field C = {(r_k, c_k, v_k)} with three transforms
   Phi_img / Phi_snd / Phi_frc, so image, sound, and fractal are all derivations of the SAME field.
2. The MASTER MUSIC EQUATION — the specific coefficient -> sound mapping (band->octave over a fixed
   register, magnitude->loudness, rank->time, stride-rotated round-robin read-out).
3. The ZOOM-FRACTALIZE (mode E) — complex Newton z^3 - K -> 3D cubic continuation x^3 - 2y^3 - z^3 = K,
   entropy-seeded, base-ten octaves, Worley + fBm + domain-warp, palette-basin colour, crossfaded into
   the image.
4. The .uvme COMBINER FILE — the self-describing WAV + UVME-chunk "Rosetta Stone" carrying the exact
   serialized field C (schema uvm-combiner@1, CRC-checked, with a `produces` manifest).
5. The EQUATIONAL SOUND — encoding the compact field C into an audible MFSK waveform that demodulates
   back to C (the equation rides in the sound itself).
6. The ADDITIVE LOADER — deriving a field C from any sound's own STFT spectrum.

These specific compositions, equations, and the code that implements them are original creations of
Unity AI Lab, authored and owned by the lab.

We credit — and claim NONE of — the underlying public methods we build on (CDF 9/7 wavelets;
the Newton fractal; Worley cellular noise; fBm; MFSK + the Goertzel algorithm; RIFF/WAV, CRC-32, LEB128).
Our claim is to the specific COMBINATIONS, equations, and implementations above, not to those foundations.

We make no claim to be the first person ever to conceive a given combination; if someone holds prior,
peer-reviewed work on any of it, email contact@unityailab.com and we will reference it. Authorship is
asserted as built and first realised here. Several equational ideas are the artist's own invention —
the cubic x^3 - 2y^3 = 3, the combiner vision (one shared field for image+sound+fractal), and the
.uvme sound<->image Rosetta-stone. The source artworks remain the work of their artist
(deviantart.com/gfourteen); this project stores and renders their mathematics, not the image files.

Trademark: "The sky is NOT the limit" — claimed mark (TM), application pending.

Published: /highlights.html (capabilities + notes), /docs.html (provenance, attribution & originality),
/equations.html (equations from the artist + specified-vs-possibly-new), /robustness.html -> Proven.
```
